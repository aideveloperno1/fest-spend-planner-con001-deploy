# 1. 근거 계산 계층 — 상세 구현 계획

문서 기준일: 2026-09-16
상위 문서: [작업진행.md](./작업진행.md) 1장
기준 문서: [개발업무 워크플로우](../개발업무_워크플로우.md) 8~9장, 12장
폴더 사양: [evidence/README.md](src/policy_signal_map/evidence/README.md) · [tests/fixtures/evidence/README.md](tests/fixtures/evidence/README.md) · [scripts/README.md](scripts/README.md)

이 문서는 1번 작업을 **그대로 따라 구현할 수 있을 정도로** 데이터 형태, 검증 규칙, 오류 문구, 사례 수치, 기대 결과, 테스트 목록을 정리한다.
경계 사례와 합성 파일의 모든 수치·기대 결과는 작성 시점에 정수·분수 연산으로 계산해 확인했다.

---

## 목차

1. [목표와 범위](#1-목표와-범위)
2. [확정한 결정](#2-확정한-결정)
3. [진행 순서](#3-진행-순서)
4. [1-1 설정](#4-1-1-설정)
5. [1-3a 근거 파일 형태 정의](#5-1-3a-근거-파일-형태-정의)
6. [1-2 경계 사례 22개](#6-1-2-경계-사례-22개)
7. [1-3b 검증과 로더](#7-1-3b-검증과-로더)
8. [1-4 비교 계산](#8-1-4-비교-계산)
9. [1-5 합성 근거 파일](#9-1-5-합성-근거-파일)
10. [1-6 공개 자료 검사와 의존 방향 검사](#10-1-6-공개-자료-검사와-의존-방향-검사)
11. [테스트 전체 목록](#11-테스트-전체-목록)
12. [커밋과 기록](#12-커밋과-기록)
13. [완료 기준](#13-완료-기준)
14. [위험과 대응](#14-위험과-대응)
15. [데이터 담당에게 확인할 점](#15-데이터-담당에게-확인할-점)

---

## 1. 목표와 범위

### 목표

데이터 분석 담당의 `review_evidence.json`을 읽어 **형식·상태값·수치 관계를 검증**하고, 인접 월의 **비교 A(외국인 금액 방향 vs 전체 분모 비중 방향)**와 **비교 B(전체 분모 비중 방향 vs 미상 제외 비중 방향)**를 기획 문서 규칙대로 정확히 계산한다.

### 범위에 포함

- 설정 읽기 (근거 파일 경로, LLM 제공자)
- 근거 파일 형태 정의·검증·로드
- 비교 계산과 기간·구간 요약
- 테스트용 경계 사례 22개와 생성 스크립트
- 시연용 합성 근거 파일과 생성 스크립트
- 공개 저장소 실제 자료 검사, 패키지 의존 방향 검사

### 범위에 포함하지 않음

| 항목 | 담당 단계 |
|---|---|
| 앱 시작 시 근거 파일 로드·금지 조합 차단 연결 | 2-1 |
| 억원·%·%p 표시 형식 | 2-1 `web/formatters.py` |
| 화면에 나갈 문장 | 3번 `review/`, 2-3 화면 |
| 원본 카드 CSV 집계 (F·T·U 계산) | 데이터 담당 (`../Analysis/`) |

### 산출물

| 구분 | 파일 |
|---|---|
| 코드 | `config.py`, `evidence/__init__.py`, `evidence/schema.py`, `evidence/loader.py`, `evidence/compare.py`, `evidence/summary.py` |
| 스크립트 | `scripts/_evidence_builder.py`, `scripts/build_fixtures.py`, `scripts/build_demo_evidence.py`, `scripts/check_public_bundle.py` |
| 자료 | `tests/fixtures/evidence/*.json` 22개, `resources/evidence/review_evidence_demo_v1.json`, `.env.example` |
| 테스트 | `test_config.py`, `test_evidence_loader.py`, `test_evidence_compare.py`, `test_evidence_summary.py`, `test_public_bundle.py`, `test_boundaries.py` |

---

## 2. 확정한 결정

| # | 결정 | 채택안 | 이유 |
|---|---|---|---|
| ① | `.env` 읽기 방식 | **`uv run --env-file .env ...`** 사용, 새 라이브러리 추가 없음 | uv가 기본 지원. 의존성을 늘리지 않음. 코드는 `os.environ`만 읽음 |
| ② | 경계 사례 JSON 관리 | **스크립트(`scripts/build_fixtures.py`)로 생성해서 커밋** | 형식이 바뀌면 스크립트 한 곳만 고쳐 22개를 다시 생성. 비중 값을 금액에서 계산해 사례끼리 어긋나지 않음 |
| ③ | 근거 파일에 모르는 필드가 있을 때 | **경고 후 계속 읽기** | 데이터 담당이 설명용 필드를 추가해도 서비스가 멈추지 않음. 경고는 로드 결과에 담아 화면·로그에 보여줌 |

결정 ②에 따라 기존 README의 "직접 관리" 문구를 1-2 커밋에서 고친다 (`scripts/README.md`, `tests/fixtures/evidence/README.md`).

### 검토 반영 (9/16)

계획 검토에서 발견한 문제를 아래처럼 본문에 반영했다. 괄호는 반영 위치.

| 구분 | 문제 | 반영 |
|---|---|---|
| A-1 | git이 한글 파일 이름을 이스케이프해 출력 → 검사 스크립트가 파일을 열지 못하고 건너뜀 (실행 확인) | `core.quotepath=false`, `-z` 사용, 열 수 없는 파일은 위반 처리 (10장) |
| A-2 | pytest 경로에 `scripts`가 없어 검사 스크립트 import 실패 (실행 확인) | `pyproject.toml` `pythonpath`에 `scripts` 추가 (10장, 12장) |
| A-3 | 오류 문구에 받은 값 출력 → 실제 금액 노출 | 금액·건수·비중 필드는 값 대신 형식만 표기 (7장) |
| A-4 | 형식이 틀린 값으로 관계 검사 시 예외, 틀린 상태값의 규칙 적용 불명확 (실행 확인) | 규칙별 선행 조건 명시 (7장) |
| A-5 | 폴더 README와 계획서 사양 불일치 | 커밋에 README 동기화 포함 (12장) |
| B-1 | 실제 자료 판단을 `data_kind` 표시에만 의존 | 경로 `private/`·이름 `_real_`도 실제 자료로 취급 (4장, 7장) |
| B-2 | 의존 방향 검사는 직접 import만 확인 | 문구를 "직접 import 금지"로 정정 (10장) |
| B-3 | 생성 스크립트와 커밋된 JSON 불일치 가능 | 재생성 비교 테스트 추가 (6장, 11장) |
| B-4 | 오류 사례 7번이 "한 곳만 바꾼다" 원칙과 모순 | 비중은 null, 금액만 0으로 정의 (6장) |
| B-5 | 없는 달(13월) 통과, 비중 정수 허용 불명확, 두 달 보류 사유 불명확 | M1·M8 규칙 보강, 보류 사유 표기 규칙 (7장, 8장) |
| B-6 | autocrlf 경고 반복, 생성 파일 줄바꿈 혼재 | `.gitattributes` 추가 (12장) |
| C-1 | 하루 분량 과다 | 9/16 커밋 1~3, 9/17 오전 커밋 4~5 (14장) |
| C-2 | 시도 레코드 매칭 규칙 미정 | `find_record`는 `region_key`만 받음, 매칭 규칙은 2-3 전 결정 (작업진행.md 결정 항목) |

---

## 3. 진행 순서

```
1-1 설정
  ↓
1-3a 형태 정의 (schema.py 앞부분: 데이터 형태·허용 값)
  ↓
1-2 경계 사례 생성 (_evidence_builder.py, build_fixtures.py, JSON 22개)
  ↓
1-3b 검증·로더 (schema.py 뒷부분, loader.py, test_evidence_loader.py)
  ↓
1-4 비교 계산 (compare.py, summary.py, 테스트 2개)
  ↓
1-5 합성 근거 파일 (build_demo_evidence.py)
  ↓
1-6 공개 자료 검사·의존 방향 검사
```

작업진행.md 번호와 순서가 다른 이유: 경계 사례 JSON을 만들려면 필드 구조가 코드로 먼저 정해져 있어야 한다.

---

## 4. 1-1 설정

### 만들 파일

- `src/policy_signal_map/config.py`
- `.env.example` (저장소 루트)
- `tests/test_config.py`

### 설정 항목

| 환경변수 | 필드 | 기본값 | 허용 값 |
|---|---|---|---|
| `PSM_EVIDENCE_PATH` | `evidence_path: Path` | `RESOURCES_DIR / "evidence" / "review_evidence_demo_v1.json"` | 파일 경로. 상대 경로는 실행 위치 기준 |
| `PSM_LLM_PROVIDER` | `llm_provider: str` | `"none"` | `none`, `cloud`, `local` |
| `PSM_LLM_BASE_URL` | `llm_base_url: str \| None` | None | local일 때 필수 |
| `PSM_LLM_MODEL` | `llm_model: str \| None` | None | cloud·local일 때 필수 |
| `PSM_LLM_API_KEY` | `llm_api_key: str \| None` | None | cloud일 때 필수 |

### 코드 형태

```python
@dataclass(frozen=True)
class Settings:
    evidence_path: Path
    llm_provider: Literal["none", "cloud", "local"]
    llm_base_url: str | None
    llm_model: str | None
    llm_api_key: str | None = field(repr=False)   # 로그·오류 출력에 키가 찍히지 않게

class SettingsError(Exception): ...

def load_settings(environ: Mapping[str, str] = os.environ) -> Settings
def check_llm_data_combination(settings: Settings, is_real_evidence: bool) -> None
```

- `is_real_evidence`는 `evidence/loader.py`의 `is_real_evidence(path, file)`로 판단한다: 레코드에 `real`이 있거나, **경로에 `private/` 폴더가 있거나, 파일 이름에 `_real_`이 있으면** 실제 자료로 본다 (B-1). 파일 안 표시가 잘못돼도 차단이 뚫리지 않게 한다

- `load_settings`: 허용되지 않는 제공자 값, cloud인데 모델·키 없음, local인데 주소·모델 없음 → `SettingsError(한국어 메시지)`
- `check_llm_data_combination`: `is_real_evidence and llm_provider == "cloud"` → `SettingsError("실제 분석 근거 파일과 클라우드 LLM은 함께 사용할 수 없습니다 …")`
- local + 실제 자료는 데이터 담당 합의 전이다. 이번에는 이 함수에서 **차단하지 않고 통과**시키되, 2-1에서 앱 시작 시 연결할 때 합의 결과에 따라 차단 여부를 정한다. 이 내용을 함수 docstring에 적는다

### `.env.example`

```
# 복사해서 .env로 저장하고 값을 채운다. .env는 git에 올라가지 않는다.
# 실행: uv run --env-file .env policy-signal-map

# 분석 근거 파일 (비우면 합성 파일 사용). 실제 파일은 private/ 아래에만 둔다.
PSM_EVIDENCE_PATH=

# LLM (C 단계 전까지 none)
PSM_LLM_PROVIDER=none
PSM_LLM_BASE_URL=
PSM_LLM_MODEL=
PSM_LLM_API_KEY=
```

- 빈 문자열은 "설정하지 않음"으로 처리한다

### 테스트 `test_config.py`

| 테스트 | 확인 |
|---|---|
| `test_defaults_use_demo_evidence_and_no_llm` | 환경변수 없음 → 합성 파일 경로, provider none |
| `test_empty_values_are_treated_as_unset` | `PSM_EVIDENCE_PATH=""` → 기본값 |
| `test_unknown_provider_is_rejected` | `PSM_LLM_PROVIDER=gpt` → SettingsError |
| `test_cloud_requires_model_and_key` | cloud, 키 없음 → SettingsError |
| `test_api_key_not_in_repr` | `repr(settings)`에 키 문자열 없음 |
| `test_real_evidence_with_cloud_is_blocked` | is_real=True, cloud → SettingsError |
| `test_real_evidence_without_llm_is_allowed` | is_real=True, none → 통과 |

---

## 5. 1-3a 근거 파일 형태 정의

### 위치

`src/policy_signal_map/evidence/schema.py` 앞부분

### 허용 값 상수 (형식이 바뀌면 이 부분만 고친다)

```python
SUPPORTED_SCHEMA_VERSIONS = frozenset({"2.0"})
DATA_KINDS = ("synthetic", "real")
GEOGRAPHIC_SCOPES = ("national", "sido")
POPULATIONS = ("foreign_code_3",)
INDUSTRY_SCOPES = ("all_provided",)
AGE_SCOPES = ("all",)
CALCULATION_STATUSES = ("ok", "no_data", "invalid_input", "invalid_denominator")
APPLICABILITY_STATUSES = ("allowed", "needs_review", "blocked")
REQUIRED_RULES = ("R07", "R06", "R02")
AMOUNT_UNITS = ("KRW",)
NATIONAL_REGION_KEY = "ALL"
SHARE_TOLERANCE_PP = Fraction(1, 10**6)   # 파일 비중과 재계산 비중의 허용 차이 (%p)
```

### 데이터 형태

필드 이름은 워크플로우 9-3장 JSON과 **같게** 둔다. 모두 `@dataclass(frozen=True)`.

```python
@dataclass(frozen=True)
class Scope:
    geographic_scope: str          # national | sido
    region_key: str                # national이면 "ALL"
    population: str
    industry_scope: str
    age_scope: str
    period_start: str              # "YYYY-MM"
    period_end: str

@dataclass(frozen=True)
class Applicability:
    status: str                    # allowed | needs_review | blocked
    reason: str

@dataclass(frozen=True)
class MonthValue:
    month: str
    calculation_status: str
    foreign_amount: int | None     # F
    total_amount: int | None       # T
    unknown_amount: int | None     # U
    transaction_count: int | None  # C (거래 건수)
    foreign_share_pct: float | None
    known_only_share_pct: float | None
    unknown_share_pct: float | None
    warnings: tuple[str, ...]

@dataclass(frozen=True)
class EvidenceRecord:
    evidence_id: str
    data_kind: str
    scope: Scope
    region_basis: str
    applicability: Mapping[str, Applicability]   # "R07", "R06", "R02"
    amount_unit: str
    months: tuple[MonthValue, ...]
    limitations: tuple[str, ...]

@dataclass(frozen=True)
class EvidenceFile:
    schema_version: str
    dataset_version: str
    records: tuple[EvidenceRecord, ...]

    @property
    def data_kind(self) -> str      # 레코드가 모두 같은 종류임이 검증된 뒤의 값
```

- 계산에 쓰는 값은 정수(F·T·U·C)뿐이다. 파일의 `*_share_pct`는 **대조·표시 참고용**이며 계산에 쓰지 않는다

---

## 6. 1-2 경계 사례 22개

### 만들 파일

| 파일 | 역할 |
|---|---|
| `scripts/_evidence_builder.py` | 월·레코드·파일 JSON 조립 함수. 1-5와 공유 |
| `scripts/build_fixtures.py` | 사례 정의 → `tests/fixtures/evidence/*.json` 22개 생성 |
| `tests/fixtures/evidence/*.json` | 생성 결과 (커밋) |

### `_evidence_builder.py` 함수

```python
def ok_month(month: str, F: int, T: int, U: int, C: int, warnings=()) -> dict
    # 비중을 정수에서 계산해 소수 10자리로 반올림 (9-3장 예시 8.8888888889 형식)
    # T - U == 0 이면 known_only_share_pct = None, warnings에 "미상 제외 분모 0" 추가

def status_month(month: str, status: str, *, F=None, T=None, U=None, C=None, warnings=()) -> dict
    # ok가 아닌 월. 비중은 모두 None

def record(evidence_id: str, months: list[dict], *, data_kind="synthetic",
           geographic_scope="national", region_key="ALL",
           applicability=None, limitations=None) -> dict
    # period_start/end는 months 첫·끝 월
    # applicability 기본값: R07 allowed / R06 blocked / R02 needs_review (사유 포함)

def evidence_file(records: list[dict], *, dataset_version: str,
                  schema_version="2.0", case: str | None = None) -> dict
    # case가 있으면 최상위 "_case" 필드로 설명 추가

def write_json(path: Path, data: dict) -> None
    # UTF-8, ensure_ascii=False, indent=2, 끝 줄바꿈
```

### 공통 규칙

- 월: `2026-01`부터 필요한 만큼 연속
- 거래 건수 C: 외국인 금액과 무관한 가상 값 (전체 금액 ÷ 20)
- `dataset_version`: `fixture-{파일이름}`
- `data_kind`: 모두 `synthetic` (단, `mixed_data_kind.json`의 두 번째 레코드만 `real`, 수치는 가짜)
- 오류 사례는 **정상 파일을 만든 뒤 한 곳만 바꾸는 방식**으로 생성해, 기대한 오류 외에 다른 오류가 섞이지 않게 한다. 테스트는 오류 사례마다 **오류 문구가 정확히 1개**인지도 확인한다 (금액을 바꾼 사례의 비중 불일치는 오류가 아닌 경고라 허용)
- 생성 스크립트는 `build_all() -> dict[파일이름, dict]` 함수를 제공하고, 테스트가 이 결과와 커밋된 파일을 비교한다 (B-3: 스크립트만 고치고 재생성을 잊는 경우 방지)

### 정상 사례 10개 — 비교 계산 확인

표의 비중은 %, 변화는 %p, 증감률은 %. 방향 표기: 금액/전체 분모 비중/미상 제외 비중.

#### ① `amount_up_share_down.json` — 외국인 금액 증가, 전체가 더 빠르게 증가 (8-5장 1행)

| 월 | F | T | U | 전체 분모 비중 | 미상 제외 비중 | 미상 비중 |
|---|---:|---:|---:|---:|---:|---:|
| 2026-01 | 800 | 10,000 | 1,000 | 8.0000 | 8.8889 | 10.0000 |
| 2026-02 | 900 | 12,000 | 1,200 | 7.5000 | 8.3333 | 10.0000 |

| 구간 | 방향 | 비교 A | 비교 B | 외국인 증감률 | 전체 증감률 | 비중 변화 | 미상 제외 변화 |
|---|---|---|---|---:|---:|---:|---:|
| 01→02 | up / down / down | **True** | False | +12.5 | +20.0 | −0.5 | −0.5556 |

#### ② `amount_down_share_up.json` — 외국인 금액 감소, 전체가 더 빠르게 감소 (8-5장 2행)

| 월 | F | T | U |
|---|---:|---:|---:|
| 2026-01 | 900 | 12,000 | 1,200 |
| 2026-02 | 800 | 10,000 | 1,000 |

| 구간 | 방향 | 비교 A | 비교 B | 외국인 증감률 | 전체 증감률 | 비중 변화 |
|---|---|---|---|---:|---:|---:|
| 01→02 | down / up / up | **True** | False | −11.1111 | −16.6667 | +0.5 |

#### ③ `same_direction.json` — 금액·비중 모두 증가 (8-5장 3행)

| 월 | F | T | U |
|---|---:|---:|---:|
| 2026-01 | 800 | 10,000 | 1,000 |
| 2026-02 | 1,000 | 11,000 | 1,100 |

| 구간 | 방향 | 비교 A | 비교 B | 비중 변화 |
|---|---|---|---|---:|
| 01→02 | up / up / up | False | False | +1.0909 |

#### ④ `flat_change.json` — 금액 변화 0, 비중 변화 0 (8-5장 4행)

| 월 | F | T | U | 전체 분모 비중 | 미상 제외 비중 |
|---|---:|---:|---:|---:|---:|
| 2026-01 | 800 | 10,000 | 1,000 | 8.0000 | 8.8889 |
| 2026-02 | 800 | 11,000 | 1,000 | 7.2727 | 8.0000 |
| 2026-03 | 1,600 | 22,000 | 2,000 | 7.2727 | 8.0000 |

| 구간 | 방향 | 비교 A | 비교 B | 확인할 점 |
|---|---|---|---|---|
| 01→02 | **flat** / down / down | False | False | 금액 flat이면 반대 아님, 증감률 0 |
| 02→03 | up / **flat** / **flat** | False | False | 비중 flat이면 반대 아님, 변화 0 |

#### ⑤ `prev_foreign_zero.json` — 이전 외국인 금액 0 (8-5장 5행)

| 월 | F | T | U | 경고 |
|---|---:|---:|---:|---|
| 2026-01 | 0 | 10,000 | 1,000 | "외국인 집단 관측 행 없음 (실제 소비 0으로 단정하지 않음)" |
| 2026-02 | 500 | 10,000 | 1,000 | — |

| 구간 | 방향 | 비교 A | 외국인 증감률 | 금액 차이 | 전체 증감률 |
|---|---|---|---|---:|---:|
| 01→02 | up / up / up | False | **None (계산 불가)** | +500 | 0 |

#### ⑥ `missing_month.json` — 월 자료 없음 (8-5장 6행)

| 월 | 상태 | F | T | U |
|---|---|---:|---:|---:|
| 2026-01 | ok | 800 | 10,000 | 1,000 |
| 2026-02 | **no_data** | null | null | null |
| 2026-03 | ok | 900 | 12,000 | 1,200 |

| 구간 | 결과 | 사유 |
|---|---|---|
| 01→02 | skipped | "2026-02 자료 없음(no_data)" |
| 02→03 | skipped | "2026-02 자료 없음(no_data)" |
| 01→03 | **만들지 않음** | 누락 월을 건너뛰어 잇지 않음 |

#### ⑦ `tiny_change.json` — 0.01%p 미만의 반대 방향

| 월 | F | T | U | 전체 분모 비중 |
|---|---:|---:|---:|---:|
| 2026-01 | 800 | 10,000 | 1,000 | 8.0000000000 |
| 2026-02 | 801 | 10,013 | 1,000 | 7.9996005193 |

| 구간 | 방향 | 비교 A | 비중 변화 | 확인할 점 |
|---|---|---|---:|---|
| 01→02 | up / down / down | **True** | −0.0003994807 | 변화가 매우 작아도 방향은 유지. 표시는 2-1에서 "0.01%p 미만 (감소)" |

교차곱: `801×10,000 − 800×10,013 = −400` → down

#### ⑧ `known_only_differs.json` — 비교 B만 반대

| 월 | F | T | U | 전체 분모 비중 | 미상 제외 비중 | 미상 비중 |
|---|---:|---:|---:|---:|---:|---:|
| 2026-01 | 800 | 10,000 | 1,000 | 8.0000 | 8.8889 | 10.0000 |
| 2026-02 | 790 | 10,500 | 2,000 | 7.5238 | 9.2941 | 19.0476 |

| 구간 | 방향 | 비교 A | 비교 B | 확인할 점 |
|---|---|---|---|---|
| 01→02 | down / down / **up** | False | **True** | 두 비교를 따로 계산하고 합치지 않음 |

#### ⑨ `large_amounts.json` — 소수점 계산으로는 방향이 틀리는 큰 금액

| 월 | F | T | U |
|---|---:|---:|---:|
| 2026-01 | 70,000,000,000,000 | 1,000,000,000,000,000 | 50,000,000,000,000 |
| 2026-02 | 70,000,000,000,004 | 1,000,000,000,000,057 | 50,000,000,000,000 |

| 판정 방식 | 전체 분모 비중 방향 |
|---|---|
| 정수 교차곱 `F1×T0 − F0×T1` | **up** (정답) |
| 소수점 `F1/T1 − F0/T0` | **flat** (틀림) |

| 구간 | 방향 | 비교 A | 비교 B |
|---|---|---|---|
| 01→02 | up / up / down | False | True |

이 사례는 워크플로우 8-2장이 "반올림한 퍼센트가 아니라 원래 금액의 부호로 판정"하도록 정한 이유를 테스트로 고정한다. 테스트는 소수점 방식이 flat을 내는 것까지 함께 확인한다.

#### ⑩ `unknown_equals_total.json` — 미상 제외 분모 0

| 월 | 상태 | F | T | U | 전체 분모 비중 | 미상 제외 비중 |
|---|---|---:|---:|---:|---:|---:|
| 2026-01 | ok | 800 | 10,000 | 1,000 | 8.0000 | 8.8889 |
| 2026-02 | ok | 0 | 5,000 | 5,000 | 0.0000 | **null** (경고 "미상 제외 분모 0") |

| 구간 | 방향 | 비교 A | 비교 B | 미상 제외 변화 |
|---|---|---|---|---|
| 01→02 | down / down / **None** | False | **None** | None |

- 전체 분모 비중 비교는 계산하고, 미상 제외 비교만 계산 불가로 둔다
- 이 경우의 `calculation_status`를 `ok`로 둘지 `invalid_denominator`로 둘지는 [데이터 담당 확인 대상](#15-데이터-담당에게-확인할-점). 현재 계획은 `ok` + 해당 비중 null + 경고

### 오류 사례 12개 — 로더 검증 확인

모두 ① `amount_up_share_down.json` 내용을 기준으로 한 곳만 바꾼다. 11번 `denominator_zero.json`만 로드에 성공해야 한다.

| # | 파일 | 바꾸는 곳 | 기대 결과 | 기대 오류 문구 (핵심어) |
|---|---|---|---|---|
| 1 | `invalid_status_value.json` | 02월 `calculation_status: "okay"` | EvidenceError | `허용되지 않는 값 "okay"` |
| 2 | `invalid_applicability.json` | `applicability.R07.status: "maybe"` | EvidenceError | `허용되지 않는 값 "maybe"` |
| 3 | `missing_reason.json` | `applicability.R06.reason: ""` | EvidenceError | `사유(reason)가 비어 있습니다` |
| 4 | `relation_broken.json` | 02월 `unknown_amount: 11200` (F+U > T) | EvidenceError | `외국인+미상 금액이 전체 금액보다 큽니다` |
| 5 | `negative_amount.json` | 01월 `foreign_amount: -800` | EvidenceError | `0 이상이어야 합니다` |
| 6 | `decimal_amount.json` | 01월 `foreign_amount: 800.5` | EvidenceError | `원 단위 정수여야 합니다` |
| 7 | `no_data_with_zero.json` | 02월 상태 `no_data`, 비중·건수는 null, **F·T·U만 0** | EvidenceError | `자료 없음(no_data) 월의 금액은 null이어야 합니다` |
| 8 | `month_omitted.json` | `period_end: 2026-03`인데 months는 01·02만 | EvidenceError | `2026-03 월이 없습니다 (누락 월도 상태와 null로 포함)` |
| 9 | `schema_version_unsupported.json` | `schema_version: "1.0"` | EvidenceError | `지원하지 않는 형식 버전 "1.0"` |
| 10 | `mixed_data_kind.json` | 레코드 2개, 두 번째 `data_kind: "real"` (가짜 수치) | EvidenceError | `합성 자료와 실제 자료가 한 파일에 섞여 있습니다` |
| 11 | `denominator_zero.json` | 02월 상태 `invalid_denominator`, F·T·U = 0, 비중 null | **로드 성공** | 비교 시 01→02 skipped |
| 12 | `national_region_key_wrong.json` | `geographic_scope: national`, `region_key: "11"` | EvidenceError | `전국 범위의 region_key는 "ALL"이어야 합니다` |

---

## 7. 1-3b 검증과 로더

### 위치

- `evidence/schema.py` 뒷부분: `parse_evidence(data: dict) -> ParseResult`
- `evidence/loader.py`: 파일 입출력, 레코드 찾기

### 검증 방식

- 표준 라이브러리만 사용 (pydantic 사용하지 않음 — 오류 문구를 한국어·위치 포함 형식으로 통제하기 위해)
- 필드를 하나씩 확인하며 오류를 **모아서** 끝에 한 번에 반환한다. 첫 오류에서 멈추지 않는다 (데이터 담당에게 한 번에 전달)
- 정수 확인은 `type(x) is int`로 한다 (`True`/`False`는 정수로 인정하지 않음, `800.0`도 거부)
- 오류가 하나라도 있으면 데이터 형태 객체를 만들지 않는다

### 오류·경고 문구 형식

```
[{evidence_id} / {month} / {field}] {내용} (받은 형식: {type})      ← 금액·건수·비중 필드
[{evidence_id} / {month} / {field}] {내용} (받은 값: "{value}")     ← 상태값·범위값·버전 등 글자 필드
```

- **금액(`*_amount`)·건수(`transaction_count`)·비중(`*_share_pct`) 필드는 값을 출력하지 않는다** (A-3). 실제 파일 오류가 화면·로그·메신저로 옮겨질 때 카드 수치가 새지 않게 한다. 형식은 `정수`, `소수`, `글자`, `참/거짓`, `null`, `목록`, `객체`로 표기
- 관계 오류(F+U>T 등)와 비중 불일치 경고에도 숫자를 넣지 않는다
- 글자 필드의 값은 40자에서 자른다
- 레코드 수준 오류는 월 칸 생략: `[DEMO-R07-01 / scope.region_key] …`
- 파일 수준 오류는 위치 `[파일]`
- evidence_id를 읽지 못한 레코드는 `[records[1]]`처럼 순번 표기

### 규칙 적용 선행 조건 (A-4)

형식이 틀린 값으로 다음 검사를 하면 프로그램 예외가 나거나 같은 원인의 오류가 여러 번 나온다. 그래서 규칙마다 앞 조건을 둔다.

| 규칙 | 적용 조건 |
|---|---|
| R4~R6, R13 (범위·기간·월 목록) | `scope`가 객체이고 해당 필드가 글자 |
| R13 | `period_start`·`period_end` 형식이 올바르고, 모든 월의 `month` 형식이 올바를 때만 |
| R8~R11 | `applicability`가 객체 |
| M4~M14 (상태별 규칙) | `calculation_status`가 허용 값일 때만. 틀리면 M2 오류 하나만 |
| M7 (금액 관계) | M4·M5를 통과한 월만 |
| M9·M10 (비중) | M4~M8을 통과한 월만 |
| F6·F7 (파일 전체) | 해당 필드가 읽힌 레코드만 대상 |

### 검증 규칙 전체

#### 파일 수준

| # | 규칙 | 결과 |
|---|---|---|
| F1 | 최상위가 JSON 객체 | 오류 |
| F2 | `schema_version`, `dataset_version`, `records` 존재 | 오류 |
| F3 | `schema_version`이 지원 목록에 있음 | 오류 ("데이터 담당에게 형식 버전을 확인하세요" 포함) |
| F4 | `dataset_version`이 빈 문자열이 아님 | 오류 |
| F5 | `records`가 1개 이상인 목록 | 오류 |
| F6 | `evidence_id` 중복 없음 | 오류 |
| F7 | 모든 레코드의 `data_kind`가 같음 | 오류 |
| F8 | `_`로 시작하는 최상위 필드 | 무시 |
| F9 | 그 밖의 모르는 최상위 필드 | 경고 |

#### 레코드 수준

| # | 규칙 | 결과 |
|---|---|---|
| R1 | 필수 필드: `evidence_id, data_kind, scope, region_basis, applicability, amount_unit, months, limitations` | 오류 |
| R2 | `data_kind` 허용 값 | 오류 |
| R3 | `scope`의 각 값이 허용 값 (`geographic_scope, population, industry_scope, age_scope`) | 오류 (지원하지 않는 범위로 계산하지 않음) |
| R4 | national이면 `region_key == "ALL"` | 오류 |
| R5 | sido면 `region_key`가 비어 있지 않고 `"ALL"`이 아님 | 오류 |
| R6 | `period_start`, `period_end`가 `YYYY-MM` 형식이고 시작 ≤ 끝 | 오류 |
| R7 | `region_basis`가 문자열 | 오류 |
| R8 | `applicability`에 R07·R06·R02가 모두 있음 | 오류 |
| R9 | 각 applicability `status` 허용 값 | 오류 |
| R10 | 각 applicability `reason`이 비어 있지 않은 문자열 | 오류 |
| R11 | applicability에 그 밖의 규칙 키 | 경고 |
| R12 | `amount_unit == "KRW"` | 오류 |
| R13 | `months`의 월이 기간의 모든 월을 **순서대로 한 번씩** 포함 | 오류 (빠진 월·중복 월·순서 뒤바뀜 각각 문구) |
| R14 | `limitations`가 문자열 목록 | 오류 |
| R15 | 레코드 안의 모르는 필드 | 경고 |

#### 월 수준

| # | 규칙 | 적용 상태 | 결과 |
|---|---|---|---|
| M1 | `month`가 `YYYY-MM` 형식이고 월이 01~12 (R6의 기간 값도 같은 규칙) | 모든 상태 | 오류 |
| M2 | `calculation_status` 허용 값 | 모든 상태 | 오류 |
| M3 | `warnings`가 문자열 목록 | 모든 상태 | 오류 |
| M4 | F·T·U·C가 모두 정수 | ok | 오류 |
| M5 | F·T·U·C ≥ 0 | ok, invalid_denominator | 오류 |
| M6 | T > 0 | ok | 오류 ("전체 금액이 0이면 invalid_denominator로 표시") |
| M7 | F ≤ T, U ≤ T, F + U ≤ T | ok | 오류 |
| M8 | `foreign_share_pct`, `unknown_share_pct`가 숫자 (정수·실수 모두 허용, 참/거짓 불가) | ok | 오류 |
| M9 | `known_only_share_pct`: T−U > 0이면 숫자, T−U = 0이면 null | ok | 오류 |
| M10 | 파일 비중 − 재계산 비중의 절대값 ≤ 0.000001%p | ok | **경고** (숫자를 고치지 않음) |
| M11 | F·T·U·C와 비중이 모두 null | no_data | 오류 (0이면 "no_data 월의 금액은 null") |
| M12 | 비중이 모두 null | invalid_denominator, invalid_input | 오류 |
| M13 | 금액이 있으면 정수 | invalid_denominator, invalid_input | 오류 |
| M14 | `warnings`가 비어 있음 | invalid_input | 경고 ("입력 오류 사유를 warnings에 적어 주세요") |
| M15 | 월 안의 모르는 필드 | 모든 상태 | 경고 |

### `loader.py`

```python
class EvidenceError(Exception):
    messages: list[str]          # 사용자에게 보여줄 한국어 문구
    path: Path | None

@dataclass(frozen=True)
class LoadResult:
    file: EvidenceFile
    warnings: tuple[str, ...]
    source_path: Path

def load_evidence(path: Path) -> LoadResult
def find_record(file: EvidenceFile, geographic_scope: str, region_key: str) -> EvidenceRecord | None
def has_real_records(file: EvidenceFile) -> bool
def is_real_evidence(path: Path, file: EvidenceFile | None) -> bool
    # has_real_records(file) 또는 경로에 "private" 폴더 또는 파일 이름에 "_real_" (B-1)
    # file이 None(로드 실패)이어도 경로만으로 판단
```

- `find_record`는 근거 파일의 `region_key` 문자열만 받는다. 입력 화면의 행정코드(예: `5100000000`)와 `region_key`를 어떻게 연결할지는 데이터 담당 Q5 답을 받아 **2-3 전에 정한다** (C-2). 정해지기 전에는 시도를 골라도 전국 참고로 표시되는 안전한 쪽으로 동작한다

| 상황 | 처리 |
|---|---|
| 파일 없음 | `EvidenceError(["근거 파일을 찾을 수 없습니다: {경로}"])` |
| UTF-8이 아님 | `EvidenceError(["근거 파일은 UTF-8이어야 합니다"])` |
| JSON 문법 오류 | `EvidenceError(["JSON 형식 오류: {줄}행 {칸}열 {내용}"])` |
| 검증 오류 | `EvidenceError(모든 오류 문구)` |
| 성공 | `LoadResult(file, warnings, path)` |

- `find_record`는 요청한 범위가 없으면 None을 반환한다. **전국 레코드로 자동 대체하지 않는다.** 대체해서 보여줄지는 화면(2-3)이 결정하고 "전국 참고"로 표시한다 (9-3장)
- 캐시하지 않는다. 앱 시작 시 한 번 읽어 보관하는 것은 2-1의 몫이다

### 테스트 `test_evidence_loader.py`

| 테스트 | 확인 |
|---|---|
| `test_valid_fixtures_load` (정상 10개 + `denominator_zero` 매개변수화) | 로드 성공 |
| `test_invalid_fixture_reports_expected_error` (오류 11개 매개변수화) | EvidenceError, 기대 핵심어 포함, **오류 문구 정확히 1개** |
| `test_amount_errors_do_not_print_values` | 금액 소수·음수 사례의 문구에 숫자 값이 없음 (A-3) |
| `test_wrong_type_does_not_crash_relation_checks` | 금액이 글자인 월 → 예외 없이 형식 오류 1개 (A-4) |
| `test_invalid_month_number` | `2026-13` → 오류 (B-5) |
| `test_integer_share_is_accepted` | 비중 `8` → 로드 성공 (B-5) |
| `test_is_real_evidence_uses_path_and_name` | 합성 표시 파일이라도 `private/`·`_real_` 경로면 True (B-1) |
| `test_fixtures_match_generator` | `build_fixtures.build_all()` 결과 = 커밋된 22개 파일 (B-3) |
| `test_errors_are_collected_not_first_only` | 오류 2곳을 넣은 파일 → 메시지 2개 |
| `test_error_message_includes_location` | `[fixture-… / 2026-02 / calculation_status]` 형식 |
| `test_bool_is_not_accepted_as_amount` | `true` 금액 거부 |
| `test_share_mismatch_is_warning_not_error` | 비중을 0.01 틀리게 → 로드 성공 + 경고 |
| `test_unknown_fields_are_warnings` | 레코드·월에 모르는 필드 → 경고 |
| `test_underscore_top_level_is_ignored` | `_case` → 경고 없음 |
| `test_missing_file_and_bad_json` | 파일 없음·문법 오류 문구 |
| `test_find_record_does_not_fall_back_to_national` | sido 요청, 전국만 있음 → None |
| `test_demo_evidence_loads` (1-5 이후 추가) | 합성 기본 파일 로드 성공, 경고 없음 |

---

## 8. 1-4 비교 계산

### `evidence/compare.py`

#### 데이터 형태

```python
Direction = Literal["up", "down", "flat"]

@dataclass(frozen=True)
class DirectionComparison:
    left: Direction | None
    right: Direction | None
    opposite: bool | None

@dataclass(frozen=True)
class MonthPair:
    month_0: str
    month_1: str
    status: Literal["ok", "skipped"]
    skip_reason: str | None
    foreign_amount_diff: int | None
    foreign_amount_growth_pct: Fraction | None
    total_amount_growth_pct: Fraction | None
    share_change_pp: Fraction | None
    known_only_share_change_pp: Fraction | None
    comparison_a: DirectionComparison   # 외국인 금액 방향 vs 전체 분모 비중 방향
    comparison_b: DirectionComparison   # 전체 분모 비중 방향 vs 미상 제외 비중 방향

def compare_record(record: EvidenceRecord) -> tuple[MonthPair, ...]
def direction_of(value: int) -> Direction
def opposite_of(left: Direction | None, right: Direction | None) -> bool | None
def next_month(month: str) -> str
```

#### 계산 절차 (월 목록의 인접 쌍마다)

1. `month_1 != next_month(month_0)`이면 `skipped`, 사유 "연속되지 않은 월". 검증을 통과한 파일에서는 발생하지 않지만 방어적으로 둔다
2. 두 달 중 하나라도 `calculation_status != "ok"`이면 `skipped`, 사유는 ok가 아닌 달을 **모두** 적는다: `"2026-03 자료 없음(no_data)"`, 두 달 모두면 `"2026-03 자료 없음(no_data), 2026-04 분모 0(invalid_denominator)"` (B-5). 모든 수치·비교는 None
3. 금액
   - `foreign_amount_diff = F1 − F0`
   - `foreign_amount_growth_pct = Fraction(F1 − F0, F0) × 100` (F0 = 0이면 None)
   - `total_amount_growth_pct = Fraction(T1 − T0, T0) × 100` (T0 = 0이면 None, ok 월은 T > 0이 보장됨)
4. 방향
   - 금액: `direction_of(F1 − F0)`
   - 전체 분모 비중: `direction_of(F1×T0 − F0×T1)`
   - 미상 제외 비중: K0 = T0−U0, K1 = T1−U1이 모두 > 0이면 `direction_of(F1×K0 − F0×K1)`, 아니면 None
5. 변화량
   - `share_change_pp = (Fraction(F1, T1) − Fraction(F0, T0)) × 100`
   - `known_only_share_change_pp`: K0, K1 > 0일 때만 같은 방식, 아니면 None
6. 비교
   - A = `opposite_of(금액 방향, 전체 분모 비중 방향)`
   - B = `opposite_of(전체 분모 비중 방향, 미상 제외 비중 방향)`

#### `opposite_of` 규칙

| left | right | 결과 |
|---|---|---|
| None 포함 | — | None |
| flat 포함 | — | False |
| up | down | True |
| down | up | True |
| 같음 | 같음 | False |

#### 하지 않는 것

- float 계산으로 방향 판정
- 비교 A와 B를 합친 점수
- 반대 방향 구간만 골라 반환하는 함수
- 서로 다른 레코드(범위·버전)끼리 비교 — 함수가 레코드 하나만 받으므로 구조적으로 불가

### `evidence/summary.py`

```python
@dataclass(frozen=True)
class PeriodTotals:
    months_used: tuple[str, ...]         # ok 월
    months_excluded: tuple[str, ...]     # ok가 아닌 월
    foreign_amount: int
    total_amount: int
    unknown_amount: int
    foreign_share_pct: Fraction | None
    known_only_share_pct: Fraction | None
    unknown_share_pct: Fraction | None

@dataclass(frozen=True)
class PairSummary:
    pair_count: int              # 전체 인접 구간 수
    comparable_count: int        # status ok
    skipped_count: int
    opposite_a_count: int        # comparison_a.opposite is True
    same_a_count: int            # comparison_a.opposite is False
    undetermined_a_count: int    # ok인데 comparison_a.opposite is None
    opposite_b_count: int
    same_b_count: int
    undetermined_b_count: int

def period_totals(record: EvidenceRecord) -> PeriodTotals
def summarize_pairs(pairs: Sequence[MonthPair]) -> PairSummary
```

- `period_totals`: ok 월의 F·T·U를 **각각 더한 뒤** 비중을 계산한다 (8-1장). 월 비중의 평균이 아니다. ok 월이 없으면 금액 0, 비중 None
- 문장은 만들지 않는다. "5개 구간 중 2개" 같은 문장은 3번 `review/`와 2-3 화면이 이 숫자로 만든다

### 테스트 `test_evidence_compare.py`

| 테스트 | 사례 | 확인 |
|---|---|---|
| `test_amount_up_share_down_is_opposite` | ① | A True, B False, 증감률 12.5·20.0, 변화 −0.5 |
| `test_amount_down_share_up_is_opposite` | ② | A True |
| `test_same_direction_is_not_opposite` | ③ | A False |
| `test_flat_amount_or_share_is_not_opposite` | ④ | 두 구간 모두 A False, flat 표기 |
| `test_previous_zero_foreign_amount_has_no_growth_rate` | ⑤ | 증감률 None, diff +500, 금액 방향 up |
| `test_missing_month_skips_both_pairs_and_does_not_bridge` | ⑥ | 구간 2개 모두 skipped, 01→03 없음 |
| `test_tiny_change_keeps_direction` | ⑦ | A True, 비중 변화 정확값 `Fraction(-4, 10013)` %p (≈ −0.0003994807) |
| `test_comparison_b_is_independent_from_a` | ⑧ | A False, B True |
| `test_large_amounts_use_exact_cross_product` | ⑨ | 전체 분모 비중 up, **float 방식은 flat임을 함께 확인** |
| `test_zero_known_only_denominator_gives_none` | ⑩ | B None, A False |
| `test_invalid_denominator_month_is_skipped` | 오류 11 | 01→02 skipped |
| `test_opposite_of_truth_table` | — | 위 규칙표 전체 |
| `test_results_are_exact_fractions` | ① | `Fraction` 타입, float 아님 |

### 테스트 `test_evidence_summary.py`

| 테스트 | 확인 |
|---|---|
| `test_period_share_uses_summed_amounts_not_average` | 합성 파일: 기간 비중 8.3777670060 ≠ 월 비중 평균 8.3759582834 |
| `test_period_totals_exclude_non_ok_months` | ⑥: months_excluded = ("2026-02",), 합계는 01·03만 |
| `test_pair_summary_counts` | 합성 전국: 구간 5, 비교 가능 5, A 반대 2, A 같음 3 |
| `test_pair_summary_with_skipped` | ⑥: skipped 2, comparable 0 |
| `test_no_ok_months_gives_none_shares` | 모든 월 no_data → 비중 None |

---

## 9. 1-5 합성 근거 파일

### 만들 파일

- `scripts/build_demo_evidence.py`
- `src/policy_signal_map/resources/evidence/review_evidence_demo_v1.json` (생성 결과, 커밋)

### 파일 머리

```json
{
  "schema_version": "2.0",
  "dataset_version": "demo-001",
  "records": [ … ]
}
```

### 레코드 `DEMO-R07-NATIONAL` — 전국, 6개월 모두 계산 가능

| 월 | F | T | U | 전체 분모 비중 | 미상 제외 비중 | 미상 비중 |
|---|---:|---:|---:|---:|---:|---:|
| 2026-01 | 820 | 10,000 | 450 | 8.2000 | 8.5864 | 4.5000 |
| 2026-02 | 790 | 9,400 | 520 | 8.4043 | 8.8964 | 5.5319 |
| 2026-03 | 860 | 10,300 | 560 | 8.3495 | 8.8296 | 5.4369 |
| 2026-04 | 905 | 10,600 | 540 | 8.5377 | 8.9960 | 5.0943 |
| 2026-05 | 930 | 10,890 | 900 | 8.5399 | 9.3093 | 8.2645 |
| 2026-06 | 880 | 10,700 | 760 | 8.2243 | 8.8531 | 7.1028 |

거래 건수 C = T ÷ 20 (500, 470, 515, 530, 544, 535)

| 구간 | 방향 (금액/비중/미상 제외) | 비교 A | 외국인 증감률 | 전체 증감률 | 비중 변화 | 설계 의도 |
|---|---|---|---:|---:|---:|---|
| 01→02 | down / up / up | **반대** | −3.66 | −6.00 | +0.2043 | 금액 감소·비중 증가 |
| 02→03 | up / down / down | **반대** | +8.86 | +9.57 | −0.0547 | 금액 증가·비중 감소 |
| 03→04 | up / up / up | 같음 | +5.23 | +2.91 | +0.1882 | 같은 방향 |
| 04→05 | up / up / up | 같음 | +2.76 | +2.74 | **+0.0022** | 0.01%p 미만 변화 (표시 규칙 확인용) |
| 05→06 | down / down / down | 같음 | −5.38 | −1.74 | −0.3156 | 같은 방향 |

- 비교 A 반대 **2개 / 5개**: 반대 방향만 모은 자료가 아님 (8-3장)
- 기간 합산: F 5,185 / T 61,890 / U 3,730 → 전체 분모 비중 8.3778%, 미상 제외 8.9151%, 미상 6.0268%
- applicability
  - R07 `allowed`: "합성 전국 금액·비중 및 분모 점검 예시로만 사용"
  - R06 `blocked`: "업종·월 보정 자료 없음"
  - R02 `needs_review`: "지역 기준과 업종 후보 자료 미확인"
- `region_basis`: `"unknown"`
- limitations: "합성 자료", "전국 참고 예시이며 선택 지역 진단이 아님", "미상 제외 비중을 정답으로 해석하지 않음"

### 레코드 `DEMO-R07-SIDO-HOLD` — 가상 시도, 보류 사례

`geographic_scope: "sido"`, `region_key: "DEMO-SIDO-A"` (실제 시도 코드와 겹치지 않는 가상 키)

| 월 | 상태 | F | T | U | 경고 |
|---|---|---:|---:|---:|---|
| 2026-01 | ok | 120 | 1,500 | 90 | — |
| 2026-02 | ok | 135 | 1,580 | 100 | — |
| 2026-03 | no_data | null | null | null | "해당 월 관측 행 없음" |
| 2026-04 | ok | 128 | 1,540 | 95 | — |
| 2026-05 | invalid_denominator | 0 | 0 | 0 | "전체 금액 0으로 비중 계산 불가" |
| 2026-06 | ok | 131 | 1,560 | 98 | — |

| 구간 | 결과 |
|---|---|
| 01→02 | 계산 (up / up / up, 같음) |
| 02→03, 03→04 | skipped (03 자료 없음) |
| 04→05, 05→06 | skipped (05 분모 0) |

- 비교 가능 1개 / 5개 → 2-3 "자료가 부족한 경우 보기" 화면용
- applicability
  - R07 `needs_review`: "합성 보류 예시: 시도 자료의 누락 월이 많아 비교 결론 보류"
  - R06 `blocked`: "업종·월 보정 자료 없음"
  - R02 `blocked`: "지역 기준 미확인"
- limitations: "합성 자료", "가상 시도 예시이며 실제 지역이 아님", "누락 월을 0으로 해석하지 않음"

### 스크립트 동작

1. `_evidence_builder.py` 함수로 두 레코드 조립
2. 파일 저장
3. `load_evidence()`로 다시 읽어 **오류·경고 0개**를 확인, 아니면 종료 코드 1
4. `compare_record`로 전국 레코드 구간 요약을 출력해 위 표(반대 2개)와 같은지 눈으로 확인

### 원칙

- 최종기획서 4-2장의 실제 분석 수치(억원 단위 금액, 7%대 비중)를 쓰지 않는다. 금액은 수백~수만 단위 가상 규모
- 파일을 손으로 고치지 않는다. 바꿀 때는 스크립트를 고쳐 다시 생성한다

---

## 10. 1-6 공개 자료 검사와 의존 방향 검사

### `scripts/check_public_bundle.py`

```python
@dataclass(frozen=True)
class Violation:
    path: str
    reason: str

ALLOWED_REAL_MARKERS = frozenset({"tests/fixtures/evidence/mixed_data_kind.json"})

def tracked_files(repo_root: Path) -> list[str]
    # git -c core.quotepath=false ls-files -z   (A-1: 한글 경로가 이스케이프되지 않게, NUL 구분)
def find_violations(repo_root: Path, files: Iterable[str]) -> list[Violation]
def main() -> int                                          # 위반 출력 후 0 또는 1
```

| 검사 | 대상 | 위반 사유 문구 |
|---|---|---|
| 실제 자료 표시 | `.json` 파일 내용에 `"data_kind"` 값이 `"real"` (허용 목록 제외) | "실제 자료 표시(data_kind: real)가 있는 파일" |
| 근거 파일 버전 | `schema_version`과 `records`가 있는 JSON의 `dataset_version`이 `demo-`·`fixture-`로 시작하지 않음 | "합성 버전 이름이 아닌 근거 파일" |
| 비공개 폴더 | 경로가 `private/`로 시작하고 `private/README.md`가 아님 | "private 폴더 파일이 추적됨" |
| 비밀 설정 | 파일 이름이 `.env` 또는 `.env.`로 시작 (`.env.example` 제외) | "비밀 설정 파일이 추적됨" |
| 실제 파일 이름 | 경로에 `_real_` 포함 | "실제 자료 이름 규칙(_real_) 파일이 추적됨" |
| 원자료 형식 (구현 중 추가) | `.csv`, `.xlsx`, `.xls`, `.parquet` | "원자료 형식(csv·엑셀·parquet) 파일이 추적됨" — 원본 카드 자료가 CSV라 JSON 내용 검사 밖의 유출을 막는다 |

- JSON 파싱에 실패한 파일은 문자열 검색(`"data_kind": "real"` 정규식)으로 대신 검사한다
- **목록에 있는데 열 수 없는 파일은 건너뛰지 않고 위반**으로 처리한다: "추적 파일을 읽을 수 없음" (A-1)
- 실행: `uv run python scripts/check_public_bundle.py`
- 테스트에서 import할 수 있게 `pyproject.toml`의 `[tool.pytest.ini_options] pythonpath`에 `"scripts"`를 추가한다 (A-2)

### `tests/test_public_bundle.py`

| 테스트 | 확인 |
|---|---|
| `test_tracked_files_have_no_violations` | 현재 저장소 위반 0 (git 없으면 skip) |
| `test_detects_real_marker` | 임시 폴더에 real JSON → 위반 |
| `test_allows_registered_fixture` | 허용 목록 경로 → 위반 아님 |
| `test_detects_private_and_env` | `private/x.json`, `.env` 경로 → 위반, `private/README.md`·`.env.example` → 통과 |
| `test_korean_file_name_is_scanned` | 임시 git 저장소에 한글 이름 real JSON을 추가 → 위반으로 잡힘 (A-1) |
| `test_unreadable_tracked_file_is_violation` | 목록에만 있고 없는 파일 → 위반 (A-1) |

### `tests/test_boundaries.py`

`ast`로 `src/policy_signal_map/` 아래 모든 `.py`의 `import`·`from … import`를 읽어 절대 모듈 이름으로 바꾼 뒤 검사한다.

| 규칙 | 대상 패키지 | 금지 import |
|---|---|---|
| 로직은 웹을 직접 import하지 않는다 | `plan`, `evidence`, `review`, `choices`, `document` | `fastapi`, `starlette`, `policy_signal_map.web`, `policy_signal_map.app` |
| 근거 계산은 위층을 직접 import하지 않는다 | `evidence` | `policy_signal_map.review`, `.choices`, `.document`, `.llm`, `.plan` |
| LLM은 근거 계산을 직접 import하지 않는다 | `llm` | `policy_signal_map.evidence` |

- 아직 없는 패키지는 건너뛴다
- **한계 (B-2):** 이 검사는 직접 import만 본다. `llm → review → evidence`처럼 거쳐서 모듈이 불러와지는 것은 막지 못한다. LLM에 카드 수치가 넘어가지 않게 하는 실제 장치는 C 단계의 수치 없는 요약 함수(`ReviewOutcome.to_llm_summary`)와 출력 검사(`llm/guard.py`)이며, 이 테스트는 코드 구조가 흐트러지는 것을 막는 보조 장치다
- 확인 절차: `evidence/compare.py`에 `import fastapi`를 임시로 넣어 실패하는지 본 뒤 되돌린다 (커밋하지 않음)

---

## 11. 테스트 전체 목록

| 파일 | 개수(예상) | 워크플로우 12장 대응 |
|---|---:|---|
| `test_config.py` | 7 | — |
| `test_evidence_loader.py` | 약 32 (매개변수화 포함) | 숫자·코드 오류, 미상 0 또는 관측 행 없음 |
| `test_evidence_compare.py` | 13 | 분모 0·자료 없음, 비교 A/B, 이전 F=0·변화 없음·월 누락, 아주 작은 방향 차이 |
| `test_evidence_summary.py` | 5 | — |
| `test_public_bundle.py` | 6 | 공개 배포 |
| `test_boundaries.py` | 3 | — |
| **합계** | **약 66** | 기존 18개와 합쳐 약 84개 |

---

## 12. 커밋과 기록

| 순서 | 커밋 메시지 | 포함 |
|---|---|---|
| 1 | `1-1 설정: 근거 파일 경로·LLM 제공자 환경변수와 금지 조합 검사` | config.py, .env.example, test_config.py, `.gitattributes`(B-6), pyproject `pythonpath`에 scripts(A-2), 루트 README 실행 방법 |
| 2 | `1-2·1-3 근거 파일 형태·경계 사례 22개·검증과 로더` | schema.py, loader.py, __init__.py, _evidence_builder.py, build_fixtures.py, JSON 22개, test_evidence_loader.py, **evidence·fixtures·scripts README를 계획서 사양으로 동기화**(A-5) |
| 3 | `1-4 비교 계산: 정수 교차곱 방향 판정과 기간·구간 요약` | compare.py, summary.py, 테스트 2개, evidence README 필드 이름 동기화 |
| 4 | `1-5 합성 근거 파일 demo-001` | build_demo_evidence.py, review_evidence_demo_v1.json, 로더 테스트에 합성 파일 추가, resources/evidence README 동기화 |
| 5 | `1-6 공개 자료 검사·패키지 의존 방향 검사` | check_public_bundle.py, test_public_bundle.py, test_boundaries.py, scripts·tests README 동기화 |

커밋마다 할 일

1. `uv run pytest` 전체 통과
2. 해당 폴더 README "만들 파일" 표의 상태를 "있음"으로 변경
3. [작업진행.md](./작업진행.md) 1장 체크박스 채움
4. [checks.md](./checks.md) 5장 진행 기록에 확인 방법과 함께 추가

푸시: 커밋 5 이후 `check_public_bundle.py` 통과를 확인하고, 개발 담당에게 확인받은 뒤 푸시한다.

---

## 13. 완료 기준

- [ ] 워크플로우 8-5장 6가지 상황이 테스트로 확인됨 (①~⑥)
- [ ] 워크플로우 12장 근거 관련 시험 7개가 테스트로 확인됨 (11장 표)
- [ ] 오류 사례 11개가 모두 한국어 문구와 위치로 멈추고, 분모 0 사례는 로드 후 비교에서 보류됨
- [ ] 소수점 방식이 틀리는 큰 금액 사례에서 정수 교차곱이 정답을 냄
- [ ] 합성 근거 파일이 오류·경고 없이 로드되고, 전국 레코드의 비교 A 반대가 2개/5개
- [ ] 합성 파일과 경계 사례에 실제 분석 수치가 없음
- [ ] `check_public_bundle.py` 위반 0, `test_boundaries.py` 통과
- [ ] `uv run pytest` 전체 통과 (약 75개)
- [ ] README·작업진행.md·checks.md 갱신

---

## 14. 위험과 대응

| 위험 | 가능성 | 영향 | 대응 |
|---|---|---|---|
| 0-1에서 데이터 담당이 형식을 바꿈 | 중 | 스키마·사례·테스트 수정 | 허용 값은 `schema.py` 상수 한곳, 사례는 스크립트로 재생성, 기대 오류는 핵심어로만 비교 |
| 미상 제외 분모 0의 상태값 해석이 다름 | 중 | ⑩ 사례와 M9 규칙 수정 | 15장 확인 항목으로 올림. 바뀌어도 사례 1개·규칙 1개만 수정 |
| 파일 비중 반올림 자릿수가 다름 | 중 | 경고가 많이 뜸 | 오류가 아닌 경고로 처리, 허용 오차를 상수로 두어 합의 후 조정 |
| 하루 분량이 많음 (C-1) | 중 | 2단계 시작 지연 | 9/16에 커밋 1~3(설정·검증·계산), 9/17 오전까지 커밋 4~5. 2단계 화면 시작이 반나절 늦어지는 수준 |
| 사례 22개 작성에 시간이 초과됨 | 낮음 | 1번 완료 지연 | 우선순위: 정상 ①~⑥ + 오류 1·3·4·7·8·9 → 나머지 10개는 같은 날 뒤에 |
| 실제 파일 오류 문구로 수치 노출 | 낮음 | 보안서약 위반 | 금액·비중 필드는 값 대신 형식만 출력 (A-3) |
| 공개 자료 검사가 정상 파일을 잘못 잡음 | 낮음 | 테스트 실패 | 허용 목록을 경로 단위로 명시, 위반 사유를 구체적으로 출력 |

---

## 15. 데이터 담당에게 확인할 점

작업진행.md 0-1(형식 확정) 때 함께 확인한다. 확인 전에는 아래 **현재 계획**대로 구현하고, 답이 다르면 해당 규칙과 사례만 고친다.

| # | 질문 | 현재 계획 | 영향 범위 |
|---|---|---|---|
| Q1 | 전체 금액 > 0인데 미상 금액 = 전체 금액이면(미상 제외 분모 0) 상태를 `ok`로 두나, `invalid_denominator`로 두나? | `ok` + `known_only_share_pct: null` + 경고 | 규칙 M9, 사례 ⑩ |
| Q2 | `*_share_pct`를 소수 몇 자리까지 반올림해서 주나? | 10자리 (9-3장 예시 `8.8888888889`) | 허용 오차 상수, 경고 빈도 |
| Q3 | 외국인 행이 전혀 없는 달은 `ok` + F=0 + 경고로 주나? | 예 (8-1장 "관측 합계 0 가능, 관측 행 없음 경고") | 사례 ⑤ |
| Q4 | `invalid_input` 월에 금액을 채워 주나, null로 주나? | 둘 다 허용, 사유는 warnings에 필수(없으면 경고) | 규칙 M13·M14 |
| Q5 | 시도 레코드의 `region_key`는 어떤 고정 키 목록을 쓰나? (예: 행정코드 앞 2자리) | 비어 있지 않고 ALL이 아니면 허용 | 규칙 R5, 2-3 지역 매칭 |
| Q6 | 레코드에 설명용 필드를 추가할 계획이 있나? | 모르는 필드는 경고 후 계속 | 규칙 F9·R15·M15 |

**확인된 답 (2026-09-17 기록).** 위 표는 작성 당시 질문이라 그대로 둔다.

| # | 답 | 출처 | 서비스 영향 |
|---|---|---|---|
| Q2 | 비중은 미리 반올림하지 않고 원본 정밀도로 전달 | 데이터 담당 결정 문서(9/15, 저장소 밖) | 없음. 임시본 사전 점검에서도 허용 오차(`SHARE_TOLERANCE_PP`) 안에서 일치해 상수 유지 |
| Q6 | 임시본에 `comparisons` 칸이 추가됨 | 임시본 사전 점검(9/17, checks.md 2장 1차) | 현재 계획대로 경고 후 계속 읽음. 유지 여부는 데이터 담당과 결정 |
| Q1·Q3·Q4·Q5 | 답 없음(결정 문서에 없음). 확정본을 받은 뒤 다시 확인 | — | 현재 계획 유지 |
