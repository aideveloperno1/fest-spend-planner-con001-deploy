# 작업진행 — 서비스 완성까지 남은 작업

문서 기준일: 2026-09-16
기준 문서: [최종기획서](../최종기획서.md) · [개발업무 워크플로우](../개발업무_워크플로우.md)
관련 기록: [checks.md](./checks.md) (합의·진행 기록) · 폴더별 `README.md` (파일 사양)

> **2026-09-21 공개 배포본:** 이 복사본은 공개 합성 데이터와 Google AI Studio API만 사용한다. 현재 실행·설정은 README와 `6LLM참고의견계획.md`를 기준으로 한다. 아래 이전 기록은 내부용 개발 이력이다.

> **2026-09-16 인수인계:** 서비스 개발 담당이 팀을 떠나고 남은 팀원이 이어받는다. 이 문서의 "개발 담당·사용자"는 이제 이어받은 사람이다.
> 인수인계 요약은 **저장소 밖 상위 폴더**(`Service/`의 한 단계 위)에 있다: `인수인계.md`, AI 도구용 `인수인계_ai.md`, 개발 반영 표시를 단 기준 문서 사본. (9/16에는 `1차공모/인수인계/`에 있었고 그 뒤 상위 폴더로 옮겨졌다.)

이 문서는 **무엇을, 어떤 순서로, 왜, 어디까지 하면 끝인지**를 정리한다. 파일 안의 세부 사양은 각 폴더 README에 있으므로 여기서는 링크로 연결한다.
작업을 끝내면 해당 항목의 체크박스를 채우고 `checks.md` 진행 기록에 확인 방법과 함께 적는다.

---

## 목차

- [현재 상태와 목표](#현재-상태와-목표)
- [전체 순서와 의존 관계](#전체-순서와-의존-관계)
- [0. 데이터 담당과 확인할 것](#0-데이터-담당과-확인할-것-916)
- [1. 근거 계산 계층](#1-근거-계산-계층-evidence-916)
- [2. 2단계 근거 확인 화면](#2-2단계-근거-확인-화면-91617)
- [3. 3단계 검토 질문](#3-3단계-검토-질문-917)
- [4. 4단계 보완 선택](#4-4단계-보완-선택-91718)
- [5. 5단계 보완 기획안과 저장](#5-5단계-보완-기획안과-저장-918)
- [6. 실제 결과 연결과 전체 검증](#6-실제-결과-연결과-전체-검증-91819)
- [7. 공개 배포](#7-공개-배포-919)
- [8. 제출 준비](#8-제출-준비-91921)
- [이번 범위에서 뺀 것](#이번-범위에서-뺀-것)
- [일정이 밀릴 때 줄이는 순서](#일정이-밀릴-때-줄이는-순서)
- [정할 것](#정할-것)
- [작업 공통 규칙](#작업-공통-규칙)

---

## 현재 상태와 목표

### 끝난 것

| 항목 | 내용 | 기록 |
|---|---|---|
| 개발 환경 | uv + Python 3.12, FastAPI + Jinja2, pytest | checks.md 개발 환경·기술 방식 |
| 폴더 구조 | 기능별 패키지와 웹 계층 분리, 폴더마다 README | 루트 README |
| 저장소 | GitHub 공개 저장소, 라이선스 없음(모든 권리 보유), 실제 자료 제외 규칙과 공개 자료 검사 | checks.md 코드 저장소·라이선스 |
| 1~5장 | 근거 계산, 1~5단계 화면(기획 입력·근거 확인·검토 질문·보완 선택·보완 기획안), Markdown·PDF 저장 — **합성 자료로만 검증** | 아래 각 장 체크박스, `1~5…계획.md` |
| AI 참고 의견 | 공개 배포본은 Google AI 모델 4개 선택·수동 생성·모델별 캐시를 구현 | `6LLM참고의견계획.md` |
| 테스트 | 458개 통과 (2026-09-19, 지역 확장 시험 포함) | `tests/README.md` |
| 남은 것 | 0장 데이터 쪽 확인, 5-5, 6장 실제 결과 연결, 7장 배포(**보류**), 8장 제출 | 아래 |

### 목표

- **9/21 내부 제출**, 공식 마감 **9/22 18:00** (워크플로우 13장)
- 완료 기준은 화면이 있는 것이 아니라 **입력·근거·선택·출력이 일치하는 것** (최종기획서 11장)
- 반드시 연결할 흐름: **성과지표 검토(R07) → 사용자 선택 → 전체 기획안 작성·저장** (최종기획서 11장 구현 범위)

---

## 전체 순서와 의존 관계

```
0 데이터 담당 확인 ─┐ (병행, 개발을 막지 않음)
                    │
1 근거 계산 ──→ 2 근거 화면 ──→ 3 검토 질문 ──→ 4 보완 선택 ──→ 5 기획안·저장
                                    ↑                                   │
                           0-4 규칙 조건 검수                            ↓
                                                   6 실제 연결·전체 검증 ──→ 7 배포 ──→ 8 제출
```

- **1 → 2:** 화면은 계산 결과를 표시만 하므로 계산이 먼저 있어야 한다
- **2 → 3:** 규칙은 근거 레코드와 비교 요약을 입력으로 받는다
- **3 → 4:** 선택은 검토 질문과 대안 목록에 대해 이루어진다
- **4 → 5:** 기획안은 원안 + 선택으로 만든다
- **5 → 6:** 끝까지 흐름이 있어야 실제 수치로 전체를 대조할 수 있다
- **6 → 7 → 8:** 검증한 버전을 배포하고, 배포 화면으로 제출 자료를 만든다. (2026-09-16: 7 배포가 **보류**되어, 8은 배포 없이 진행할 수 있게 캡처 방법을 다시 정해야 한다 — 8-2)

순서를 건너뛰면 앞 단계 형태가 확정되지 않은 상태에서 뒤 단계를 만들게 되어 다시 고칠 일이 생긴다.

---

## 0. 데이터 담당과 확인할 것 (9/16)

**담당:** 개발 담당 + 데이터 담당 (인수인계 후 한 사람. 검수는 다른 날 하거나 제3자 확인)
**성격:** 개발을 막지는 않지만, 늦을수록 다시 고칠 범위가 커진다.

### 0-1. 1차 연결 확인: 근거 파일 형식 확정

- [ ] 완료 — **부분 진행 9/17:** 형식은 데이터 담당이 확정(9/15 결정 문서, checks.md 3장). 확정본에서 `data_kind`·`month` 표기 반영 확인이 남음

**왜:** 서비스는 `review_evidence.json`의 필드 이름·단위·상태값을 그대로 읽는다. 형식이 1번 작업 이후에 바뀌면 `evidence/schema.py`, 경계 사례 22개, 테스트를 모두 다시 고쳐야 한다.

**할 일**
1. 워크플로우 9-3장 JSON 예시를 데이터 담당과 함께 한 필드씩 확인한다
2. 특히 합의할 점
   - 금액은 원 단위 정수, 비중은 퍼센트 값(8% → `8.0`)
   - 자료가 없는 달을 **생략하지 않고** `calculation_status`와 null로 둔다
   - `no_data` 월의 금액은 0이 아니라 null
   - `applicability`의 `reason`은 반드시 채운다
   - 시도 레코드를 추가할 경우 `region_key`에 쓸 고정 키 목록
3. 가능하면 **실제 결과 1건**(한 달이라도)을 받아 필드 구성만 대조한다. 파일은 `private/`에 두고 공개 저장소·외부 AI에 올리지 않는다
4. 바뀐 점이 있으면 워크플로우 9-3장과 `evidence/README.md`를 함께 고친다

**완료 기준:** `checks.md` 3장 "전달 형식" 상태를 "확인"으로 바꾸고 날짜 기록

### 0-2. 운영 사항 확정

- [ ] 완료

**왜:** `checks.md` 1장에 "미정"으로 남아 있어 연결 확인 일정을 잡을 수 없다.

**할 일**
- 두 사람 이름
- ~~분석 근거 파일 전달 방법~~ → **9/18 정리: 방법을 미리 정하지 않는다.** 직접 전달이든 두 사람만 보는 비공개 폴더든 편한 대로 하되, 단체방·메신저·이메일·GitHub·외부 AI 도구는 쓰지 않는다
- 연결 확인 시간: 1차(9/16 완료), 2차(최종 파일 도착 후), 3차(9/20)

**완료 기준:** `checks.md` 1장·2장 해당 칸 채움

### 0-3. 결정 사항 공유

- [ ] 완료

**왜:** 코드와 문서는 두 사람의 공동 작업물이다. 저장소 공개와 라이선스는 한 사람이 정한 상태로 두면 안 된다.

**공유할 것**
| 내용 | 근거 |
|---|---|
| 데이터 담당 산출물은 저장소 밖 상위 폴더에 둔다 (합의 당시 이름 `Analysis/`, 9/18 현재는 `Data/`·`Data2/`로 받고 있다) | checks.md 데이터 담당 산출물 위치 |
| GitHub 저장소가 **공개**라서 실제 분석 수치·화면 캡처를 올리면 안 된다 | 보안관리 서약서 2항 |
| GPL v3를 삭제하고 라이선스 없음(모든 권리 보유)으로 바꾼 이유 | 참가신청서류 준수사항 5항 |
| A(규칙 기반) 완성 후 C(LLM) 도입, LLM에는 수치를 보내지 않는 원칙 | checks.md 구현 순서·LLM 전달 자료 |

**완료 기준:** 데이터 담당 동의 여부를 `checks.md` 상태 칸에 기록

### 0-4. R01·R03·R04 실행 조건 검토 요청

- [x] 요청 (9/16 `docs/review_rules.md` 전달)
- [x] 검수 완료 — 9/17 데이터 담당: R01·R03 사용 가능, R04 조건부 사용 가능("31일은 서비스 운영 기준"을 문서에 밝힐 것). 9/18 반영 완료(`docs/review_rules.md` R04 "31일 기준의 성격", `review/README.md`, `basic_checks.py` 주석)
- [ ] (추가) R07·R05 검수 — 요청해 둔 상태, 의견 오면 문구·캡처 반영

**왜:** 워크플로우 11장에는 규칙마다 조건이 한 줄씩만 있다. `review/README.md`에 적은 구체 조건은 개발 담당 초안이라, 근거 해석이 맞는지 데이터 담당 확인이 필요하다 (워크플로우 2장: 개발 담당이 해석 기준을 임의로 만들지 않는다).

**할 일**
1. 3-1에서 만든 `docs/review_rules.md` 초안을 전달한다 (3-1이 끝나는 대로)
2. 확인받을 질문
   - R03: 대상에 "관광객·방문객·여행객" 문구가 있고 외국인 카드 지표를 쓰면 질문한다 — 이 기준이 적절한가
   - R04: 31일 미만 사업 + 카드 지표 직접 평가 시 질문한다 — 기간 기준이 적절한가
   - R01: 참여 상점 이용 확대 목표 + 쿠폰 사용처 비어 있음 — 입력 항목만으로 판단 가능한가
   - 질문 문구가 근거 이상으로 단정하지 않는가
3. 같은 문서에 **AI 참고 의견용 상황 설명(`llm_context`)** 열을 함께 넣어 한 번에 검수받는다 (9/16 추가)
   - 규칙·상황마다 수치 없이 한 줄. 규칙 문구(`messages`)에는 수치가 들어 있어 LLM에는 이 문장을 대신 넘긴다(규칙 번호·제목·적용 범위와 함께). 대안별 "문서에 들어가는 문장"도 같은 문서에 있다
4. **최종기획서 11장 "조건부 확장"에 한 줄 추가할지 결정** (9/16 추가, 아직 요청하지 않음)
   - "AI 참고 의견(로컬 LLM) — 규칙 결과에 대한 확인 사항 제시, 문서·선택에는 반영하지 않음"
   - 8장은 고치지 않는다. 로컬 실행이라 "외부 LLM에 보내지 않는다"를 그대로 지킨다

**완료 기준:** `docs/review_rules.md`의 "데이터 담당 검수" 칸에 날짜 기록 (9/17까지), 기획서 11장 반영 여부 기록

---

## 1. 근거 계산 계층 `evidence/` (9/16)

**담당:** 개발 담당
**사양:** [evidence/README.md](src/policy_signal_map/evidence/README.md), [tests/fixtures/evidence/README.md](tests/fixtures/evidence/README.md), [scripts/README.md](scripts/README.md)
**왜 먼저:** 모든 화면·규칙·문서가 이 계층의 결과를 쓴다. 계산이 틀리면 뒤 단계 전체가 틀린 숫자를 보여준다. 반대로 여기서 기획 문서의 예외 규칙을 테스트로 고정해 두면 뒤 단계에서 숫자를 의심할 필요가 없다.

### 1-1. 설정 파일 `config.py`, `.env.example`

- [x] 완료 (9/16)

**왜:** 근거 파일 경로와 LLM 제공자를 코드에 박아 두면, 실제 파일 연결이나 LLM 도입 때 코드를 고쳐야 한다. 합성/실제 선택은 개발 담당이 나중에 정하기로 했으므로 설정으로 바꿀 수 있어야 한다.

**할 일**
1. `src/policy_signal_map/config.py`
   - 환경변수 `PSM_EVIDENCE_PATH`(기본: 합성 파일), `PSM_LLM_PROVIDER`(기본: `none`), `PSM_LLM_BASE_URL`, `PSM_LLM_MODEL`, `PSM_LLM_API_KEY`
   - 앱 시작 시 한 번 읽어 변경 불가 객체로 보관
   - 실제 근거 파일 + `PSM_LLM_PROVIDER=cloud` 조합이면 시작 시 오류로 멈춤
2. 저장소 루트 `.env.example`: 위 항목과 설명, 실제 값 없음
3. README의 실행 방법에 환경변수 설명 추가

**완료 기준**
- 환경변수 없이 실행하면 합성 파일 경로를 사용
- 금지 조합 테스트 통과
- `.env`는 git에 올라가지 않고 `.env.example`만 올라감

### 1-2. 경계 사례 JSON 22개

- [x] 완료 (9/16)

**왜:** 워크플로우 8-5장과 12장은 "이전 달 외국인 금액 0", "월 누락", "분모 0" 같은 예외를 서비스가 어떻게 보여줘야 하는지 정해 두었다. 실제 데이터에는 이런 경우가 없을 수도 있어서, 일부러 만든 작은 파일로 확인해야 한다.

**할 일**
1. 정상 파일 10개 (비교 계산 확인): 금액↑·비중↓, 금액↓·비중↑, 같은 방향, 변화 0, 이전 F=0, 월 누락, 0.01%p 미만, 비교 B만 반대, 큰 금액, 미상=전체
2. 오류 파일 12개 (로더 검증 확인): 잘못된 상태값, 잘못된 applicability, reason 누락, F+U>T, 음수, 소수 금액, 자료 없음을 0으로 적음, 월 생략, 지원하지 않는 버전, 합성·실제 혼합, 분모 0(로드는 성공), 전국인데 region_key가 ALL이 아님
3. 각 파일 2~3개월, 최상위에 `"_case"` 한 줄 설명
4. 모든 수치는 가상 규모. `mixed_data_kind.json`의 real 레코드도 수치는 가짜

**완료 기준:** 22개 파일이 README 표와 이름·내용이 일치

### 1-3. 형식 검증 `schema.py`, `loader.py`

- [x] 완료 (9/16)

**왜:** 워크플로우 9-2장은 "목록에 없는 상태값은 파일 검증 오류로 처리한다"고 정했다. 잘못된 파일을 조용히 읽으면 화면에 틀린 숫자나 "0"이 나오고, 사용자는 그것이 자료 문제인지 알 수 없다.

**할 일**
1. `schema.py`: 9-3장 필드명 그대로 dataclass 정의, 검증 규칙 8가지 구현
2. `loader.py`
   - `load_evidence(path)`: 읽기 → 검증 → 결과 또는 `EvidenceError(한국어 메시지 목록)`
   - 오류 메시지에 레코드 ID·월·필드명 포함 (데이터 담당에게 그대로 전달할 수 있게)
   - `find_record(file, 범위, 지역 키)`: 없으면 None. 전국 자료로 자동 대체하지 않음
3. `tests/test_evidence_loader.py`: 오류 파일 12개 각각 기대 오류 확인, 합성 기본 파일 로드 성공

**완료 기준:** 오류 파일은 모두 `EvidenceError`, 정상 파일은 모두 로드 성공

### 1-4. 비교 계산 `compare.py`, `summary.py`

- [x] 완료 (9/16)

**왜:** 이 서비스의 첫 사례 핵심 근거는 "외국인 금액과 비중이 다르게 움직일 수 있다"는 관측이다. 방향 판정을 반올림한 퍼센트로 하면 0.007%p 같은 작은 변화의 방향이 틀릴 수 있어, 워크플로우 8-2장은 원래 금액의 교차곱 부호를 쓰도록 정했다.

**할 일**
1. `compare.py`
   - 인접 월 쌍마다 금액 차이, 증감률, 전체 분모 비중 변화, 미상 제외 비중 변화
   - 방향: `F1×T0 − F0×T1` 등 **정수 교차곱 부호** (Python 정수는 자릿수 제한이 없어 큰 금액도 정확)
   - 비교 A(금액 vs 전체 분모 비중), 비교 B(전체 vs 미상 제외 비중)를 따로 계산하고 합치지 않음
   - `opposite`: 양쪽이 up/down으로 다를 때만 True, flat이면 False, 계산 불가면 None
   - 두 달 중 하나라도 `ok`가 아니면 skipped + 사유, 연속되지 않은 월은 잇지 않음
2. `summary.py`
   - 기간 합산: F·T·U를 각각 더한 뒤 비중 계산 (월 비중 평균 아님)
   - 구간 요약: 전체·비교 가능·반대 방향·같은 방향·보류 구간 수
   - 반대 방향 구간만 골라내는 함수는 만들지 않음 (8-3장)
3. `tests/test_evidence_compare.py`, `tests/test_evidence_summary.py`: 정상 파일 10개 기대 결과

**완료 기준:** 8-5장 표의 6가지 기대 설명이 모두 테스트로 확인됨

### 1-5. 합성 근거 파일 만들기 `scripts/build_demo_evidence.py`

- [x] 완료 (9/16)

**왜:** 화면 개발과 공개 시연에 쓸 기본 파일이 필요하다. 비중을 손으로 적으면 금액과 맞지 않는 파일이 생기므로, 정수 금액만 정하고 비중은 1-4의 함수로 계산해 넣는다.

**할 일**
1. 레코드 `DEMO-R07-NATIONAL`: 전국, 1~6월 모두 `ok`, 반대·같은 방향 구간 혼합, 0.01%p 미만 구간 1개, applicability(R07 allowed / R06 blocked / R02 needs_review), limitations 3개
2. 레코드 `DEMO-R07-SIDO-HOLD`: 가상 시도, `no_data`·`invalid_denominator` 월 포함, R07 needs_review
3. 출력: `resources/evidence/review_evidence_demo_v1.json`, 만든 파일이 1-3 로더를 통과하는지 스크립트 끝에서 확인

**완료 기준**
- 파일 로드 성공
- `dataset_version: demo-001`, 모든 레코드 `synthetic`
- 최종기획서 4-2장 실제 수치와 무관한 가상 규모

### 1-6. 공개 자료 검사와 의존 방향 검사

- [x] 완료 (9/16)

**왜:** 저장소가 공개라서 실수 한 번이 보안서약 위반이 된다. `.gitignore`는 파일 이름으로만 막으므로 **내용으로 검사**하는 장치가 필요하다. 또한 LLM이 카드 수치에 접근하지 못하게 한다는 원칙을 사람의 주의가 아니라 테스트로 지키게 한다.

**할 일**
1. `scripts/check_public_bundle.py`
   - `git ls-files` 대상, `"data_kind": "real"`·`private/`(README 제외)·`.env`·`demo-`로 시작하지 않는 근거 파일 탐지
   - 허용 예외: `tests/fixtures/evidence/mixed_data_kind.json` (경로 명시 등록)
2. `tests/test_public_bundle.py`: 위 검사 함수 호출, git 없으면 건너뜀
3. `tests/test_boundaries.py`: `ast`로 import 검사
   - 로직 폴더 → `fastapi`, `web` 금지
   - `evidence` → `review`·`choices`·`document`·`llm` 금지
   - `llm` → `evidence` 금지

**완료 기준:** `uv run pytest` 통과, 일부러 금지 import를 넣으면 실패하는 것 확인 후 되돌림

---

## 2. 2단계 근거 확인 화면 (9/16~17)

**담당:** 개발 담당
**사양:** [web/README.md](src/policy_signal_map/web/README.md), [routes/README.md](src/policy_signal_map/web/routes/README.md), [templates/steps/README.md](src/policy_signal_map/web/templates/steps/README.md), [partials/README.md](src/policy_signal_map/web/templates/partials/README.md), [static/js/README.md](src/policy_signal_map/web/static/js/README.md)
**왜:** 사용자가 질문을 받기 전에 "왜 이런 질문을 하는지" 근거를 먼저 봐야 한다 (최종기획서 3장 2단계). 또한 1번 계산이 화면에서 기획 문서의 표시 규칙대로 보이는지 처음 확인하는 단계다.

### 2-1. 표시 형식 `formatters.py`, 공통 의존성 `dependencies.py`

- [x] 완료 (9/16) — 표시 형식은 루트 `formatting.py`, 근거 상태는 `web/evidence_state.py` ([2근거확인화면계획.md](./2근거확인화면계획.md))

**왜:** 같은 숫자가 화면마다 다른 자릿수·단위로 보이면 신뢰를 잃는다. 워크플로우 8-3장 표시 규칙(소수 둘째 자리, 0.01%p 미만 표기, 빨간 경고 금지)을 한곳에서 지키게 한다.

**할 일**
1. `web/formatters.py` Jinja2 필터: `krw_eok`(억원), `pct`, `pp_change`(0.01%p 미만), `growth`(None → 계산 불가), `direction`, `calc_status`, `scope_label`
2. `web/dependencies.py`: 세션 가져오기, 앱 시작 시 로드한 근거 파일, 단계 잠금 조건
3. `app.py`에서 근거 파일을 시작 시 로드하고 실패하면 오류 화면
4. `tests/test_formatters.py`

**완료 기준:** None이 어떤 필터에서도 "0"으로 나오지 않음, 0.004%p → "0.01%p 미만 (증가)"

### 2-2. 화면 조각 3개

- [x] 완료 (9/16) — `evidence_badges.html`, `month_table.html`, `pair_table.html`(구간 칩 대신 표), 화면 데이터 조립 `web/evidence_view.py`

**왜:** "전국 참고 — 특정 지역의 진단이 아님" 표시는 2·3·5단계에 반복된다. 조각으로 만들어야 한 화면에서만 빠지는 실수를 막는다 (워크플로우 12장 "전국 자료 + 특정 지역 기획").

**할 일:** `partials/evidence_badges.html`, `month_table.html`, `pair_chips.html`

**완료 기준:** 세 조각이 받은 값만 표시하고 계산하지 않음

### 2-3. 근거 확인 화면 `evidence.html`, `routes/evidence.py`, `charts.js`

- [x] 완료 (9/16)

**왜:** 최종기획서 4-2장의 관측(금액과 비중이 다른 질문에 답한다)을 사용자가 직접 보고 이해하는 화면이다.

**할 일**
1. 라우트: 원안 지역에 맞는 레코드 조회 → 없으면 전국 레코드를 **전국 참고로 표시**
2. 화면 구성 (시안 S2)
   - 범위 칩
   - 요약 문장: 반대 구간 0개일 때 다른 문장
   - 금액 막대·비중 선 차트와 전체/미상 제외 전환
   - 월별 표, 구간 칩 5개
   - 계산 방법·해석 한계
   - "자료가 부족한 경우 보기"
3. `charts.js`: 서버가 넘긴 JSON만 사용, null은 끊어서 그림, Chart.js 4.5.1 CDN 고정
4. `routes/steps.py`에서 2단계 제거, `app.py`에 라우터 등록
5. `test_routes.py`에 "전국 참고"·"합성" 표시, 보류 사례 표시 테스트 추가

**완료 기준:** 브라우저에서 예시 기획 → 2단계 확인, 콘솔 오류 없음, 테스트 통과

### 2-4. 상단 자료 표시를 불러온 파일 기준으로

- [x] 완료 (9/16)

**왜:** 참고 시안에는 "시연용 합성 수치"와 "자료 버전 real-20260917-1"이 함께 적혀 있는 모순이 있었다 (checks.md 시안 오류 수정). 표시가 실제 불러온 파일과 다르면 합성과 실제를 혼동하게 된다.

**할 일:** `base.html` 상단 칩을 근거 파일의 `data_kind`·`dataset_version`으로 표시

**완료 기준:** 합성 파일이면 "시연용 합성 수치 · demo-001"

---

## 3. 3단계 검토 질문 (9/17)

**담당:** 개발 담당 (3-1은 데이터 담당 검수)
**사양:** [review/README.md](src/policy_signal_map/review/README.md), [resources/rules/README.md](src/policy_signal_map/resources/rules/README.md), [docs/README.md](docs/README.md)
**왜:** 이 서비스는 판정하지 않고 **질문**한다 (최종기획서 1장). 근거와 사용자 입력을 조합해 "무엇을 확인해야 하는지"를 만드는 핵심 단계다.

### 3-1. 규칙 설명 문서 `docs/review_rules.md`

- [x] 초안 (9/16)
- [ ] 데이터 담당 검수 — **R01·R03·R04 완료(9/17, 검수 칸에 날짜 기록). R07·R05 대기**

**왜:** 워크플로우 7장이 정한 공동 파일이다. 개발 담당이 초안을 쓰고 두 사람이 확정해야, 규칙 문구가 근거 이상으로 단정하지 않게 된다.

**할 일:** 규칙마다 실행 조건, 실행하지 않는 조건, 보여줄 근거, 질문 문구, 대안, 추가 입력, 문서 반영 위치, 재확인 조건, 중복 질문 묶기, 검수 날짜

**완료 기준:** R07·R01·R03·R04·R05·R06·R02 모두 작성, 검수 날짜 기록

### 3-2. 규칙 원본 JSON v1.0

- [x] 완료 (9/16)

**왜:** checks.md에서 "규칙 JSON이 원본"으로 정했다. 문구를 코드에 흩어 두면 md 문서와 화면 문구가 어긋난다.

**할 일:** `review_rules.json`에 질문 문구, 대안 A~D(바뀌는 곳·필요 자료·운영 부담·추가 입력·문서 반영), `related_fields`, `merge_group` 추가. 버전 1.0

**완료 기준:** `docs/review_rules.md`와 ID·문구 일치, "오류·잘못·실패" 단어 없음

### 3-3. 규칙 실행 코드

- [x] 완료 (9/16)

**왜:** 같은 입력이면 항상 같은 질문이 나와야 심사·검증 때 설명할 수 있다.

**할 일**
1. `outcome.py`
   - 결과 형태와 종류 5가지: 질문 / 안내 / 추가 확정 필요 / 검토하지 않음 / 보류
   - LLM용 수치 없는 요약 함수
2. `r07_indicator.py`
   - 판단 순서: 지표 확인 → applicability → 비교 가능 구간 → 지표 용도 → 목표와 지표 불일치
3. `basic_checks.py`: R01·R03·R04·R05, R06·R02는 "검토하지 않음"
4. `engine.py`: 실행 순서, 중복 질문 묶기(근거 ID 모두 유지), 검토하지 않은 규칙 목록
5. `rules.py`: JSON 규칙 ID와 코드 함수 일치 검사
6. `tests/test_review_rules.py`

**완료 기준:** 워크플로우 12장 중 "방향이 같은 두 기간", "참고용 지표", "전국 자료 + 특정 지역 기획", "검증 시도 자료 + 시군구 기획", "자료 수집 미정" 테스트 통과

### 3-4. 검토 질문 화면

- [x] 완료 (9/16)

**왜:** 시안 원칙인 "'문제없음'과 '검토하지 않음'을 구분"을 사용자에게 보여줘야 서비스가 모든 것을 검토한 것처럼 오해하지 않는다.

**할 일:** `questions.html`(규칙 카드, 근거 칩, "왜 묻나요?", 오른쪽 검토하지 않은 항목), `routes/questions.py`, `partials/rule_card.html`, 원안 보관 시 규칙 실행 결과를 세션에 저장

**완료 기준:** 예시 기획으로 R07 질문 표시, 지표 용도를 참고 현황으로 바꾸면 오류 없이 안내만 표시

---

## 4. 4단계 보완 선택 (9/17~18)

**담당:** 개발 담당
**사양:** [choices/README.md](src/policy_signal_map/choices/README.md), [plan/README.md](src/policy_signal_map/plan/README.md)
**왜:** 서비스가 대신 결정하지 않고 담당자가 고른다 (최종기획서 1장). 고른 내용이 무엇이고 비어 있는 값이 무엇인지 기록해야 기획안에 정확히 반영할 수 있다.

### 4-1. 원안 변경 비교 `plan/changes.py`

- [x] 완료 (9/16)

**왜:** 워크플로우 11장은 "목표·지역·지표가 바뀌면 영향받는 선택을 재확인 필요로 바꾼다"고 정했다. 지금은 원안이 조금이라도 바뀌면 전체를 재확인으로 보는데, 사업명 오타 수정만으로 모든 선택을 다시 하게 하면 불편하다.

**할 일:** 변경된 필드 계산, 재확인 트리거(목표·지역·성과지표·지표 용도·대상·기간) 구분, `tests/test_plan_changes.py`

**완료 기준:** 사업명만 바꾸면 재확인 대상 없음, 목표를 바꾸면 해당 필드 반환

### 4-2. 선택 로직 `choices/`

- [x] 완료 (9/16)

**왜:** 선택 취소·빈 값·재확인을 로직에서 처리해야 기획안 생성 단계가 단순해지고 테스트로 확인할 수 있다.

**할 일**
1. `models.py`: 선택, 실행 조건, 선택 모음
2. `selection.py`
   - 선택 적용·취소·검증
   - JSON에 없는 대안 거부, 수정 시 문장 필수
   - 옵션 A는 수집자료 1개 이상 필수, 담당자·주기는 빈 값 허용
   - 옵션 D는 초안 상태만
3. `recheck.py`: 변경 필드와 규칙의 재확인 필드가 겹치는 선택만 재확인, 질문이 사라진 선택은 보관함으로
4. `tests/test_choices.py`

**완료 기준:** 워크플로우 12장 "목표 유지·대안 취소", "목표·지역 변경" 테스트 통과

### 4-3. 세션·폼 확장

- [x] 완료 (9/16)

**할 일:** `WorkState`에 검토 결과·선택 모음·변경 필드 추가(`review_restarted` 대체), `forms.py`에 선택 폼 해석, 오래된 세션 정리(2시간)

**완료 기준:** 기존 테스트 유지, 선택 저장·취소 후 세션 상태 확인 테스트

### 4-4. 보완 선택 화면

- [x] 완료 (9/16)

**왜:** 대안마다 "바뀌는 곳·필요 자료·운영 부담"을 보여줘야 담당자가 실행 가능성을 따져 고를 수 있다 (시안 S4).

**할 일:** `choices.html`, `routes/choices.py`(저장·취소 POST 후 303), `partials/option_card.html`, `pending_badge.html`, `choices.js`(실행 조건·수정 칸 표시)

**완료 기준:** 옵션 A 선택 → 실행 조건 입력 → 저장 → 다시 열었을 때 유지, 미선택 상태로 5단계 진입 차단

---

## 5. 5단계 보완 기획안과 저장 (9/18)

**담당:** 개발 담당
**사양:** [document/README.md](src/policy_signal_map/document/README.md), [resources/documents/README.md](src/policy_signal_map/resources/documents/README.md), [static/js/README.md](src/policy_signal_map/web/static/js/README.md)
**왜:** 최종 결과물은 권고 목록이 아니라 **전체 보완 기획안**이다 (최종기획서 13장: 사용자가 권고를 다시 문서에 옮기는 부담을 줄이기 위해). 이 단계가 없으면 서비스의 핵심 가치가 증명되지 않는다.

### 5-1. 기획안 생성 `document/`

- [x] 완료 (9/16, 커밋 `a58051f`) — 테스트는 `test_document_describe/builder/render/filename.py`로 나눠 작성

**왜:** AI 없이 확인된 입력과 선택만 양식에 채워야 미정 값을 지어내지 않는다 (워크플로우 S04).

**할 일**
1. `models.py`: 문서, 장, 줄(원안/변경/추가/추가 확정 필요), 변경 기록, 근거 참조
2. `builder.py`
   - 8개 장 구성
   - 선택 없는 항목은 원안 유지, 채택·수정만 변경 기록
   - 취소한 변경은 흔적 없음, 원안에 없는 값은 추가 확정 필요
   - 재확인 필요가 있으면 생성 차단
3. `render.py`: Jinja2 양식 렌더링, 사용자 입력 Markdown 이스케이프, 변경 줄 표기 `〔변경 · 변경 001〕`(내부 규칙 번호 비노출)
4. `filename.py`: `보완기획안_{사업명}_{YYYYMMDD}.md`, 금지 문자 제거
5. `resources/documents/plan.md.j2`, `request.md.j2`
6. `tests/test_document.py`

**완료 기준:** 선택 없음 → 변경 0건, 옵션 A 채택 → 7장 반영과 별첨 1 기록, 취소 → 흔적 없음, 예산 미정 → 금액 없음

### 5-2. 기획안 화면

- [x] 완료 (9/16, 커밋 `d321677`) — 원안↔보완안 두 열 대신 보완안 목록 + 변경 전후 표 (사유는 `templates/steps/README.md`)

**할 일:** `draft.html`(원안↔보완안, 변경 요약, 근거 추적), `document.html`(전체 문서), `request.html`(요청서 초안, "계약·자료 제공이 확정된 것이 아닙니다" 고정), `routes/draft.py`(다운로드 응답에 한글 파일명 인코딩), `partials/evidence_trace.html`

**완료 기준:** 다운로드 응답 본문이 `document.html`에 보이는 내용과 같음

### 5-3. 결과 저장 `save.js`, 근거 추적 `trace.js`

- [x] 완료 (9/16, 커밋 `d321677`) — 브라우저별 저장 확인은 사용자 확인 대기

**왜:** checks.md 결정: [결과 저장]을 누르면 저장 위치 선택 창이 열리고 사용자가 고른 폴더에 저장한다.

**할 일**
1. `save.js`
   - 다운로드 주소에서 본문·파일명 받기
   - 저장 위치 선택 창이 있으면 사용
   - 사용자가 창을 닫으면 조용히 종료
   - 창이 없는 브라우저는 다운로드 폴더 저장 후 안내
   - 실패 시 전체 문서 보기 안내
2. `trace.js`: 근거 칩 클릭 시 패널 열고 닫기

**완료 기준:** Chrome·Edge에서 폴더 선택 저장, Firefox에서 다운로드 폴더 저장 안내 확인

### 5-4. 임시 화면 삭제

- [x] 완료 (9/16, 커밋 `d321677`)

**할 일:** `routes/steps.py`, `templates/steps/placeholder.html` 삭제, 관련 테스트 정리, README 표 갱신

**완료 기준:** 1~5단계가 모두 실제 화면

### 5-6. PDF로 저장 (9/16 추가)

- [x] 완료 — `@media print`(A4 여백, 화면 전용 요소 숨김, 장 단위 페이지 나눔, 표 최소 폭 해제)와 `print.js`([PDF로 저장] → 브라우저 인쇄 창)

**왜:** 결재·공유는 고쳐 쓸 수 없는 사본이 필요하다. `.md` 저장은 담당자가 고쳐 쓰는 원고로 남긴다.
**서버 PDF 생성기를 쓰지 않은 이유:** 한글 글꼴과 브라우저 바이너리를 배포에 얹어야 해서 공모전 일정에 위험이 크다.
**완료 기준:** 인쇄 결과에 상단바·버튼이 없고, 변경 줄 표시와 표가 A4 폭에서 잘리지 않으며, 합성 수치 고지가 남는다.

### 5-5. 외부 편집기 확인

- [ ] 완료 — 미확인. 5단계에서 [결과 저장]으로 파일을 만든 뒤 확인한다

**왜:** 워크플로우 12장 "Markdown 내보내기: 외부에서 열었을 때 화면과 내용 일치". 표·목록이 편집기에 따라 깨질 수 있다.

**할 일:** 저장한 파일을 메모장, VS Code, Markdown 뷰어 1개로 열어 표·변경 표기·추가 확정 필요 표시 확인

**완료 기준:** `checks.md` 테스트 표 "Markdown 저장" 결과 기록

---

## 6. 실제 결과 연결과 전체 검증 (9/18~19)

**담당:** 6-1·6-2 공동, 6-3·6-4 개발
**왜:** 합성 자료로 만든 흐름이 실제 결과에서도 같은 수치·질문·문서를 만드는지 확인해야 제출 PDF에 실제 분석을 담을 수 있다 (최종기획서 10장).

### 6-1. 2차 연결 확인

- [ ] 완료

**할 일**
1. 데이터 담당의 실제 `review_evidence_real_v{n}.json`을 `private/`에 둔다
2. `PSM_EVIDENCE_PATH`를 실제 파일로 설정하고 `PSM_LLM_PROVIDER=none` 확인
3. 데이터 담당의 분석 결과와 화면을 대조: 월별 금액·비중, 반대 방향 구간 수, 질문, 기획안 근거 표시
4. 차이가 있으면 입력조건·근거 ID·잘못된 표시·기대 결과를 기록해 원인을 가림 (서비스 계산인지 파일인지). **결과 파일을 직접 고치지 않는다**
5. Claude 등 외부 AI 도구에 실제 파일 내용을 보여주지 않는다

**완료 기준:** `checks.md` 2장 2차 결과 칸에 일치 여부와 조치 기록

### 6-2. 시도 한 곳 추가 여부

- [x] 결정 — **9/18 이번 제출에는 추가하지 않음(향후 확장).** 이유와 기록은 `checks.md` 1장 "시도(제주) 근거 추가" 행, 협의 배경은 저장소 밖 `협의사항.md` A-1
- [x] 구현 — **9/19 복제본 `Service_사본_20260918/`에서 지역 확장을 코드로 완성.** 고른 시도의 자료가 있으면 그것을 쓰고, 없거나 쓸 수 없으면 전국으로 되돌린 뒤 되돌린 이유를 화면에 밝힌다. 연결 키는 행정표준코드 10자리(C-1). 계획은 저장소 밖 `데이터시나리오코드.md`. **원본 저장소에는 아직 반영하지 않았다**

**왜:** 워크플로우 8-4장은 시도 추가를 조건부로 허용했다. 선정 이유를 먼저 정하고 자료 조건을 확인해야 하며, 원하는 방향이 나오는 지역을 고르면 안 된다.

**할 일:** 데이터 담당이 조건 6가지 확인 결과와 applicability를 제공하면, 서비스는 "해당 시도로 분류된 자료의 지표 비교"와 시군구 기획 시 "넓은 범위 참고"를 표시

**완료 기준:** 추가하면 3-3의 해당 테스트와 화면 확인, 추가하지 않으면 checks.md에 "전국 참고 유지"와 이유 기록

**9/19 상태:** 복제본에서 코드·시험·문서를 마쳤다(시험 458개 통과). 실제 시도 자료를 받으면 파일만 바꿔 끼우면 되고, 사람이 숫자를 대조한다(`checks.md` 2장).

### 6-3. 시험 16개 결과 기록

- [ ] 완료 — 9/17 갱신: 개발 확인 가능한 줄은 모두 채움. 남은 칸: GENDER_CD(데이터 담당), Markdown 외부 편집기(사용자), 공개 배포(보류). **시도 자료 칸은 6-2를 하지 않기로 해 해당 없음(9/18)**

**할 일:** `checks.md` 4장 표의 결과 칸을 테스트 이름·수동 확인 결과로 채움. GENDER_CD 시험은 데이터 담당 결과를 받아 기록

**완료 기준:** 16칸 모두 채움, 실패 항목은 6장 남은 작업으로 이동

### 6-4. 브라우저 전체 흐름 확인

- [x] 완료 (2026-09-17, 결과는 checks.md 5장)

**할 일:** 아래 5가지 흐름을 처음부터 끝까지 확인
1. 정상 사례 → 옵션 A → 저장
2. 자료 부족 보류 사례
3. 대안 선택 후 취소
4. 5단계까지 간 뒤 목표 변경 → 재확인
5. 모바일 폭 400px

**완료 기준:** 흐름별 결과와 콘솔 오류 여부를 checks.md에 기록

### 6-5. 전체 검토 수정 (2026-09-17)

- [x] 완료

**왜:** 전체 서비스 검토에서 테스트로 재현한 문제를 시연 전에 고친다.

| # | 문제 (재현) | 수정 | 커밋 |
|---|---|---|---|
| 1 | 원안을 바꾼 뒤 선택을 다시 저장해도 재확인 표시가 다시 붙어 5단계로 갈 수 없음 | 선택 정리를 원안 제출마다 한 번만 (`WorkState.sync_choices`) | `35feeb3` 6-5a |
| 2 | 4단계를 거치지 않고 5단계·내려받기로 가면 재확인 없이 바뀌기 전 선택으로 문서 생성 | 5단계 화면·전체 문서·요청서·내려받기도 문서 생성 전에 선택 정리 | `35feeb3` 6-5a |
| 3 | 질문이 사라지거나, 채택을 원안 유지로 바꾸거나, R07 A를 B로 바꾸면 쓰지 않는 실행 조건이 8장 "추가 확정 필요"에 남음 | 실행 조건을 쓰는 대안이 없으면 지움 (`drop_unused_execution`) | `17da1a9` 6-5b |
| 4 | AI 의견을 기다리는 사이 원안이 바뀌면 이전 원안 의견이 새 원안 것으로 보관됨 (코드 확인) | 요청을 시작한 원안과 같을 때만 보관 | `cc7fecb` 6-5c |
| 5 | 결과 저장이 실패 응답을 받으면 주소를 그대로 내려받아 오류 문장이 `.md`로 저장될 수 있음 | 본문을 먼저 받아 409면 재확인 안내, 실패면 안내만 | 6-5d |

**검토 반영 (계획 검토 9/17):**

| 검토에서 찾은 것 | 반영 |
|---|---|
| 계획의 "대안을 고른 선택이 없으면 지움" 기준으로는 R07 A → B 변경이 해결되지 않음 (시제품에서 재현) | "실행 조건을 쓰는 대안(입력칸이 있는 A)"을 채택·수정한 선택이 없으면 지우는 기준으로 변경. 규칙 정보가 필요해 `choices/selection.py` 함수로 둠 |
| R04 B는 입력칸이 없지만 문서 문장에서 공유 주기(`{cycle}`)를 씀. 지우면 입력한 주기가 [추가 확정 필요]로 바뀌고 8장 목록과도 어긋남 | 사용자 결정 **가**: 문서 문장에서 실행 조건을 쓰는 대안도 "사용 중"으로 봄 |
| 5단계를 연 뒤 원안이 바뀌면 결과 저장이 409를 내려받을 수 있음 | 저장 스크립트 수정을 6-5d에 추가 |

**남긴 것:** 보관한 선택 안내("…건을 보관했습니다")가 원안을 여러 번 바꾸면 누적됨(문서에는 반영 안 됨). R04 B만 고르고 대안 A를 한 번도 고르지 않으면 문장 안의 [추가 확정 필요]가 8장에 모이지 않음(이전부터 있던 동작).

---

## 7. 공개 배포 (9/21 반영)

> **Vercel로 결정(사용자, 2026-09-21).** 공개 합성 자료, Google AI Studio API, Upstash Redis 세션을 사용한다.

**담당:** 7-1 사용자 결정, 7-2·7-3 개발
**왜:** 심사위원과 멘토가 링크로 직접 체험할 수 있어야 한다. 합성 자료만 담은 공개 버전과 실제 자료로 검증한 내부 버전을 분리한다 (워크플로우 S06).

### 7-1. 호스팅 결정

- [x] Vercel FastAPI + Upstash Redis

**왜:** Vercel 인스턴스가 바뀌어도 1~5단계 상태를 유지해야 한다. 쿠키에는 무작위 ID만 두고 기획·선택·AI 의견은 Redis에 저장한다.

### 7-2. 배포 설정

- [x] 코드 완료

**할 일**
- `pyproject.toml` Vercel 진입점과 `vercel.json`
- Vercel에서 `secure`인 세션 쿠키
- Redis JSON 세션, 24시간 만료, 동시 저장 충돌 검사
- `/health` 상태 확인
- 공개 배포 환경변수(Google AI + Upstash Redis)

**완료 기준:** 공개 주소에서 1~5단계와 저장 동작

### 7-3. 배포 전 검사

- [x] 코드 검사 완료
- [ ] Vercel 프로젝트 연결·Secret 등록 뒤 Preview 실배포 확인

**할 일:** `uv run pytest`, `scripts/check_public_bundle.py` 통과 확인 후 배포

**완료 기준:** 검사 결과를 checks.md에 기록

---

## 8. 제출 준비 (9/19~21)

**담당:** 표 참고
**사양:** [docs/README.md](docs/README.md), [docs/screenshots/README.md](docs/screenshots/README.md)
**왜:** 심사는 제출 PDF로 이루어진다. 분석 기법과 화면을 나열하는 데서 끝나지 않고 "이 수치 때문에 무엇을 확인했고, 선택 후 무엇이 바뀌었는가"를 연결해야 한다 (최종기획서 10장).

### 8-1. 기존 대안 비교표 `docs/alternatives_comparison.md`

- [ ] 작성 (개발)
- [ ] 검수 (데이터)

**왜:** 기존 유사성 보고서는 초기 상권·인구구조 구상을 평가한 것이라 현재 서비스의 독창성 근거가 되지 못한다 (워크플로우 13장).

**할 일:** 비교 기준 4가지(자료 적용 범위 확인·담당자 선택·전체 기획안 반영·변경 근거), 기존 보고서의 서비스·출처 재사용, 확인 못 한 기능은 "확인 못 함", 확인 날짜

### 8-2. 사용법과 화면 캡처

- [ ] 완료 — 9/17: `usage_guide.md`, 캡처 6장(스크립트). 남은 것: `05_saved_markdown.png`(사용자)

**할 일:** `docs/usage_guide.md`, 캡처 7장(`docs/screenshots/` 이름 규칙). 합성 화면만 저장소에 둠

**주의 (2026-09-16):** 원래 공개 배포 화면으로 캡처할 계획이었으나 배포가 보류됐다. 어떤 환경의 화면으로 찍을지 먼저 정한다(로컬 실행 화면이 대안).

### 8-3. PDF 구성

- [ ] 구성안
- [ ] PDF 완성

**할 일**
1. `docs/submission_outline.md`: 평가 관점 5개별 증거
2. 실제 분석 화면은 데이터 담당 확인 후 PDF에만 넣고 저장소에는 올리지 않음
3. 페이지마다 실제 구현 / 분석 예시 / 향후 기능 표시

### 8-4. 3차 연결 확인 (9/20)

- [ ] 완료

**할 일:** PDF의 수치·화면·기획안 예시가 실제 분석 결과·서비스 동작과 일치하는지 두 사람이 대조

### 8-5. 접수

- [ ] 9/21 접수
- [ ] 9/22 18:00 전 접수 확인

---

## 이번 범위에서 뺀 것

| 항목 | 처리 | 근거 |
|---|---|---|
| 랜딩 페이지 | 9/19~20 여유 시 추가 | checks.md 랜딩 페이지 |
| R06 고액 소비 근거 | 분석 예시로 제시, 여유 시 화면 | 최종기획서 5-1, 11장 |
| LLM C 단계 | **9/16 구현 완료 (로컬 전용, 합성 자료).** [6LLM참고의견계획.md](./6LLM참고의견계획.md) · 실제 모델 확인과 기획서 11장 한 줄 추가 여부 결정이 남음 | checks.md 구현 순서 |
| R02 업종 후보 | 향후 기능 | 최종기획서 5-2, 11장 |
| 로그인, 작업 상태 저장·복원, 문서 업로드, 대규모 RAG, 결제·정산 연동 | 하지 않음 | 최종기획서 11장 후순위 |

## 일정이 밀릴 때 줄이는 순서

워크플로우 13장 원칙: 장기 보관·문서 업로드·RAG를 줄이고 첫 사례의 전체 문서 생성과 PDF를 유지한다.

1. 랜딩 페이지·LLM 제외
2. R01·R03·R04 기본 검토를 R05와 같은 "추가 확정 필요 표시" 수준으로 축소
3. 차트를 월별 표로 대체 (`charts.js` 생략)
4. 근거 추적 패널을 별첨 표로 대체 (`trace.js` 생략)
5. **끝까지 유지:** R07 사례의 입력 → 근거 → 질문 → 선택 → 기획안 저장 흐름과 제출 PDF

## 정할 것

| 결정 | 기한 | 관련 항목 |
|---|---|---|
| 데이터 담당 확인 사항 (형식·운영·공유) | 9/16 | 0-1 ~ 0-3 |
| R01·R03·R04 조건 확정 (데이터 담당과) | 9/17 | 0-4, 3-1 |
| ~~공개 배포 여부와 호스팅 방식~~ → **Vercel + Upstash Redis** (사용자 결정 9/21) | 코드 반영, Preview 실배포 확인 남음 | 7장 |
| ~~제출 화면 캡처 방법~~ → **로컬 실행 + 합성 자료** (사용자 결정 9/17) | 8-2 전 | 8-2 |
| 인수인계 사본의 〔개발 반영〕을 최종기획 원본에 옮길지, 11장에 AI 참고 의견 한 줄을 넣을지 | 8-3 전 | 저장소 밖 상위 폴더 |
| ~~실제 자료로 AI 참고 의견을 쓸지~~ → **로컬 허용** (사용자 결정 9/17, checks.md 1장) | 필요할 때 사용자가 지시 | `6LLM참고의견계획.md` |
| 시도 한 곳 추가 여부 (데이터 담당 판단 반영) | 9/18 | 6-2 |
| 입력 화면 행정코드와 근거 파일 `region_key` 연결 규칙 (데이터 담당 Q5 답 반영) | 2-3 전 (9/17) | 2-3, 1근거계산계층계획 C-2 |
| 합성/실제 근거 파일 연결 방식 (보류 중) | 6-1 전 | 1-1, 6-1 |

## 작업 공통 규칙

- **순서:** 폴더 README 사양 확인 → 테스트 작성 → 구현 → `uv run pytest` → 브라우저 확인(화면 작업) → README 표 상태 갱신 → checks.md 진행 기록 → 커밋
- **커밋:** 항목 번호 단위로 커밋하고 메시지에 번호를 넣는다 (예: `1-3 근거 파일 형식 검증`)
- **푸시:** 푸시 전 `check_public_bundle.py`(1-6 이후) 통과
- **실제 자료:** `private/`에만 두고, 공개 저장소·외부 AI 도구·화면 캡처 저장소에 넣지 않는다
- **수치:** 서비스가 숫자를 만들거나 보정하지 않는다. 틀리면 데이터 담당에게 전달해 새 버전을 받는다
- **표현:** 판정·점수·성공 확률·빨간 경고를 쓰지 않는다. 관측은 질문으로 전달한다
