# 4. 보완 선택 — 상세 구현 계획

문서 기준일: 2026-09-16
상위 문서: [작업진행.md](./작업진행.md) 4장 · 선행: [3검토질문계획.md](./3검토질문계획.md) (완료)
기준: 워크플로우 S03·S05·11장·12장 · 최종기획서 3장·4-4·4-5장 · 시안 S4

3단계 질문마다 담당자가 **원안 유지 / 채택 / 수정 / 보류**를 고르고, 필요한 실행 조건을 입력한다. 서비스는 대신 고르지 않고 점수·성공 확률을 표시하지 않는다.

---

## 1. 확정한 결정 (검토 반영)

| # | 결정 | 내용 | 이유 |
|---|---|---|---|
| ① | 저장 방식 | 질문마다 따로 저장 (`POST /step/4`에 `question_key`) | 하나를 고쳐도 다른 선택이 영향받지 않고 취소도 질문 단위 |
| ② | 실행 조건 입력 | `merge_group` 단위로 한 번만 입력하고 여러 질문이 공유 | 같은 자료를 두 번 묻지 않음 (워크플로우 11장) |
| ③ | **기본 검토 질문의 대안** | R01·R03·R04에 **대안을 1~2개씩 규칙 JSON에 추가** (A-1 ㉮) | 대안이 없으면 "채택"을 고를 수 없는데 워크플로우 11장은 이 규칙들이 문서를 바꾸도록 정함 |
| ④ | **안내(notice) 처리** | 대안 B(해석 조건 명시)와 원안 유지 중 고를 수 있게 함. **선택은 필수가 아님** | 참고 현황으로 쓰는 기획도 "분모·범위를 문서에 적기"를 반영할 방법이 있어야 함 |
| ⑤ | **보류(held) 처리** | 선택 없이 문서 8장(추가 확인사항)에 자동 기록. 사유·근거 ID 포함 | 근거가 부족하다는 사실은 담당자 선택과 무관한 관측 |
| ⑥ | 5단계 진입 조건 | **미선택 질문 0건**이면 통과. 보류도 결정으로 인정. 재확인 필요가 있으면 막고 목록 표시 | 워크플로우 11장 "최종 문서 생성 전에 확인받는다" |
| ⑦ | 재확인 처리 | 바뀐 입력 항목과 규칙의 `related_fields`가 겹치는 선택만 재확인 필요 | 사업명 오타 수정으로 전체를 다시 고르게 하지 않음 |
| ⑧ | 사라진 질문 | 보관함(`archived`)으로 옮기고 "원안 변경으로 더 이상 해당하지 않음" 표시. 문서에는 반영하지 않음 | 조용히 사라지면 사용자가 알 수 없음 |

후순위(일정이 밀리면 뺀다): 선택 이유 입력칸.

---

## 2. 규칙 JSON에 추가할 대안 (결정 ③)

문구는 `docs/review_rules.md`에 함께 적고 데이터 담당 검수를 받는다.

| 규칙 | 대안 | 바뀌는 곳 | 필요 자료 | 운영 부담 | 추가 입력 |
|---|---|---|---|---|---|
| R01 | A 사용처 범위를 확정 항목으로 추가 | 4. 혜택·사용처 | 참여 점포 목록 또는 상권 범위 | 낮음 | — |
| R01 | B 목표에서 참여 상점 이용 확대 제외 | 2. 목적·대상 | 없음 | 낮음 | — |
| R03 | A 참여자 확인 자료 추가 | 7. 성과 측정계획 | 쿠폰 발급·사용·정산 자료 | 중 | 실행 조건(공유) |
| R03 | B 대상 해석 범위 명시 | 2. 목적·대상 | 없음 | 낮음 | — |
| R04 | A 기간에 맞춘 별도 실적 수집 | 7. 성과 측정계획 | 사업 기간 참여 실적 | 중 | 실행 조건(공유) |
| R04 | B 카드 지표를 참고로 두고 확인 주기 명시 | 3. 일정 | 없음 | 낮음 | — |

R03·R04의 A는 R07의 A와 같은 `participation_data` 묶음이라 **실행 조건을 한 번만 입력**한다 (결정 ②가 실제로 동작하게 됨).

---

## 3. 데이터 형태

```python
class Decision(StrEnum):
    KEEP_ORIGINAL = "keep_original"   # 원안 유지
    ADOPT = "adopt"                   # 채택
    MODIFY = "modify"                 # 수정 (대안을 고르고 문장을 고침)
    HOLD = "hold"                     # 보류

@dataclass(frozen=True)
class ExecutionInput:               # merge_group 단위로 공유
    collect_items: tuple[str, ...]    # 수집자료 (1개 이상 필수)
    availability: str | None          # 확보 여부: available | negotiating | unavailable
    owner: str                        # 담당자 (비우면 추가 확정 필요)
    cycle: str                        # 확인 주기 (비우면 추가 확정 필요)

@dataclass(frozen=True)
class Choice:
    question_key: str                 # 규칙 ID
    rule_id: str
    merge_group: str | None
    decision: Decision
    option_id: str | None
    modified_text: str | None
    reason: str
    evidence_ids: tuple[str, ...]
    needs_recheck: bool = False

@dataclass(frozen=True)
class ArchivedChoice:
    choice: Choice
    reason: str                       # "원안 변경으로 더 이상 해당하지 않음"

@dataclass
class ChoiceSet:
    choices: dict[str, Choice]
    executions: dict[str, ExecutionInput]
    archived: list[ArchivedChoice]
```

## 4. 검증 규칙 (`choices/selection.py`)

| 규칙 | 오류 문구 |
|---|---|
| 결정값이 목록에 있어야 함 | "선택 항목을 골라 주세요." |
| 채택·수정은 대안 ID 필요, 규칙 JSON에 있는 ID만 | "대안을 골라 주세요." |
| 수정은 고친 문장 필요 | "수정할 내용을 적어 주세요." |
| 대안이 실행 조건을 요구하면 수집자료 1개 이상 | "수집할 자료를 하나 이상 적어 주세요." |
| 담당자·주기는 비워도 됨 | (오류 아님 → "추가 확정 필요") |
| 확보 여부는 목록 값만 | "확보 여부를 다시 골라 주세요." |

- 아직 없는 자료는 **계획으로 남긴다.** 확보된 것처럼 바꾸지 않는다 (워크플로우 S03)
- 대안 D는 **요청서 초안** 상태로만 저장한다. 계약·제공 확정 필드를 두지 않는다
- 취소하면 기록을 지운다 (문서에도 남지 않게, 워크플로우 12장)

`pending_from_choices()`가 돌려주는 "추가 확정 필요" 항목: 담당자 미입력, 확인 주기 미입력, 확보 여부가 "확보 어려움/협의 중", 보류한 질문.

## 5. 재확인과 보관 (`choices/recheck.py`, `plan/changes.py`)

`diff_plan(before, after) -> frozenset[str]` — 바뀐 입력 항목 이름. 규칙의 `related_fields`와 같은 이름을 쓴다.

| 항목 이름 | 비교 대상 |
|---|---|
| `goals` | 목표 목록 + 기타 설명 |
| `metrics` | 성과지표 목록 + 기타 설명 |
| `indicator_use`, `target`, `usage_place`, `data_status` | 각 값 |
| `region` | 범위·시도·시군구 코드 |
| `period` | 시작일·종료일 |
| `budget` | 상태와 금액 |
| `name`, `fixed_conditions` | 각 값 (어떤 규칙도 참조하지 않음 → 재확인 없음) |

- `mark_recheck(choice_set, changed_fields)`: 겹치는 선택만 `needs_recheck = True`
- `archive_missing(choice_set, outcomes)`: 다시 실행한 검토에 없는 질문의 선택을 보관함으로

## 6. 화면 `/step/4`

| 구역 | 내용 |
|---|---|
| 머리 | 제목, "서비스는 정답을 고르지 않습니다. 성공 확률이나 점수를 표시하지 않습니다." |
| 재확인 필요 | 원안이 바뀌어 다시 확인할 선택 목록 (있을 때만) |
| 질문 카드 | 질문 문구 + 대안 카드(바뀌는 곳·필요 자료·운영 부담) + [원안 유지] [보류] + 수정 문장 칸 + [저장] [선택 취소] |
| 실행 조건 | 대안이 요구할 때만. 같은 묶음의 첫 질문에서 입력하고, 다른 질문에는 "R07에서 입력한 내용을 함께 씁니다" |
| 안내(notice) | 대안 B와 원안 유지만 표시. 선택하지 않아도 5단계로 갈 수 있음 |
| 보류(held)·추가 확정 필요(pending) | 선택 없이 "문서의 추가 확인사항에 기록됩니다" 표시 |
| 하단 | [← 검토 질문으로] [보완 기획안 만들기 →] (막히면 이유 목록) |

- 라우트: `GET /step/4`, `POST /step/4`(질문 하나 저장), `POST /step/4/cancel`. 저장 후 303
- `partials/option_card.html`, `pending_badge.html`, `static/js/choices.js`
- 라우터는 임시 라우터보다 앞에 등록, `steps.py`는 5단계만

## 7. 테스트

| 파일 | 개수 | 확인 |
|---|---|---|
| `test_plan_changes.py` | 7 | 항목별 변경 감지, 사업명 변경은 재확인 없음 |
| `test_choices.py` | 20 | 검증 6종, 취소, 추가 확정 필요 목록, 재확인 표시, 보관, 5단계 차단 신호, 실행 조건 공유 |
| `test_choice_routes.py` | 12 | 잠금, 저장·유지, 취소, 공유 입력 1회, 미선택 차단, 재확인 목록, 안내 선택 없이 통과, 판정 단어 없음 |
| 브라우저 | — | 예시 기획 → 대안 A → 실행 조건 → 저장 → 재방문, 취소, 원안 변경 후 재확인, 400px |

238개 → 약 277개

## 8. 커밋

| # | 커밋 |
|---|---|
| 1 | `4-0 기본 검토 대안 추가` (규칙 JSON·docs) |
| 2 | `4-1 원안 변경 비교` |
| 3 | `4-2 보완 선택 로직` |
| 4 | `4-3·4-4 세션·화면` |

## 9. 위험

| 위험 | 대응 |
|---|---|
| 새 대안 문구가 근거 이상으로 단정 | 금지 단어 테스트가 JSON 전체 문구를 검사, `docs/review_rules.md` 검수 요청 |
| 상태가 복잡해져 5단계에서 꼬임 | 선택은 규칙 ID로만, 실행 조건은 묶음으로 분리 |
| 일정 | 선택 이유 입력은 후순위 |
