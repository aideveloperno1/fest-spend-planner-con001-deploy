# 5. 보완 기획안과 저장 — 상세 구현 계획

문서 기준일: 2026-09-16
상위 문서: [작업진행.md](./작업진행.md) 5장 · 선행: 1~4번 완료
기준: 워크플로우 S04·S05·12장 · 최종기획서 3장·4-5·6장 · [document/README.md](src/policy_signal_map/document/README.md)

원안과 담당자 선택으로 **전체 보완 기획안**을 만들고, 화면에서 비교한 뒤 Markdown 파일로 저장한다. AI를 쓰지 않고 확인된 값만 양식에 채운다.

---

## 1. 확정한 결정 (검토 반영)

| # | 결정 | 내용 | 이유 |
|---|---|---|---|
| ① | 대안의 문서 문장 | 규칙 JSON의 각 대안에 `document` 추가: `section`(장 번호), `mode`(`append`·`replace`), `replace_key`, `lines` | 문구 원본을 JSON 한곳에 둔다. 교체가 필요한 대안(R01 B 등)이 있어 방식도 함께 적는다 |
| ② | 라벨·원안 문장 | 선택지 한글 라벨을 **루트 `labels.py`로 이동**하고, `document/describe.py`가 원안 문장을 만든다. 화면도 같은 함수를 쓴다 | `document/`는 `web/`을 import할 수 없다(경계 규칙). 원안 문장 함수가 아직 없다 |
| ③ | "수정" 동작 | 담당자가 고친 문장이 **대안 문장을 대체**한다. 별첨 1에는 "대안 A를 고쳐서 적용"으로 남긴다 | 사용자가 최종 문장을 정한다 |
| ④ | 생성일 | `build_document(..., today=...)`로 주입한다 | 같은 입력이면 같은 문서가 나와야 테스트로 고정할 수 있다 |
| ⑤ | 8장 구성 | **입력 검증 미정 항목 + 선택에서 생긴 항목**(보류·담당자·주기·확보 협의) + 보류/추가 확정 필요 결과의 사유·근거. 규칙 문구는 별첨 1의 근거로만 | 같은 항목이 두 번 실리지 않게 한다 |

추가 규칙: 같은 장에 같은 문장이 두 번 들어가면 한 번만 남긴다 (R07·R03·R04의 A를 모두 채택한 경우).

## 2. 문서 구성

```
1 사업 개요   2 목적·대상   3 일정   4 혜택·사용처   5 모집·정산
6 예산 상태   7 성과 측정계획   8 추가 확인사항
별첨 1 변경 전후 · 별첨 2 근거와 해석 한계 · 별첨 3 정밀 분석 요청서 초안(대안 D 선택 시)
```

원안 문장(`describe.py`)은 줄마다 **키**를 갖는다: `name`, `period`, `region`, `goals`, `target`, `schedule`, `usage_place`, `recruit`, `budget`, `metrics`, `indicator_use`, `collection`.
대안의 `replace_key`가 이 키를 가리키면 그 줄을 바꾸고, 아니면 장 끝에 덧붙인다.

입력에 없는 항목은 채우지 않고 `원안에 기재 없음 **[추가 확정 필요]**`로 적는다.

## 3. 대안별 문서 문장 (규칙 JSON)

| 규칙·대안 | 장 | 방식 | 문장 |
|---|---|---|---|
| R07 A 참여 실적 추가 | 7 | 덧붙이기 | 주요 지표: {collect_items} / 참고 지표: 외국인 결제금액·비중(전체 분모·미상 제외) / 수집 담당: {owner} / 확인 주기: {cycle} / 자료 확보 여부: {availability} |
| R07 B 해석 조건 명시 | 7 | 덧붙이기 | 지표 해석 조건: 분모 기준(전체·미상 제외)과 전국 참고 범위를 함께 기록 / 카드 지표는 단독 성과 판정에 사용하지 않음 |
| R07 C 자료 확보 전 | 8 | 덧붙이기 | 성과 확인 자료 확보 방안 협의 (담당·일정 미정) |
| R07 D 정밀 분석 요청서 초안 | 8 | 덧붙이기 | 정밀 분석 요청서 초안을 별첨 3으로 첨부 (계약·자료 제공이 확정된 것은 아님) |
| R01 A 사용처 범위 확정 항목 | 4 | 덧붙이기 | 사용처 범위 확정 필요: 참여 점포 목록 또는 상권 범위 |
| R01 B 목표에서 제외 | 2 | 교체(`goals`) | 사업 목표: {goals_without_store_usage} |
| R03 A 참여자 확인 자료 | 7 | 덧붙이기 | 참여자 확인 자료: {collect_items} / 수집 담당: {owner} / 확인 주기: {cycle} |
| R03 B 대상 해석 범위 명시 | 2 | 교체(`target`) | 사업 대상: {target} (성과 자료는 외국인 전체 기준이며 관광객과 거주 외국인을 구분하지 않음) |
| R04 A 기간 별도 실적 | 7 | 덧붙이기 | 사업 기간 실적: {collect_items} / 수집 담당: {owner} / 확인 주기: {cycle} |
| R04 B 확인 주기 명시 | 3 | 덧붙이기 | 카드 지표는 참고 자료로 사용하고, 사업 기간 성과는 {cycle} 주기로 별도 확인 |

자리표시자: `{collect_items} {owner} {cycle} {availability} {usage_place} {target} {goals} {goals_without_store_usage} {metrics}`
**빈 값은 빈칸이 아니라 `[추가 확정 필요]`로 채운다.**

## 4. 만들 파일

| 파일 | 내용 |
|---|---|
| `labels.py` (루트로 이동) | 목표·지표·지표 용도·자료 상태 라벨 (화면·문서 공용) |
| `document/models.py` | `PlanDocument`, `Section`, `Line`(key·text·state), `Change`, `EvidenceRef`, `DocumentBlocked` |
| `document/describe.py` | 원안 문장 (장·줄 키) |
| `document/builder.py` | 선택 적용, 변경 목록, 8장, 근거, 요청서 초안 여부 |
| `document/render.py` | Jinja2로 Markdown, 사용자 입력 이스케이프 |
| `document/filename.py` | `보완기획안_{사업명}_{YYYYMMDD}.md` |
| `resources/documents/plan.md.j2`, `request.md.j2` | 양식 |
| `web/routes/draft.py` | `/step/5`, `/step/5/document`, `/step/5/download`, `/step/5/request` |
| `templates/steps/draft.html`, `document.html`, `request.html`, `partials/evidence_trace.html` | 화면 |
| `static/js/save.js`, `trace.js` | 저장 위치 선택, 근거 추적 |

## 5. 화면 `/step/5`

- 4단계 차단 사유가 남아 있으면 **4단계로 돌려보내고 이유를 표시**한다
- 요약: 변경 N건 · 추가 확정 필요 N건 · 원안 유지 N개 장
- 원안 ↔ 보완안 두 열, 변경 줄에 `[변경]`과 근거 칩, 근거 칩을 누르면 추적 패널
- 버튼: [결과 저장] [전체 문서 보기] [정밀 분석 요청서 초안](대안 D 선택 시). 인쇄/PDF는 범위 밖

## 6. 테스트

| 파일 | 개수 | 확인 |
|---|---|---|
| `test_document_describe.py` | 6 | 장·줄 키, 미입력 항목 표기 |
| `test_document_builder.py` | 18 | 선택 없음 → 변경 0, 덧붙이기·교체, 수정이 대안 문장을 대체, 빈 담당자 → 추가 확정 필요, 보류 → 8장, 중복 문장 제거, 차단 시 생성 안 함, 근거·전국 참고 |
| `test_document_render.py` | 8 | 구조, 이스케이프, 같은 입력이면 같은 문자열(생성일 주입), 합성 표시 |
| `test_document_filename.py` | 5 | 금지 문자·길이·빈 사업명 |
| `test_draft_routes.py` | 12 | 차단 시 4단계로, 비교 화면, 전체 문서, 다운로드 헤더·본문, 요청서, 임시 화면 제거 확인 |
| 수동 | — | 저장 위치 선택 창(사용자 확인 필요), 외부 편집기 열기, 400px |

280개 → 약 329개

## 7. 커밋

| # | 커밋 |
|---|---|
| 1 | `5-0 라벨 이동과 대안별 문서 문장 정의` |
| 2 | `5-1 보완 기획안 생성` |
| 3 | `5-2 보완 기획안 화면과 다운로드` |
| 4 | `5-3·5-4 결과 저장 스크립트와 임시 화면 삭제` |

## 7-1. 구현하면서 계획과 달라진 것 (9/16)

| # | 계획 | 실제 | 이유 |
|---|---|---|---|
| 1 | `test_document.py` 한 파일 | `test_document_describe/builder/render/filename.py` + `document_helpers.py` | 한 파일이 너무 커지고, 깨진 곳을 파일 이름으로 바로 알 수 있다 |
| 2 | 5단계 화면 "원안 ↔ 보완안 두 열" | 보완안 목록 + 변경 전후 표 | 좁은 화면에서 두 열은 접히고 같은 내용을 두 번 보여준다. 원안은 표의 "원안" 칸에서 본다 |
| 3 | `[추가 확정 필요]`를 문장에 `**`로 박아 둠 | 문서 렌더 단계에서 굵게 처리 | 화면에 `**`가 그대로 보였다. 표시 기호는 데이터가 아니라 출력 형식이다 |
| 4 | `request.md.j2`를 `plan.md.j2`가 include | 별첨 3은 `plan.md.j2`에 직접, `request.md.j2`는 요청서만 따로 저장할 때 사용 | 요청서만 담당 부서에 보내는 경우가 있다 |
| 5 | (없음) | 규칙 파일에 `merge_groups` 한글 이름 추가 | 보완 기획안 8장에 내부 키 `participation_data`가 그대로 실렸다. 이름 없는 묶음은 시작할 때 막는다 |
| 6 | `md_escape`가 대괄호도 이스케이프 | 대괄호는 그대로 둠 | `[추가 확정 필요]` 표기가 `\[추가 확정 필요\]`로 깨졌다. 표를 깨뜨리는 것은 `|`와 백틱이다 |

## 8. 위험

| 위험 | 대응 |
|---|---|
| `where` 문자열과 장 번호가 어긋남 | 장 번호를 상수로 두고, 모든 대안의 `document.section`이 실제 장과 맞는지 테스트 |
| 문서에 실제 수치가 들어감 | 문서 머리에 자료 종류·버전 표시, 생성물을 저장소에 넣지 않음(검사 스크립트) |
| 저장 위치 선택 창은 자동 확인 불가 | 다운로드 대체 경로·헤더는 자동 확인, 폴더 선택은 사용자 확인 |
| 임시 화면 삭제로 기존 테스트 3곳이 깨짐 | 5-4에서 5단계 화면 기준으로 옮김 |
