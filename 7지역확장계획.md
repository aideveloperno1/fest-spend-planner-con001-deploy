# 7. 지역·사업 유형 확장 계획 — 계약 문서

이 문서는 **고도화(지역 소비 프로필 · 유사 지역 · 성과지표 점검 · 사업 유형별 질문)를 만들기 전에 정해 둔 약속**이다.
1~6번 계획서와 같은 자리에 있고 쓰임도 같다. 본문은 작성 당시 기록이라 나중에는 **코드가 우선**이다.

- 작성: 2026-09-20 (묶음 0 — 계약 정리)
- 바깥 계획서: 작업 폴더의 `최종검토v3.md` (왜 이렇게 하는지, 작업 순서)
- **이 문서에는 실제 분석 수치를 적지 않는다.** 기준값은 근거 파일에서 받는다(7장)

---

## 0. 무엇을 만드는가

| 층 | 내용 | 언제 켜지나 |
|---|---|---|
| 공통 | 지역 소비 프로필, 유사 지역, 성과지표 점검 | 지역을 고르면 항상 |
| 사업 유형별 | 쿠폰·지역화폐 / 외국인·관광 / 축제·행사 / 연령 대상 | 고른 유형에 맞는 질문만 |

기존 5단계 흐름(입력 → 근거 → 질문 → 선택 → 보완 기획안)은 **그대로 둔다.** 새 기능은 이 흐름 안에 들어간다.

---

## 1. 확정한 결정

| # | 결정 | 왜 |
|---|---|---|
| D1 | 근거 파일은 **2.0 레코드를 그대로 두고 `profiles` 묶음만 더한다.** 버전 문자열만 허용 목록에 추가하지 않는다 | 2.0 파일을 넣으면 지금 동작이 그대로 되고, 새 화면만 "자료 없음"이 된다. 되돌리기가 파일 경로 한 줄이다 |
| D2 | **보완 기획안의 장은 8개 그대로 둔다.** 프로필 요약과 유사 지역은 **별첨 2(근거와 해석 한계)**에 넣는다 | 장을 늘리면 문서 뼈대·별첨·변경 전후·시험이 모두 따라 바뀐다. 프로필은 "사업 내용"이 아니라 "근거"라서 별첨이 제자리다 |
| D3 | **지역 연결 키는 지금처럼 행정표준코드 10자리.** 서비스가 5자리를 받아 뒤에 0을 붙여 추측하지 않는다 | 추측해서 이으면 틀린 지역의 수치를 그 지역 것처럼 보여 주게 된다 |
| D4 | **질문 이름표(내부 키)를 규칙 번호에서 떼어낸다.** 한 규칙이 질문을 둘 내면 `규칙ID-이름` | 지금은 둘이 같아서, 기간 질문과 시기 질문을 나누면 선택이 서로 덮어쓴다 |
| D5 | **재확인 기준(관련 입력 항목)도 질문 단위로 내린다.** 질문에 없으면 규칙 값을 쓴다 | 지금은 규칙 단위라 기간만 바꿔도 시기 선택까지 "다시 확인"이 뜬다 |
| D6 | **기준값(하위 20% 같은 것)은 코드에 적지 않고 근거 파일에서 받는다** | 분석 결과가 바뀌면 값도 바뀐다. 코드에 박으면 자료와 화면이 어긋난다 |
| D7 | **인구로 나눈 값(1인당 소비 등)은 만들지 않는다** | 카드 이용자와 등록 인구는 세는 범위가 다르다 |
| D8 | **유사 지역은 공개·시연에서 합성 자료로만 보여 준다** | 고르지 않은 다른 지자체의 소비 구조를 보여 주는 기능이다. 실제 자료로는 내부 확인용으로만 켠다 |
| D9 | **사업 유형은 새로 쓰는 기획에서 필수.** 옛 입력은 "유형 미지정"으로 둔다 | 마음대로 분류하면 엉뚱한 질문이 뜬다 |
| D10 | **성과지표 입력에 선택지를 먼저 늘린다.** 자유 문장은 낱말로 단정하지 않고 되묻는다 | 지금은 "방문객 수"가 자유 문장으로만 들어와 엉뚱하게 걸린다 |

---

## 2. 용어표

화면·문서에는 **왼쪽 말**을 쓴다. 오른쪽은 코드·자료 안에서만 쓴다.

| 화면·문서에 쓰는 말 | 코드·자료 이름 | 뜻 |
|---|---|---|
| 지역 소비 프로필 | `profile` | 그 지역의 업종·연령·외국인 구성과 월별 흐름을 한 장으로 본 것 |
| 유사 지역 | `peers` | 소비 구성이 가까운 지역. **생활·산업·재정이 비슷하다는 뜻이 아니다** |
| 월별 특이 흐름 | `season_index` | 그 달의 결제가 그 지역 평소보다 얼마나 컸는지(전국 공통 흐름은 뺀 값) |
| 전국에서의 자리 | `national_rank` / 백분위 | 같은 지표를 가진 지역들 사이의 순위. **유효 지역 수를 함께 적는다** |
| 운영 기준 | `threshold` | 질문을 띄울 지점. 통계적 유의성 기준이 아니다 |
| 사업 유형 | `pack` | 켜질 질문 묶음을 고르는 값 |
| 질문 이름표 | `question_key` | 선택을 저장할 때 쓰는 내부 키. 화면에 나오지 않는다 |
| 준비 상태 | `readiness` | 그 기능을 켤 수 있는지 (3장) |
| 근거 파일 | evidence file | 데이터 담당이 만들어 주는 JSON |

**규칙 번호(R01~R12)는 화면·보완 기획안 어디에도 쓰지 않는다.** 이 문서·규칙 파일·시험에만 쓴다.

---

## 3. 근거 파일 2.1 계약

> **2026-09-21 상태:** 이 장은 서비스를 만들 때 정한 **내부 2.1 계약**이다. 이후 받은 분석 전달본도 `schema_version: "2.1"`이지만 프로필을 `records[]` 안에 두는 다른 계약이다. 전달본을 고치지 않고 서비스가 받아들이는 전환 계획은 10장에 적었다. 구현할 때 3장의 기존 계약은 하위 호환용으로 유지한다.

### 3-1. 파일 뼈대

```
{ "schema_version": "2.1",
  "dataset_version": "...", "data_kind": "synthetic" | "real",
  "generated_at": "...", "pipeline_version": "...",
  "records":  [ ... ],      ← 2.0과 완전히 같다. 한 글자도 바꾸지 않는다
  "profiles": [ ... ],      ← 새로 더하는 묶음
  "thresholds": { ... } }   ← 질문을 띄우는 운영 기준 (7장)
```

| 넣은 파일 | 기존 화면 | 새 화면 |
|---|---|---|
| 2.0 | 그대로 동작 | "자료 없음" |
| 2.1 | 그대로 동작 (`records`로) | 동작 (`profiles`로) |

**2.0 파서(`evidence/schema.py`)는 건드리지 않는다.** 2.1은 `evidence/profile_schema.py`가 따로 읽는다.
이유: 지금 파서는 모르는 필드를 **경고만 내고 무시**한다. 버전 문을 따로 두지 않으면 2.1 파일이 조용히 반만 읽힌다.

### 3-2. `profiles` 한 지역의 칸

| 칸 | 담는 것 | 함께 적어야 하는 것 |
|---|---|---|
| `region` | 서비스 10자리 코드, 자료 쪽 지역 키, 지역 수준, 기준 시점 | 연결 상태 (4장) |
| `region_basis` | `merchant`(가맹점 자리) / `cardholder`(이용자) / `unconfirmed` | 확인 출처. **답이 오기 전에는 `unconfirmed`** |
| `industry_share` | 업종 11개의 금액과 비중 | 분모 범위, **0원과 자료 없음을 구분** |
| `age_share` | 연령 6구간의 금액과 비중 | 미상 제외 여부, 미상 규모, 코드 1의 경계 확인 상태 |
| `industry_age` | 업종 안의 연령 구성 | 그 업종 안의 분모, 결측, 전국에서의 자리와 유효 지역 수 |
| `foreign_profile` | 외국인 결제의 업종 구성 | 표시 가능 여부, 산식 이름 |
| `season_index` | 월별 값 | 산식 이름, 대상 월, 계산 불가 사유 |
| `national_rank` | 지표별 백분위 | **유효 지역 수(분모)**, 동률 처리 방법 |
| `peers` | 이웃 지역 키·거리·순서 | 특성 목록, 표준화 기준, 모델 버전 |
| `external` | 인구, 등록외국인 | 관측 시점, 출처, 연결 상태, 월 결측 |
| `readiness` | 기능별 준비 상태 | 아래 3-3 |

#### `industry_age` 상세 형식 (묶음 3-4에서 확정)

```json
"industry_age": [
  {
    "industry_code": "IND01",
    "age_denominator": "known_only",
    "ages": [
      {
        "code": "AGE1", "label": "시연 연령 1",
        "amount": 0, "share_pct": 0.0, "status": "ok",
        "percentile": 0.0, "regions": 1
      }
    ]
  }
]
```

- `industry_code`는 같은 프로필의 `industry`에 있는 코드여야 하고 한 번만 나온다.
- `ages` 항목은 같은 프로필의 `age`에 있는 코드여야 한다. 모양은 `industry`·`age` 항목과 같고, `status: no_data`이면 금액·비중·순위를 비운다.
- `age_denominator`는 `known_only`(연령 미상 제외) 또는 `all`이다. 업종마다 따로 적어 어떤 분모의 구성비인지 잃지 않는다.
- `percentile`과 `regions`는 함께 적는다. R08은 둘 다 있는 업종×연령 조합만 "낮은 편" 운영 기준과 비교한다.
- 이 묶음이 잘못되면 그 지역의 **R08만 끄고** 프로필 카드와 다른 질문은 계속 사용한다. 일부 조합을 다른 지역 값으로 채우거나 낮다고 추정하지 않는다.

#### `external` 상세 형식 (묶음 4-1에서 확정)

```json
"external": {
  "population": {
    "status": "ok", "count": 1,
    "observed_at": "2026-06-30", "source_name": "시연용 합성 인구"
  },
  "registered_foreigners": {
    "status": "ok", "count": 0,
    "observed_at": "2026-06-30", "source_name": "시연용 합성 등록외국인"
  }
}
```

- 항목 상태는 `ok`·`no_data`·`unlinked`·`blocked` 중 하나다. `ok`이면 0 이상의 정수 `count`, 관측일, 출처 이름을 함께 적는다. 주민등록인구는 1명 이상이어야 한다.
- R11은 `population`만 사용한다. 등록외국인 수는 뒤의 대상 자료 보강을 위한 자리이며 이번 질문 조건에는 쓰지 않는다.
- 카드 자료 기간과 인구 관측일이 다를 수 있으므로 관측일을 잃지 않는다. **인구로 카드 결제값을 나눈 지표는 만들지 않는다.**
- 외부 자료 묶음이 틀리면 그 지역의 외부 자료 기능과 R11만 끈다. 프로필 카드와 카드 자료 질문은 계속 사용한다.

### 3-3. 준비 상태 값

| 값 | 화면 표시 | 뜻 |
|---|---|---|
| `ok` | (그냥 보여 준다) | 쓸 수 있다 |
| `insufficient` | "이 지역은 자료가 부족해 보여 드리지 않습니다" | 자료는 있으나 판단 기준에 못 미친다 |
| `unlinked` | "이 지역은 아직 연결되지 않았습니다" | 지역 대응표에 없다 |
| `blocked` | "이 자료는 쓸 수 없습니다" | 분석 쪽이 쓰지 말라고 표시했다 |

**새 규칙(R08·R10·R11·R12)의 사용 가능 여부는 여기에 적는다.** 2.0 레코드의 `applicability`는 R07·R06·R02만 받고 다른 값은 경고 내고 버린다(`evidence/schema.py:27`, `:434`).

### 3-4. 파일 검사

| 막을 것 | 어떻게 |
|---|---|
| 숫자가 아닌 값(`NaN`·무한대), 음수 금액·건수 | 파일 거부 |
| 같은 지역·같은 달이 두 번 | 파일 거부 |
| 외국인 + 미상 > 전체 | 파일 거부 |
| 분모가 0 (전체 0, 미상 제외 0, 연령 분모 0) | 거부하지 않고 **"계산 불가"로 남긴다** |
| 외부 슬롯 한 칸이 잘못됨 | **그 기능만 끈다.** 기존 R07·기본 검토까지 멈추지 않는다 |
| 오류 문구 | 지금처럼 **금액·비중 값을 넣지 않고** 형식만 적는다 |

### 3-5. 만든 것 (묶음 2-3, 2026-09-20)

| 무엇 | 어디 |
|---|---|
| 프로필·운영 기준 읽기와 검사 | `evidence/profile_schema.py` (새 파일) |
| 2.1 허용과 **버전 문 지키기** | `evidence/schema.py` — 2.0 파일에 프로필이 들어 있으면 **거부**한다 |
| 읽은 결과를 담아 넘김 | `evidence/loader.py` — `LoadResult.profiles`·`thresholds`·`profile(지역키)` |
| 경계 시험 파일 3개 | `tests/fixtures/evidence/profiles_ok · profiles_one_broken · profiles_version_mismatch` |

**고장 났을 때의 태도**

| 어디가 고장 | 어떻게 |
|---|---|
| `records` | 지금처럼 **파일 전체 거부** (2~5단계가 잘못된 수치를 보여 주면 안 된다) |
| 프로필 한 곳 | **그 지역만 버리고 경고**. 기존 검토는 그대로 돈다 |
| 2.0 파일에 프로필 | **거부.** 조용히 무시하면 담당자가 프로필을 넣었는데 화면엔 '자료 없음'만 뜬다 |
| 운영 기준 | 읽지 못한 값만 버리고 경고 |

업종 안의 연령 구성(`industry_age`)은 묶음 3-4에서, 외부 인구 자료(`external`)는 묶음 4-1에서 읽는다. 유사 지역(`peers`)은 쓸 업종 목록이 확정된 뒤 읽는다.

### 3-6. 화면에 붙임 (묶음 2-4, 2026-09-20)

| 무엇 | 어디 |
|---|---|
| 시연용 2.1 합성 파일 (지역 285곳의 프로필 + 운영 기준) | `scripts/build_demo_evidence.py` → `resources/evidence/review_evidence_demo_v1.json` |
| 2단계 **지역 소비 프로필 카드** | `web/profile_view.py`, `templates/partials/profile_card.html` |

카드에 담은 것: 업종 구성 11개 · 연령 구성 6개 · 월별 소비 흐름 · 외국인/미상 비중 · 전국에서의 자리(몇 곳 중인지 함께) · 자료의 한계.

**지키는 선**

- **고른 지역 자료만 쓴다.** 금액·비중 비교(R07)는 자료가 없으면 한 칸 넓히지만 프로필은 넓히지 않는다. 없으면 없다고 적는다.
- 준비됐다고 적히지 않은 묶음은 **이유 한 줄만** 보여 준다.
- 순위는 **자료가 있는 지역 수**를 반드시 함께 적는다.
- 시연 파일의 업종 이름은 **시연용 이름**(시연 업종 A~K)이다. 실제 업종 이름·코드는 데이터 담당이 2.1 파일에 넣는다. 연령 구간 이름은 제공 자료의 칸 정의를 쓰되 코드 1의 경계는 확인 전이다.
- 시연 파일의 유사 지역은 **미연결**로 둔다. 외부 인구는 묶음 4-1에서 합성 자료로 연결했다(3-8장).

### 3-7. 업종별 연령 구성과 R08에 붙임 (묶음 3-4, 2026-09-20)

| 무엇 | 어디 |
|---|---|
| `industry_age` 읽기와 기능 단위 차단 | `evidence/profile_schema.py` |
| 모든 시연 프로필의 업종 11개 × 연령 6개 합성 구성 | `scripts/build_demo_evidence.py` |
| 대상과 고객 구성 질문(R08) | `review/industry_checks.py`, `resources/rules/review_rules.json` |
| 쿠폰·지역화폐와 연령 대상 사업에서 질문 켜기 | `resources/rules/business_packs.json` |

R08은 사용처 업종과 대상 연령을 모두 골랐을 때만 확인한다. 고른 업종×연령 조합 가운데 순위와 유효 지역 수가 있는 항목을 근거 파일의 운영 기준과 비교한다. 낮은 조합이 없더라도 일부 조합을 확인하지 못했으면 "해당 없음"으로 넘기지 않고 보류한다.

### 3-8. 외부 인구와 R11에 붙임 (묶음 4-1, 2026-09-20)

| 무엇 | 어디 |
|---|---|
| 주민등록인구·등록외국인 수, 관측일, 출처 읽기 | `evidence/profile_schema.py` |
| 모든 시연 프로필의 합성 외부 인구 | `scripts/build_demo_evidence.py` |
| 규모와 수용 여건 질문(R11) | `review/capacity_checks.py`, `resources/rules/review_rules.json` |
| 축제·행사에서 질문 켜기 | `resources/rules/business_packs.json` |

R11은 목표 방문객 수가 주민등록인구와 같거나 클 때 수용 여건 확인 자료를 묻는다. 주민등록인구와 방문객은 같은 집단이 아니므로 규모를 가늠하는 참고라고 질문에 적고, 카드 결제값을 인구로 나누지 않는다. 외부 자료가 없거나 틀리면 R11만 보류하고 카드 자료 질문은 계속 사용한다. 유사 지역은 쓸 업종 목록이 확정되지 않아 계속 `unlinked`로 둔다.

---

## 4. 지역 대응표 서식

자료 쪽 지역 키를 서비스 10자리 코드로 잇는 표다. **묶음 1에서 채운다.** 여기서는 칸만 정한다.

| 칸 | 내용 |
|---|---|
| `region_code10` | 서비스 기준 키 (문자열, 앞자리 0 보존) |
| `region_name` | 화면에 쓰는 이름 |
| `source_key` | 자료 쪽 지역 키 (시도명 + 시군구명) |
| `source_code_current` | 자료 쪽 현재 5자리 코드 (여러 개면 목록) |
| `source_code_before` | 자료 쪽 통합 전 5자리 코드 (없으면 빈칸) |
| `code_basis_date` | 그 코드가 언제 기준인지 |
| `compose` | 합치거나 나눈 경우의 구성 목록 |
| `link_status` | `linked` / `composed` / `unlinked` / `excluded` |
| `note` | 사람이 읽는 사유 |

### 4-1. 이미 확인한 대응 결과 (개수만)

작업 폴더에서 자료 쪽 지역표(255줄)와 서비스 지역 목록(269개)을 대조한 결과다. **행정구역 이름·코드를 센 개수이며 소비 수치가 아니다.**

| 잇는 방법 | 맞은 개수 |
|---|---|
| 이름(시도명 + 시군구명) | 254 / 255 |
| 현재 5자리 코드만 | 224 / 255 |
| 통합 전 5자리 코드만 | 250 / 255 |
| **두 코드 칸을 함께** | **254 / 255** |

### 4-2. 따로 다뤄야 하는 자리

| 자리 | 개수 | `link_status` | 처리 |
|---|---|---|---|
| 세종특별자치시 | 1 | `linked` | 서비스는 "시군구 없는 시도", 자료는 시군구 한 줄. 프로필은 **시도 범위**로 표시 |
| 전남·광주 코드 변경 | 27 | `linked` | 통합 전 코드로 잇고 `code_basis_date`를 적는다 |
| 인천 개편(합산·분할) | 3 | `composed` | 한 줄에 코드가 둘이다. **하나만 잘라 쓰지 않는다.** `compose`에 구성 목록 |
| 화성시 새 일반구 | 4 | `linked` | 통합 전 코드가 없다. 현재 코드로만 |
| 구를 둔 시 13곳 | 13 | `unlinked` | 자료에 그 줄이 없다. **구 값을 합치지 않는다.** "구를 골라 주세요"로 안내 |
| 화성시 출장소 2곳 | 2 | `unlinked` | 프로필 미연결 |

**자료 쪽 표에 "대조 완료"라고 적힌 칸은 표를 만든 쪽이 스스로 적은 상태값이다.** 행정 사실 확인으로 치지 않는다.

### 4-3. 만든 결과 (묶음 1, 2026-09-20)

`scripts/build_region_mapping.py`로 만들었다. 결과는 **`private/region_mapping.json`**에 둔다 — 어느 지역이 제공 자료에 들어 있는지가 드러나므로 공개 저장소에 올리지 않는다.

| 상태 | 개수 | 뜻 |
|---|---|---|
| `linked` | 252 | 자료 한 줄이 서비스 지역 한 곳에 그대로 붙음 |
| `composed` | 3 | 여러 코드를 합치거나 나눠 붙음(인천 개편). 구성 목록을 함께 본다 |
| `unlinked` | 15 | 서비스 지역인데 자료에 그 줄이 없음(구를 둔 시 13, 출장소 2) |
| `source_only` | 0 | 자료에만 있고 서비스 목록에 없는 줄 — **없다** |

- 자료 255줄이 **모두** 붙었다(252 + 3). 서비스 지역 270곳 중 255곳이 연결됐다.
- **합계가 두 번 더해질 위험: 없음.** 구를 둔 시 13곳이 모두 `unlinked`라서, 시와 그 구가 동시에 연결된 곳이 하나도 없다.
- 통합 전 코드가 없는 줄 4개(화성시 새 일반구)는 **현재 코드로만** 이었다.
- 기준 시점은 자료 쪽 표의 문구에서 꺼내 255줄 모두 채웠다.

**아직 남은 것:** 데이터 담당이 지역표에 10자리 코드 칸을 넣어 주면, 이 대응표와 대조해 서로 맞는지 확인한다(8장 3번). 지금 대응표는 **이름과 두 코드 칸으로 이은 것**이며 행정 사실을 따로 검증한 것이 아니다.

---

## 5. 질문 이름표와 재확인 기준

### 5-1. 지금

| | 지금 | 어디 |
|---|---|---|
| 질문 이름표 | `question_key = 규칙 ID` | `review/basic_checks.py:26`, `review/r07_indicator.py:35`·`:107` |
| 재확인 기준 | 선택의 **규칙 번호**로 관련 입력 항목을 찾음 | `choices/recheck.py:16` |

### 5-2. 바꿀 것

| 규칙 | 질문 이름표 | 관련 입력 항목 |
|---|---|---|
| 한 규칙 = 질문 하나 | 지금 그대로 규칙 ID (`R07`, `R01`, …) | 규칙의 값 |
| R04 기간 | `R04-period` | 기간, 지표 용도 |
| R04 시기 | `R04-season` | 기간, 지역 |

- **옛 이름표를 옮기는 작업은 필요 없다.** 선택은 서버 메모리에만 있고 다시 시작하면 사라진다.
- 검토 결과에 없는 이름표의 선택은 지금처럼 **보관함으로 옮긴다**(조용히 사라지지 않게).

### 5-3. 만든 것 (묶음 2-1, 2026-09-20)

| 무엇 | 어디 |
|---|---|
| 이름표 만드는 함수 `question_key_of(규칙ID, 질문이름)` | `review/outcome.py` |
| 질문별 관련 항목 `RuleInfo.related_fields_for(질문이름)`과 `QuestionSpec` | `review/rules.py` |
| 규칙 파일의 선택 칸 `questions: {이름: {related_fields: [...]}}` | `resources/rules/review_rules.json` (아직 쓰는 규칙 없음) |
| 선택이 관련 항목을 기억 `Choice.related_fields` | `choices/models.py`·`selection.py` |
| 재확인이 그 값을 먼저 봄 | `choices/recheck.py` |

지금 규칙은 모두 질문 하나씩이라 **화면 동작은 달라지지 않았다**(시험 503개 통과). R04를 기간·시기로 나누는 것은 묶음 3에서 한다.

---

## 5-A. 1단계 입력 확장 (묶음 2-2, 2026-09-20)

| 입력 | 어떻게 받나 | 비어 있을 때 |
|---|---|---|
| 사업 유형 | 넷 중 하나, **필수** | 고르지 않으면 묻는다. 서비스가 임의로 한 유형으로 분류하지 않는다 (D9) |
| 성과지표 | 묶음 셋으로 나눠 보여 준다 — 카드 자료로 산출할 수 있는 지표 5개 / 카드 자료에 없어 별도 자료가 필요한 지표 3개 / 그 밖 2개 | 하나 이상 필수(전과 같다) |
| 목표 방문객 수 | 선택. 숫자로 읽지 못하면 원문을 남기고 묻는다 | **빈칸(적지 않음)과 0명을 구분한다.** 축제·행사에서 빈칸이면 "추가 확정 필요"로 남긴다 |

- 받아 두기만 하고 **아직 질문을 켜거나 끄는 데 쓰지 않는다.** 사업 유형으로 질문을 고르는 것은 묶음 3, 카드로 못 재는 지표를 묻는 것(R10)은 묶음 2 뒤쪽이다.
- 지표 묶음 이름은 **안내일 뿐**이며, 고른 지표를 서비스가 판정하지 않는다.
- **사용처 업종·대상 연령은 묶음 2-5에서 넣었다** (아래 5-B장). 고를 수 있는 목록은 근거 파일이 알려 준다.

---

## 5-B. 업종·연령 입력과 지표 점검 (묶음 2-5, 2026-09-20)

| 무엇 | 어떻게 |
|---|---|
| 사용처 업종·대상 연령 입력 | 고를 수 있는 목록을 **근거 파일의 전국 프로필에서 읽어** 보여 준다. 목록에 없는 코드는 버린다. 근거 파일에 프로필이 없으면 **입력칸 자체를 두지 않는다** |
| 성과지표 점검 (R10) | 고른 지표에 사람 수(방문객·참여자·고객)가 있으면 어떤 자료로 확인할지 묻는다. 대안 A는 수집 계획을 받고, B는 결제금액·건수로 바꾼다 |
| 공유 입력 묶음 | R10은 `performance_data`(성과 확인 자료). 쿠폰 정산 실적(`participation_data`)과 **다른 자료라 묶음을 나눴다** |

**확인한 것:** 축제 시나리오(사흘 행사 + 방문객 수 지표)가 입력 → 프로필 → 질문 → 선택 → 보완 기획안까지 끊기지 않고 통과한다.

**묶음 2에서 남겨 둔 것과 현재 상태**

- 자유 문장으로 적은 지표(기타)는 낱말만 보고 단정하지 않는다. 되묻는 방법은 따로 정한다.
- 고른 업종·연령을 질문 조건으로 쓰는 R02·R08은 **묶음 3-2·3-4에서 완료했다.** 둘 다 고른 지역 자료만 쓰며 기준값은 근거 파일에서 받는다.
- **보완 기획안 Markdown의 변경 연결은 공개 순번으로 바꿨다**(예: `〔추가 · 변경 001〕`). 내부 규칙 번호는 `Change.rule_id`에만 남고, 본문과 별첨 1은 같은 공개 순번으로 연결한다 (묶음 5, 9/21).

---

## 6. 새 질문과 대안이 들어갈 자리 ★묶음 0 통과 기준

보완 기획안 장: 1 사업 개요 · 2 목적·대상 · 3 일정 · 4 혜택·사용처 · 5 모집·정산 · 6 예산 상태 · 7 성과 측정계획 · 8 추가 확인사항
별첨: 1 변경 전후 · 2 근거와 해석 한계 · 3 요청서 초안

**대안에는 문서 문장이 반드시 있어야 한다**(없으면 규칙 파일 읽기 자체가 실패한다, `review/rules.py:101`). 그래서 만들기 전에 자리를 정해 둔다.

| 질문 | 대안 | 들어갈 장 | 방식 | 실행 입력 |
|---|---|---|---|---|
| **사용처 업종 확인** (R02) | A 다른 후보 업종도 검토 | 4 혜택·사용처 | 덧붙임 | — |
| | B 사용처 유지하고 이유 적기 | 4 혜택·사용처 | 덧붙임 | — |
| | C 참여 상점 현황 따로 확인 | 8 추가 확인사항 | 덧붙임 | 자료·담당자·주기 |
| **대상과 고객 구성** (R08) | A 대상 연령이 자주 쓰는 업종도 검토 | 4 혜택·사용처 | 덧붙임 | — |
| | B 사용처 유지하고 이유 적기 | 4 혜택·사용처 | 덧붙임 | — |
| | C 참여자 연령 확인 자료 추가 | 7 성과 측정계획 | 덧붙임 | 자료·담당자·주기 |
| **성과지표 점검** (R10) | A 별도 실적 자료를 지정 | 7 성과 측정계획 | 덧붙임 | 자료·담당자·주기 |
| | B 지표를 결제금액·건수로 바꾸고 해석 조건 적기 | 7 성과 측정계획 | 교체 | — |
| **기간 확인** (R04-period) | 기존 대안 유지 | 기존 그대로 | 기존 그대로 | 기존 그대로 |
| **시기 확인** (R04-season) | A 비교할 기준 달을 함께 적기 | 3 일정 | 덧붙임 | — |
| | B 카드 지표는 참고로 두고 확인 주기 적기 | 7 성과 측정계획 | 덧붙임 | 주기 |
| **규모와 수용 여건** (R11) | A 수용 여건 자료(숙박·주차 등) 추가 | 8 추가 확인사항 | 덧붙임 | 자료·담당자·주기 |
| | B 목표를 단계별로 나눠 적기 | 7 성과 측정계획 | 덧붙임 | — |
| | C 인근 지역과 연계 검토 | 8 추가 확인사항 | 덧붙임 | — |
| **외국인 대상 기반** (R12) | A 외국인 방문 근거 자료 추가 | 8 추가 확인사항 | 덧붙임 | 자료·담당자·주기 |
| | B 대상을 국내 방문객으로 조정 | 2 목적·대상 | 교체 | — |
| | C 작게 시범으로 시작 | 7 성과 측정계획 | 덧붙임 | — |

**질문이 아닌 것**

| 내용 | 자리 |
|---|---|
| 지역 소비 프로필 요약 문장 | 별첨 2 (항상 들어간다. 담당자가 고르는 것이 아니다) |
| 유사 지역과 그 범위 | 별첨 2 |
| 못 재는 지표로 남은 항목 | 8 추가 확인사항 (`[추가 확정 필요]`) |

문구 자체는 **데이터 담당 검수 뒤** 규칙 파일에 넣는다. 판정 단어(문제·오류·위험·실패·성공·잘못)는 검사에서 걸린다.

---

## 7. 기준값을 두는 곳

```
"thresholds": { "version": "...", "population": "...",
                "R02": {...}, "R08": {...}, "R04_season": {...}, "R12": {...} }
```

- 값은 **근거 파일에서 받는다.** 코드에 적지 않는다(D6).
- 화면에는 **"운영 기준"**이라고 적는다. "통계적으로 유의하다"는 뜻이 아니다.
- 기준값 버전이 바뀌면 원안이 같아도 **관련 선택을 다시 확인**하게 한다.

---

## 8. 아직 비어 있는 칸

| # | 정해야 할 것 | 누가 | 정해지기 전 |
|---|---|---|---|
| 1 | 자료의 시군구가 가맹점 자리 기준인지 | 주최 측 → 데이터 담당 | `region_basis`를 `unconfirmed`로 두고, "그 지역 가맹점에서 결제된 것"처럼 단정하지 않는다 |
| 2 | 연령 코드 1의 경계 | 주최 측 | 주민등록 연령대와 맞대어 놓지 않는다 |
| 3 | 대응표에 10자리 코드 칸을 넣어 줄 수 있는지 | 데이터 담당 | 서비스가 추측해 잇지 않는다. `unlinked`로 둔다 |
| 4 | 유사 지역에 쓸 업종 목록 | 데이터 담당 | 이웃 기능을 켜지 않는다 |
| 5 | 기준값과 외국인 유형 이름 | 데이터 담당 + 검수 | 해당 질문을 켜지 않는다 |
| 6 | 2.1 파일 전달 시점 | 데이터 담당 | 합성으로만 만든다 |
| 7 | 새 질문 7개의 문장 검수 | 데이터 담당 | 화면에 내보내지 않는다 |

---

## 9. 진행 상황

| 묶음 | 내용 | 상태 |
|---|---|---|
| 0 | 계약 정리 (이 문서) | **완료 2026-09-20** |
| 1 | 지역 대응표 확정 | **완료 2026-09-20** (4-3장) |
| 2 | 질문 이름표 분리 + 입력 확장 + 2.1 로더 + 프로필 + 성과지표 점검 | **완료 2026-09-20** |
| 3 | 사업 유형 팩 + 값 기반 질문 | **완료 2026-09-20** — R02·R04 시기·R08·R11·R12 |
| 4 | 유사 지역 + 외부 자료 | **진행 중** — 외부 인구·R11 완료, 유사 지역은 업종 목록 확정 대기 |
| 5 | 선택·문서·보안 통합 | 시작 전 |
| 6 | 통합·화면 확인 | 시작 전 |

묶음 1에서는 대응표 생성 스크립트와 그 시험만 더했다. 서비스가 도는 코드는 아직 바꾸지 않았다.

**묶음 0에서 코드는 바꾸지 않았다.** 시작 시점 상태를 커밋으로 고정했고(`0-0`), 시험 481개 통과·공개 저장소 검사 위반 없음을 확인했다.

---

## 10. 받은 2.1 전달본에 맞추는 서비스 코드 변경 계획 (2026-09-21)

> **구현 완료 (2026-09-21):** 7-1~7-7을 반영했다. 공개 전달본 13개와 비공개 전달본 256개를 같은 어댑터로 읽고, 비공개 지역 255개를 검증된 대응표로 연결했다. 전체 자동 시험 679개와 공개 묶음 검사를 통과했으며 실제 JSON은 저장소에 복사하지 않았다.

### 10-1. 목표와 원칙

대상은 `개발자전달_공개용 (1)/review_evidence_demo_v2.1.json`과 `내부보관_비공개 (1)/review_evidence_real_v2.1.json`이다. 두 파일을 **분석 전달 계약의 기준본**으로 삼는다.

서비스 안의 기존 `profiles[]` 계약을 삭제하지 않는다. 로더 앞에 전달본 어댑터를 두어 새 구조를 현재 내부 모델로 바꾼다. 이 방식으로 다음을 함께 지킨다.

1. 기존 2.0 파일과 현재 서비스 합성본은 그대로 읽는다.
2. 전달본의 값·상태·결측을 추정해서 채우지 않는다.
3. 실제 파일은 저장소에 복사하지 않고 `PSM_EVIDENCE_PATH`로만 읽는다.
4. 지역 자료가 없으면 다른 지역 값으로 채우지 않는다.
5. 오류·경고에는 실제 금액·비중을 넣지 않는다.
6. `local_food_project_analysis_20260919_v4`는 축제·관광·상가 자료로 분리해 유지하며 이번 변환에 섞지 않는다.

### 10-2. 바꿀 구조

```text
받은 JSON
  └─ evidence/delivery_v21.py       전달본 판별·검사·정규화
       ├─ records 기본 필드 ───────→ evidence/schema.py
       ├─ records[].profile 등 ────→ evidence/profile_schema.py의 내부 모델
       ├─ 평면 thresholds ─────────→ 내부 Thresholds
       └─ 지역 이름·5자리 코드 ───→ private/region_mapping.json의 10자리 서비스 코드
                                         ↓
                              기존 review / web / document
```

`loader.py`는 파일을 읽은 뒤 다음 순서로 처리한다.

1. `schema_version`, 최상위 키, 레코드 모양으로 **기존 내부 형식**과 **받은 전달 형식**을 구분한다.
2. 기존 형식이면 현재 파서를 그대로 호출한다.
3. 전달 형식이면 새 어댑터가 내부 `EvidenceFile`, `RegionProfile`, `Thresholds`로 바꾼다.
4. 변환 뒤에는 기존 계산·질문·화면 계층만 사용한다. 전달 형식 분기문을 규칙과 화면에 흩뿌리지 않는다.

### 10-3. 필드 대응

| 전달본 | 서비스 내부 | 처리 |
|---|---|---|
| `records[].months` | `EvidenceRecord.months` | F·T·U·거래건수와 계산 상태는 기존 검사를 그대로 통과시킨다. `season_index`는 프로필 월 자료에도 보존한다 |
| 전국 레코드의 R02 없음 | `EvidenceRecord.applicability` | 전달 형식에서만 R07·R06을 필수로 하고 R02는 선택으로 읽는다. 기존 2.0 계약의 R02 필수 규칙은 유지한다 |
| `industry_codes`, `profile.industry_share_pct` | `RegionProfile.industry` | 코드·이름·비중·전국 백분위·유효 지역 수를 묶는다. 정확한 업종 금액이 없으므로 0원 표시 대상만 `derived_flags.no_payment_industries`로 확인하고 나머지 금액은 `None`으로 둔다 |
| `age_labels`, `profile.age_share_pct` | `RegionProfile.age` | 연령 코드·이름·비중·백분위·유효 지역 수를 묶고 분모는 `known_only`로 기록한다 |
| `profile.industry_age_share_pct` | `RegionProfile.industry_age` | 업종×연령 비중, `national_rank`의 백분위, `national_distribution`의 유효 지역 수를 결합한다 |
| `foreign_profile` | 프로필 외국인 참고 정보 | 외국인 비중과 유형을 보존한다. R03의 기존 관광객·외국인 범위 질문을 대체하지 않고 참고 정보로만 붙인다 |
| `months[].season_index`, `season_summary.threshold` | 프로필 월 자료·지역별 시기 기준 | 지역별 `season_summary.threshold`를 우선 사용한다. 현재의 소액/일반 2단계만으로 다시 계산하지 않는다 |
| `size_flag.is_small` | `small_region` | 그대로 옮긴다. 매우 소액 지역의 별도 시기 기준은 위 지역별 threshold로 보존한다 |
| `external.population_*` | `ExternalData.population` | 수·관측일·최상위 출처를 함께 옮겨 R11에 사용한다 |
| `external.registered_foreign_by_month` | 등록외국인 월 자료 | 월별 결측을 보존하는 내부 형식을 추가한다. 임의로 연평균이나 특정 날짜 값으로 바꾸지 않는다 |
| `external`의 1인당 계산값 2개 | 사용하지 않음 | 현재 제품 결정대로 화면·질문·문서에 연결하지 않는다 |
| `national_rank`·`national_distribution` | `Rank(percentile, regions)` | 백분위와 유효 지역 수가 둘 다 있을 때만 순위로 쓴다 |
| 평면 `thresholds` | 내부 `Thresholds` | 아래 표대로 명시 변환하고 원문 임계값 묶음의 안정적 지문을 기준 버전에 포함한다 |
| `display_policy.R08.level=reference` | R08 표시 수준 | R08 결과에 참고 배지를 붙이고 확정 판정처럼 표시하지 않는다 |
| `peers[]` | 유사 지역 후보 | 지역 코드 변환 뒤 보존한다. 산식 버전·특성 목록을 서비스 메타데이터로 고정하고 대조 시험을 통과한 뒤 화면을 켠다 |
| `limitations`·`external_sources` | 카드·문서의 한계·출처 | 고른 지역 자료와 함께 표시하되 AI 요청문에는 넣지 않는다 |

임계값 대응은 다음과 같이 고정한다.

| 전달본 키 | 내부 규칙·키 |
|---|---|
| `R02_R08_low_percentile` | `R02.low_share_percentile`, `R08.low_share_percentile` |
| `R04_season_index_min` | `R04.season_index`의 기본값 |
| `R04_season_index_min_small` | 이전 형식 호환용 `R04.season_index_small_region` |
| `season_summary.threshold` | 실제 지역에 적용할 R04 최종 기준 |
| `R12_low_foreign_share_percentile` | `R12.low_foreign_percentile` |
| `R11_visitor_to_resident_ratio` | `R11.visitor_to_resident_ratio` |

R11은 현재 코드에 박혀 있는 1:1 비교를 없애고 근거 파일의 비율을 사용한다. 기준이 없으면 질문을 만들지 않고 보류 사유를 남긴다.

### 10-4. 지역 연결

기존 `private/region_mapping.json`을 전달본 어댑터의 유일한 지역 연결 근거로 사용한다. 이름이나 코드 앞부분을 실행 중에 추측해 붙이지 않는다.

현재 대응표 검토 결과는 다음과 같다.

| 상태 | 수 | 처리 |
|---|---:|---|
| `linked` | 252 | 전달본 레코드를 10자리 서비스 코드로 연결 |
| `composed` | 3 | 대응표의 합산 설명을 한계 문구에 함께 표시하고 연결 |
| `unlinked` | 15 | 프로필·값 기반 질문을 `자료 없음`으로 유지 |
| `source_only` | 0 | 별도 처리 없음 |

전달본에는 시도 레코드가 없다. 시군구 비중을 임의 합산해 시도 프로필을 만들지 않는다. 시도 선택 시 지역 프로필은 `자료 없음`으로 두고, R07은 기존 사다리 원칙에 따라 전국 참고 자료를 사용할 수 있다. 시도 프로필이 필요하면 데이터 담당이 같은 계약으로 직접 제공해야 한다.

설정에는 `PSM_REGION_MAPPING_PATH`를 추가한다. 전달 형식인데 대응표가 없거나 형식이 틀리면 전국 레코드는 읽되 시군구 프로필은 `unlinked`로 두고 명확한 경고를 남긴다.

### 10-5. 코드 변경 묶음

| 묶음 | 변경 파일 | 내용 | 완료 조건 |
|---|---|---|---|
| 7-1 계약 고정 | `7지역확장계획.md`, `tests/fixtures/evidence/` | 공개 합성 자료에서 최소 구조만 옮긴 전달 형식 픽스처와 필드 대응 시험 작성 | 기존 형식·전달 형식 판별 시험 통과 |
| 7-2 전달 어댑터 | 새 `evidence/delivery_v21.py`, `evidence/loader.py`, `evidence/schema.py` | 전달 JSON 검증, 기본 레코드 정규화, 전국 R02 선택 처리 | 공개 합성본 13개 레코드가 오류 없이 로드되고 기존 2.0 오류 시험이 그대로 통과 |
| 7-3 지역 연결 | `config.py`, `web/evidence_state.py`, `scripts/build_region_mapping.py` | 대응표 경로 설정, 이름→10자리 코드 연결, composed·unlinked 상태 전달 | 252 linked·3 composed·15 unlinked가 바뀌지 않고 합계 중복 위험 0 |
| 7-4 프로필 변환 | `evidence/profile_schema.py`, 어댑터, `web/profile_view.py` | 업종·연령·업종×연령·계절·순위·외부 자료 변환 | 합성 A~L에서 자료 있음·없음·0원·결측이 전달 문서와 일치 |
| 7-5 기준과 질문 | `review/industry_checks.py`, `season_checks.py`, `capacity_checks.py`, `engine.py` | 임계값 매핑, 지역별 R04 기준, R11 비율, R08 참고 수준 반영 | R02·R04·R08·R11·R12 경계 시험 통과, 기준 누락 시 안전하게 보류 |
| 7-6 유사 지역 | 프로필 모델, 별도 `review/peer_checks.py` 또는 화면 조립, 별첨 2 | 전달된 5개 이웃·거리·산식 버전 보존, 미연결 이웃 제외 | 공개 합성본의 기대 이웃 대조와 지역 매핑 완전성 시험 통과 뒤 기능 활성화 |
| 7-7 보안·통합 | `check_public_bundle.py`, 전체 흐름 시험, 문서 | `.pkl` 등 중간 실데이터 차단, 실제 파일 로컬 검사, 화면·문서 회귀 확인 | 전체 pytest, 공개 묶음 검사, 실제 파일 구조 검사, 사람 표본 대조 통과 |

묶음은 위 순서대로 진행한다. 7-2가 끝나기 전에는 실제 파일 경로를 서비스 기본값으로 바꾸지 않는다. 7-5까지 통과하면 핵심 지역 프로필과 값 기반 질문을 사용할 수 있고, 유사 지역은 7-6을 별도 통과한 뒤 켠다.

### 10-6. 시험 계획

자동 시험은 실제 수치를 저장소에 넣지 않고 합성 픽스처로 작성한다.

1. **하위 호환:** 기존 2.0·내부 2.1·기본 합성본의 결과가 바뀌지 않는다.
2. **전달 형식:** 공개 합성본의 최상위 구조, 12개 시나리오+전국, 레코드별 필수 필드를 읽는다.
3. **값 불변식:** F+U≤T, 상태별 null, 월 순서, 비중·순위 범위, 유효 지역 수 동반을 검사한다.
4. **부분 고장:** 외부 자료·업종×연령·이웃 한 부분이 틀리면 그 기능만 끄고 R07과 다른 프로필은 유지한다.
5. **지역:** linked·composed·unlinked, 대응표 없음, 중복·source_only, 합계 중복 위험을 검사한다.
6. **규칙:** R02·R08 백분위 경계, R04 일반/소액/매우 소액, R11 비율 경계, R12 경계를 검사한다.
7. **표시:** R08 참고 배지, 자료 없음, 지역 범위·관측일·출처·버전 표시를 확인한다.
8. **보안:** 실제 파일·CSV·PKL이 git 추적 대상에 들어오면 실패하고, 실제 자료+클라우드 AI 조합은 계속 차단한다.
9. **통합:** 쿠폰·축제 입력부터 Markdown까지 기존 흐름을 다시 확인한다.

실제 파일 검사는 로컬에서만 한다. 출력은 스키마 버전, 레코드·프로필 개수, 경고 종류와 개수만 허용한다. 마지막으로 데이터 담당 가이드대로 사람이 시군구 표본의 화면 값과 JSON을 대조한다.

### 10-7. 완료 기준

- 새 공개 합성본과 비공개 실데이터본이 같은 어댑터를 거쳐 로드된다.
- 비공개 실데이터는 255개 시군구+전국으로 읽히고, 대응표의 252 linked·3 composed·15 unlinked 상태가 유지된다.
- R07과 지역 프로필, R02·R04·R08·R11·R12가 전달본의 상태·임계값을 사용한다.
- R03 유형은 참고 정보로만 보존되고 기존 관광객 범위 질문을 바꾸지 않는다.
- 시도 자료와 미연결 지역을 다른 값으로 채우지 않는다.
- 1인당 계산값은 화면·질문·문서·AI에 들어가지 않는다.
- 기존 시험 전체와 새 시험, 공개 묶음 검사, 실제 파일 구조 검사, 사람 표본 대조가 모두 통과한다.
- 실제 JSON·지역 대응표·원본·중간 산출물은 git 변경분에 없다.

### 10-8. 이번 변경에서 하지 않는 것

- 전달본 JSON 자체 수정
- BC 원본 재집계 또는 임계값 재산정
- 시군구를 합쳐 시도 프로필 생성
- v4의 축제·관광·상가 자료를 2.1 프로필에 혼합
- 1인당 결제액 사용
- 실제 자료를 클라우드 AI에 전달
