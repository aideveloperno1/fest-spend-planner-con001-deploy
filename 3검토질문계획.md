# 3. 검토 질문 — 상세 구현 계획

문서 기준일: 2026-09-16
상위 문서: [작업진행.md](./작업진행.md) 3장
선행: [1근거계산계층계획.md](./1근거계산계층계획.md), [2근거확인화면계획.md](./2근거확인화면계획.md) (완료)
기준: 워크플로우 11장(검토 규칙과 문서 반영), S02, 12장 · 최종기획서 4-3·4-4, 5장 · [review/README.md](src/policy_signal_map/review/README.md)

---

## 1. 목표와 범위

기획 입력과 근거를 조합해 **확인할 점을 질문으로** 만든다. 판정·점수를 내지 않으며, 결정은 4단계에서 담당자가 한다.

포함: 규칙 설명 문서 초안, 규칙 원본 JSON v1.0, 규칙 실행 코드(R07 + 기본 검토 4개), 3단계 화면
제외: 선택 저장(4번), 문서 반영(5번), AI 참고 의견(C 단계), R02 업종 후보(향후), R06 화면(분석 예시로만 문구 제공)

---

## 2. 확정한 결정

| # | 결정 | 채택안 | 이유 |
|---|---|---|---|
| ① | 문구 관리 | 질문·안내·대안 문구는 **JSON이 원본**, 조건 판단은 규칙 ID별 **파이썬 함수**. 문구의 자리표시자는 `{opposite_count}`처럼 이름 있는 형식으로 두고 코드가 채움 | checks.md "규칙 JSON이 원본" 결정 유지, 조건은 코드로 테스트 |
| ② | 검토 결과 저장 | 세션에 저장하지 않고 **요청마다 다시 계산**한다. 순수 함수라 결과가 같고, 근거 파일·규칙을 바꾸면 바로 반영된다 | 세션에 계산 결과를 쌓지 않아 4·5단계 재확인 처리가 단순해짐 |
| ③ | 질문 식별자 | `question_key` = **규칙 ID**. 4단계 선택이 이 키를 참조한다. `merge_group`은 별도 필드로 두고 추가 입력 공유에만 쓴다 | 처음에는 `merge_group`을 키로 삼았는데, 결정 ④에서 카드를 나누기로 하면서 R07·R03·R04의 키가 모두 같아져 **4단계 선택이 서로 덮어쓰는 문제**가 생겼다 (9/16 실행으로 확인해 수정) |
| ④ | 중복 질문 묶기 | 3단계에서는 카드를 합치지 않고 같은 `merge_group` 질문끼리 **"함께 확인: R03"**처럼 서로를 가리킨다. 실제 묶기는 **4단계의 추가 입력**(수집자료·담당자·주기를 한 번만 입력)에서 한다. 규칙 ID·근거 ID는 모두 보존 (워크플로우 11장) | 세 규칙은 묻는 내용이 서로 달라(지표 용도 / 대상 범위 / 집계 주기) 한 문장으로 합치면 뜻이 흐려진다. 중복으로 느껴지는 부분은 입력이므로 그쪽을 묶는다 |
| ⑤ | 화면 구성 | 왼쪽: 질문·안내·추가 확정 필요·보류 카드 / 오른쪽: **확인했으나 해당 없음**(조건을 확인했고 물을 것이 없는 규칙)과 **검토하지 않음**(R06 분석 예시, R02 향후)을 **나눠서** 표시 | 시안 S3. 워크플로우: '문제없음'과 '검토하지 않음'을 구분해야 하므로 목록을 둘로 나눈다 |
| ⑥ | 지역 표시 | R07 카드에 2단계와 같은 범위 칩과 지역 안내를 붙인다. 시도 연결 규칙(C-2) 전까지는 전국 참고만 | 워크플로우 8-4, 12장 |
| ⑦ | 원안 변경 안내 | 원안을 바꿔 다시 제출하면 3단계 상단에 "원안이 바뀌어 검토를 다시 실행했습니다" 안내. **이번 제출에서 바뀌었는지로 매번 다시 정한다**(한 번 켜지면 계속 남지 않게) | 현재 임시 화면의 재확인 표시를 대체. 4번에서 `plan/changes.py`의 항목별 재확인으로 교체 |
| ⑧ | 대안 미리보기 | 3단계 카드에 대안 A~D를 **보여주지 않는다**. 대안은 4단계에서만 | 3단계는 "확인할 점", 4단계는 "고르기"로 역할을 나눈다 |

---

## 3. 규칙 원본 JSON v1.0

`resources/rules/review_rules.json` — 기존 필드(`id`, `title`, `scope`, `summary`)에 추가한다.

```json
{
  "version": "1.0",
  "rules": [
    {
      "id": "R07",
      "title": "금액·비중과 성과지표 확인",
      "scope": "implement",
      "summary": "오른쪽 패널 한 줄 설명",
      "merge_group": "participation_data",
      "related_fields": ["goals", "metrics", "indicator_use", "region", "target"],
      "messages": {
        "question_direct": "…",
        "question_unknown": "…",
        "notice_reference": "…",
        "goal_mismatch_prefix": "…",
        "why_opposite": "1~6월 비교 가능한 {comparable_count}개 구간 중 {opposite_count}개 구간에서 …",
        "why_same": "…",
        "held_applicability": "…",
        "held_no_pairs": "…"
      },
      "options": [
        {
          "id": "A",
          "title": "참여 실적 추가",
          "where": "7. 성과 측정계획",
          "need": "쿠폰 발급·사용·정산 자료",
          "load": "중",
          "execution_fields": ["collect_items", "availability", "owner", "cycle"]
        },
        { "id": "B", "title": "해석 조건 명시", "where": "7. 성과 측정계획", "need": "없음", "load": "낮음" },
        { "id": "C", "title": "자료 확보 전", "where": "8. 추가 확인사항", "need": "협의 대상", "load": "낮음" },
        { "id": "D", "title": "정밀 분석 요청서 초안", "where": "별첨 3", "need": "BC 협의", "load": "비용 확인 필요" }
      ]
    }
  ]
}
```

- `scope`가 `implement`·`basic`인 규칙은 코드 함수가 반드시 있어야 한다 (시작 시 검사)
- 문구에 "문제·오류·위험·실패·성공·잘못" 단어를 쓰지 않는다 (테스트로 확인)
- 옵션 D 문구에 계약·자료 제공이 확정된 것처럼 읽히는 표현을 쓰지 않는다

---

## 4. 규칙별 조건과 문구

### R07 금액·비중과 성과지표 (검토 구현)

실행 판단 순서

| 순서 | 조건 | 결과 |
|---|---|---|
| 1 | 성과지표에 외국인 결제 비중·금액이 없음 | 결과 없음 (규칙 대상 아님) |
| 2 | 전국 레코드 없음 | `held` — 근거 없음 |
| 3 | `applicability.R07 = blocked` | `held` — 사유 표시, 수치 언급 없음 |
| 4 | `applicability.R07 = needs_review` | `held` — 확인할 내용 표시 |
| 5 | 비교 가능 구간 0개 | `held` — 자료 부족 |
| 6 | 지표 용도 `reference` | `notice` — 해석 조건만 안내 |
| 7 | 지표 용도 `direct` | `question` |
| 8 | 지표 용도 `unknown` | `question` (결론 없이 선택지만) |

- 목표와 지표가 어긋나면(금액 확대 목표 + 비중 지표 등) 질문 앞에 불일치를 먼저 언급
- "왜 묻나요?"는 반대 구간이 있을 때와 없을 때 문구를 나눈다 (12장: 같은 방향이면 반대 문구 금지)
- 대안 A~D + 공통(원안 유지·수정·보류)은 4단계에서 사용

### 기본 검토 (조건은 초안 — 데이터 담당 검수 대상)

| 규칙 | 조건 | 결과 |
|---|---|---|
| R01 목표와 사용처 | 목표에 "참여 상점 이용 확대" 포함 + 쿠폰 사용처가 비어 있음 | `question` 사용처 범위 확인 |
| R03 대상과 자료 | 대상 문구에 "관광객"이 있고 성과지표가 외국인 카드 지표 | `question` 외국인 전체 자료로 관광객 성과를 볼 수 있는지 (merge_group: participation_data) |
| R04 기간 | 사업 기간이 31일 미만 + 카드 지표를 직접 평가 | `question` 별도 실적·집계 주기 (merge_group: participation_data). "월 경계와 맞지 않음"은 판단 기준이 모호해 넣지 않는다 |
| R05 운영 조건 누락 | `validate_plan().pending`이 비어 있지 않음 | `pending` 추가 확정 필요 목록 |
| R06 | 항상 | `not_reviewed` "분석 예시로 제시" |
| R02 | 항상 | `not_reviewed` "지역 기준 확인 후 적용" (시도 근거가 allowed여도 자동 활성화 금지) |

---

## 5. 코드

| 파일 | 내용 |
|---|---|
| `review/outcome.py` | `ReviewOutcome`(rule_id, question_key, kind, title, message, why, evidence_ids, scope_label, region_note, options, related_fields, related_rule_ids, observations), `to_llm_summary()`(수치 제외), `ReviewResult`(outcomes, **no_finding**(확인했으나 해당 없음), not_reviewed(이번 범위 밖), evidence_id) |
| `review/r07_indicator.py` | 위 판단 순서, 문구 채우기 |
| `review/basic_checks.py` | R01·R03·R04·R05·R06·R02 |
| `review/engine.py` | 실행 순서(R07→R03→R04→R01→R05, 이어서 R06·R02는 검토하지 않음 표시), `merge_group` 연결(카드는 합치지 않음), 규칙 ID·함수 일치 검사(**모듈을 불러올 때 한 번** — 앱 시작 단계에서 멈추게) |
| `review/rules.py` | JSON v1.0 읽기(문구·옵션·merge_group·related_fields) |
| `web/routes/questions.py` | `GET /step/3` (원안 없으면 1단계, 근거 오류면 503 오류 화면) |
| `web/templates/steps/questions.html`, `partials/rule_card.html` | 시안 S3 구성 |
| `web/routes/steps.py` | `FIRST_PLACEHOLDER_STEP = 4` |

- `engine.run_review(plan, evidence_result)`는 순수 함수. 같은 입력이면 같은 결과 (난수·시간 사용 금지)
- 2단계의 `select_main_record`·범위 칩을 재사용해 두 화면이 같은 레코드를 쓴다

---

## 6. 테스트

| 파일 | 확인 |
|---|---|
| `test_review_rules.py` (신규, 약 20개) | 참고용 지표 → 안내(오류 문구 없음), 직접 평가 → 질문, 지표 없음 → 결과 없음, blocked·needs_review·비교 불가 → 보류, 방향이 같을 때 반대 문구 없음, 목표 불일치 문구, R01·R03·R04·R05 조건, R06·R02 검토하지 않음, merge_group 묶기와 근거 ID 보존, 판정 단어 없음, JSON 규칙 ID와 함수 일치, 같은 입력 반복 시 같은 결과 |
| `test_question_routes.py` (신규, 약 8개) | 3단계 잠금, 질문 카드 표시, 검토하지 않은 항목 표시, 전국 참고 칩, 근거 오류 시 503, 원안 변경 안내, 임시 화면이 아님, 4단계는 임시 화면 |
| 브라우저 | 예시 기획 → 3단계 질문·근거 칩·검토하지 않은 항목, 지표 용도를 참고 현황으로 바꾸면 안내로 바뀜, 콘솔 오류 없음, 400px |

예상: 193 → 약 220개

---

## 7. 커밋

| # | 커밋 | 포함 |
|---|---|---|
| 1 | `3-1·3-2 검토 규칙 문서와 규칙 원본 JSON v1.0` | docs/review_rules.md, review_rules.json, rules.py 확장, 테스트 |
| 2 | `3-3 검토 규칙 실행` | outcome.py, r07_indicator.py, basic_checks.py, engine.py, test_review_rules.py |
| 3 | `3-4 검토 질문 화면` | questions.py, questions.html, rule_card.html, CSS, steps.py, test_question_routes.py, 브라우저 확인 |

---

## 8. 위험

| 위험 | 대응 |
|---|---|
| R01·R03·R04 조건이 데이터 담당 검수에서 바뀜 | 조건을 함수 1개씩으로 분리해 수정 범위를 좁힘. `docs/review_rules.md`에 검수 칸 |
| 질문 문구가 근거 이상으로 단정 | 문구를 JSON 한곳에 두고 금지 단어 테스트, 4장 순서표대로 상태 구분 |
| 시도 연결 규칙(C-2) 미정 | 전국 참고만 사용. 결정되면 2단계와 같은 함수 1곳만 수정 |
| 4단계 선택과 연결 | `question_key`를 먼저 정해 4번에서 그대로 사용 |

## 9. 데이터 담당 확인 항목

1. R01·R03·R04 조건이 적절한가 (특히 "관광객" 문구 판정, 31일 기준)
2. R07 질문 문구가 근거 이상으로 단정하지 않는가
3. 대안 A~D의 "필요 자료·운영 부담" 표기가 맞는가
