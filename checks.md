# checks.md — 합의·연결 확인·테스트·남은 작업

기준 문서: [최종기획서](../최종기획서.md) · [개발업무 워크플로우](../개발업무_워크플로우.md)

## 1. 운영 합의

| 항목 | 결정 내용 | 상태 |
|---|---|---|
| 데이터 분석 담당 | (이름) | 미정 |
| 서비스 개발 담당 | (이름) | 미정 |
| 【들어오는 파일】 분석 근거 파일 전달 방법 | **방법을 미리 정하지 않는다(9/18).** 직접 전달(USB 등)이나 두 사람만 접근하는 비공개 공유 폴더 중 편한 방법을 쓰되, 단체방·메신저·이메일·GitHub·외부 AI 도구로는 보내지 않는다(보안 서약). 받은 파일은 `private/`에 둔다 | 결정(사용자, 9/18) |
| 【들어오는 파일】 분석 근거 파일명 | `review_evidence_real_v{버전}.json` (합성: `review_evidence_demo_v{버전}.json`) | 제안 |
| 【들어오는 파일】 서비스에서 불러오기 | 합성/실제 파일 연결 방식과 LLM 선택과의 조합은 개발 진행 상황을 보고 개발 담당이 결정. 우선 합성 파일을 코드에 연결해 개발 | 보류(개발 담당 결정 예정) |
| 코드 폴더 | 작업 폴더 아래 `Service` (uv 프로젝트, 로컬 git). `checks.md`도 이 폴더에서 관리. 합의 당시 경로는 `1차공모\Service`였고 9/18 현재 상위 폴더 이름만 바뀌었다 | 결정(개발) |
| 폴더 구조 | 기능별 패키지(plan·evidence·review·choices·document·llm)와 웹 계층(web) 분리, 코드가 읽는 자료는 `resources/`. 의존 방향 web → document → choices → review → evidence·plan, llm은 review 결과만 사용. 상세는 README | 결정(개발) |
| 데이터 담당 산출물 위치 | 저장소 밖 상위 폴더 (data_notes.md, 분석 코드). 서비스와는 `review_evidence.json`만 주고받음. 합의 당시 이름은 `1차공모\Analysis\`였고, 9/18 현재는 상위 폴더의 `Data\`·`Data2\`로 받고 있다 | 결정(개발), 데이터 담당 공유 필요 |
| 개발 환경 | uv + Python 3.12 (`.python-version`), 의존성은 `pyproject.toml`·`uv.lock` | 결정(개발) |
| 코드 저장소 | GitHub **public** 저장소 https://github.com/aideveloperno1/TM1stproject_260915 (9/15 연결·공개 전환). 실제 분석 근거 파일(`private/`, `*_real_*.json`)과 `.env`(API 키)는 `.gitignore`로 제외 | 결정(개발) |
| 라이선스 | 오픈소스 라이선스 없음(모든 권리 보유). 9/15 GPL v3 LICENSE 삭제, README에 권리 고지. 이유: 참가신청서류 준수사항 5항(수상작 지식재산권 비씨카드 귀속)과 GPL의 영구 이용 허락이 충돌. 이력의 초기 커밋(`2cb4e19`)에는 LICENSE가 남아 있음 | 결정(개발), 데이터 담당 공유 필요 |
| 지역 선택 목록 출처 | `데이터/2` 주민등록인구 CSV(공개 자료)의 시도·시군구 이름. 카드 CSV는 사용하지 않음 | 결정(개발) |
| 화면 스타일 | 직접 작성한 CSS. 디자인은 `프론트_예시` 폴더(정책맵 랜딩·워크스페이스 시안) 참고 | 결정(개발) |
| CSS 파일 나누기 | **나누지 않음.** `style.css` 한 파일로 유지(9/17 약 1,200줄). 나눠도 동작·속도는 같고, 규칙 순서가 바뀌면 인쇄 모양 등이 달라질 위험만 생김. 처음 README에 적었던 "700줄 넘으면 분리" 기준은 폐기 | 결정(사용자, 9/17) |
| 기술 방식 | 전부 Python: FastAPI + Jinja2 템플릿(서버 렌더링) + 화면 반응용 소량 JS. 테스트는 pytest. (9/15 Vite + TypeScript에서 변경) | 결정(개발) |
| 작업 상태 보관 | 로컬은 서버 메모리, Vercel은 Upstash Redis. 쿠키에는 무작위 세션 ID만 두고 기획·선택·AI 의견은 버전 있는 JSON으로 저장한다. 기본 만료는 마지막 이용 후 24시간 | 결정·구현(사용자, 9/21) |
| 차트 | Chart.js (CDN으로 템플릿에서 사용) | 결정(개발) |
| 공개 배포 호스팅 | **Vercel FastAPI + Upstash Redis**. Python 3.12, 서울 리전, 함수 최대 60초. Google AI와 Redis 자격정보는 Vercel Secret으로만 등록 | 결정·코드 반영(사용자, 9/21) |
| 화면 흐름 | 시안의 5단계 (기획 입력 → 근거 확인 → 검토 질문 → 보완 선택 → 보완 기획안). 기획서의 "실행 조건"은 보완 선택 화면의 추가 입력 칸, "출력"은 보완 기획안 화면의 [결과 저장] | 결정(개발) |
| 사업 목표 선택지 | 최종기획 기준 (금액 확대/비중 확대/참여 상점 이용 확대/기타, 여러 개) | 결정(개발) |
| 지표 용도 | 직접 성과평가 / 참고 현황 / 아직 모름 ("아직 모름"은 결론 없이 선택지만 제시) | 결정(개발) |
| 보완 선택지 | 시안의 A~D 대안 + 원안 유지·보류 + 수정(선택한 대안 내용 편집) | 결정(개발) |
| 보완 기획안 화면 버튼 | [결과 저장], [전체 문서 보기], [정밀 분석 요청서 초안], **[PDF로 저장]** | 결정(개발) · 9/16 변경: 인쇄/PDF 제외 → 브라우저 인쇄 방식으로 추가 (`.md`는 고쳐 쓰는 원고, PDF는 공유용 사본. 서버 PDF 생성기는 한글 글꼴·배포 부담으로 쓰지 않음) |
| 검토 규칙 표시 | 최종기획 기준: R07 검토 구현, R01·R03·R04·R05 기본 검토, R06 분석 예시, R02 향후 기능 | 결정(개발) |
| 시도(제주) 근거 추가 | **이번 제출 범위에서 제외, 향후 확장.** 제출용 저장소는 전국 레코드만 쓴다. 지역별 안내 문구·예시 기획 지역·캡처·사용법 재작성이 함께 필요해 제출 일정(9/21)과 겹친다. 제출 PDF에는 "조건을 갖춘 시도로 확장 가능"으로만 소개. **9/19: 복제본 `Service_사본_20260918/`에서 코드로 완성했고 원본에는 아직 넣지 않았다** | 결정(사용자, 9/18) |
| 업종 비율 화면 계산 기능 | **이번 제출 범위에서 제외, 향후 확장.** 근거 파일 형식이 업종 합계 기준이라 형식 재합의·검증·시험 자료가 모두 바뀌고, 화면에 업종 비율을 띄우면 추천으로 읽혀 "판정하지 않는다" 원칙과 부딪힌다. R06은 분석 예시, R02는 향후 기능 표시 유지 | 결정(사용자, 9/18) |
| 쿠폰 발급·사용·정산 실적 합성 | **만들지 않음.** R07 대안 A는 수집 계획을 기획안에 적는 기능이라 실적 수치 없이 끝까지 동작한다. 시행 전 사업의 실적 수치는 합성이라고 밝혀도 효과가 있었던 것처럼 읽힐 수 있다(최종기획서 10장) | 결정(사용자, 9/18) |
| 검토 규칙 관리 번호(R01~R07) 화면 표시 | **화면과 보완 기획안에서 완전히 뺀다.** 질문·선택 카드, 1단계 예정 목록, 3단계 오른쪽 목록, 함께 확인 안내, AI 의견 근거 배지, 5단계 변경 표·근거 추적, 문서 별첨 표까지 모두 **규칙 제목**을 쓴다. 번호는 `resources/rules/review_rules.json`·`docs/review_rules.md`·데이터 담당 검수 등 **내부 문서에만** 남긴다 | 결정(사용자, 9/18) |
| 정적 파일(CSS·JS) 캐시 | **주소에 파일 버전을 붙이고(`?v=`) 응답에 `Cache-Control: no-cache`를 둔다.** 기본 설정에서는 브라우저가 옛 JS를 계속 써서 고친 화면이 반영되지 않았다(입력 상태 패널이 실시간으로 안 바뀌던 원인). 버전은 서버를 시작할 때 정적 파일 수정 시각으로 정하므로, **파일을 고치면 서버를 다시 시작**한다 | 결정(개발, 9/18) |
| 서비스 이름 | **콕콕** (부제: 소비데이터로 기획안 한 번 더 보기). "확인할 곳만 콕 짚어 준다"는 동작을 그대로 담은 이름이며, 판정하지 않는 성격에 맞게 부드러운 쪽으로 골랐다. **화면·문서에 보이는 이름만 바꾸고** 꾸러미 이름(`policy_signal_map`)과 실행 명령(`policy-signal-map`)은 그대로 둔다 — 170여 곳을 고쳐도 사용자·심사위원이 볼 일이 없다 | 결정(사용자, 9/19) |
| 랜딩 페이지 | **`/` 주소에 추가함(9/19).** `base.html`을 쓰지 않는 별도 화면이며 **세션을 만들지 않는다**. 글자는 `web/landing_content.py` 한곳에 모으고, 캡처는 `docs/screenshots`를 `/shots`로 내보내 **복사하지 않는다**. 동적 효과는 **설명하는 움직임만** 넣고(등장·지역 사다리·방향 비교·보류·추적), 동작 줄이기·JS 꺼짐·인쇄·캡처에서 모두 최종 상태로 보인다 | 결정(개발, 9/19) |
| 시안 오류 수정 | 자료 버전은 불러온 파일에서 표시(합성은 `demo-`), 방향 판정은 반올림 비중이 아닌 원 금액 교차곱(워크플로우 8-2) | 결정(개발) |
| 지역 근거 연결 키 | **행정표준코드 10자리**(서울 `1100000000`, 강원 `5100000000`)로 입력 지역과 근거 파일 `region_key`를 잇는다. 코드는 화면에 보이지 않고 지역 이름으로 바꿔 보여 준다. 지역 이름이 바뀌어도(강원도 → 강원특별자치도) 코드는 그대로다 | 결정(사용자, 9/18) |
| 규칙 원본 | `resources/rules/review_rules.json`이 원본, `docs/review_rules.md`는 사람이 읽는 설명 | 결정(개발) |
| 【나가는 파일】 보완 기획안 저장 | [결과 저장] 클릭 → 저장 위치 선택 창 → 사용자가 고른 폴더에 Markdown 저장. 선택 창을 지원하지 않는 브라우저는 다운로드 폴더로 저장하고 안내 | 결정(개발) |
| 【나가는 파일】 기본 파일명·별첨 | `보완기획안_{사업명}_{날짜}.md`, 별첨(변경 전후와 근거)은 같은 파일 맨 뒤 | 결정(개발) |
| 입력폼 필수·선택 | 필수: 사업명, 목표, 대상, 지역, 기간, 현재 성과지표, 지표 용도 / 선택: 예산, 사용처, 자료 확보 상태, 변경 불가 조건. **비우면 기획안에 "추가 확정 필요"로 남는 항목은 예산·사용처·자료 확보 상태 셋**이다. 변경 불가 조건은 없을 수 있는 항목이라 비어도 남기지 않고 "원안에 기재 없음"으로만 적는다(9/18 확인). 자료 확보 상태를 확보됨이 아닌 값으로 고르면 고른 값과 함께 남는다(예: 성과 자료 확보 (협의 중)) | 결정(개발) |
| 입력폼 목표 | 여러 개 선택 가능 (금액 확대/비중 확대/참여 상점 이용 확대/기타) | 결정(개발) |
| 입력폼 지역 | 전국/시도/시군구 선택 목록 | 결정(개발) |
| 입력폼 기간·예산 | 기간은 시작일~종료일, 예산은 금액 칸 + [미정] 체크 (미정과 0원 구분) | 결정(개발) |
| 공개 배포 | 공개 배포본에는 합성 자료만 포함. 실제 분석 근거 파일은 git 제외 폴더(`private/`)에만 둠 | 기획 확정 |
| 구현 순서 | A(규칙 기반 R07 사례 완성) 후 C(LLM 추가 점검 의견) 도입 | **9/16 구현 완료(로컬 전용).** 외부 전송이 없어 기획서 8장 변경은 불필요. 11장 "조건부 확장"에 한 줄 추가할지는 미정(6장) |
| LLM 제공자 | **로컬만 사용.** 클라우드는 설정·차단 규칙과 교체 지점만 남기고 호출 코드는 만들지 않음 | 결정(사용자, 9/16). 필요해지면 합성 자료 + 클라우드 방향 |
| 공개 배포 | 보류 | 결정(사용자, 9/16). 배포하면 로컬 LLM은 동작하지 않으므로 AI 영역 표시 방법을 그때 정함 |
| 실제 자료 + 로컬 LLM | **허용.** 로컬 모델은 PC 밖으로 보내지 않고, 모델에는 수치 없이 규칙 결과와 상황 설명(예: "금액과 비중의 변화 방향이 서로 달랐던 인접 월 구간이 있었다")·담당자 입력만 넘긴다. 실제 자료 + 클라우드는 계속 차단. 2차 연결 확인(6-1) 숫자 대조 중에는 AI 의견을 끈다(`PSM_LLM_PROVIDER=none`) | 결정(사용자, 9/17) |
| AI 모델 선택 | **담당자가 3단계에서 AI 참고 의견 모델을 고르는 정식 기능.** 고를 수 있는 모델은 `.env`의 `PSM_LLM_MODELS`, 선택 위치는 3단계 AI 영역, 받아 두지 않은 모델은 고를 수 없게 표시, 모델별 의견을 나란히 비교하지 않음. 의견은 여전히 선택·문서에 반영하지 않음 | 결정(사용자, 9/17) · 구현 C-8 |
| AI 응답 시간 한도 | 120초 (C-0: 모델 전환 후 첫 응답 약 40초, PC를 켠 뒤 Gemma 첫 호출 약 74초). 처음 60초로 정했다가 같은 날 120초로 변경. `.env`의 `PSM_LLM_TIMEOUT_S`로 바꿈 | 결정(사용자, 9/17) |
| LLM 연결 구조 | 제공자를 설정으로 고르는 공통 연결 계층(`llm/base.py`). 로컬만 구현했고 클라우드는 `cloud.py`를 추가하면 붙는 자리만 있음. API 키를 쓰게 되면 서버 환경변수에 둠 | 결정(개발), 9/16 로컬만 구현 |
| LLM 전달 자료 | ① 공개 시연은 합성 자료만 사용 ② LLM에는 수치 없이 규칙 결과(규칙 번호·종류·제목·적용 범위)와 규칙별 수치 없는 상황 설명(`llm_context`), 사용자 목표·대상·지표·지표 용도만 전달. 수치는 규칙 엔진이 화면에 표시 | 개발 담당 동의, 9/16 구현. 데이터 담당 합의 전 |
| LLM 출력 원칙 | 수치·수량 표현 금지, 규칙 번호 표시와 존재 확인, 검토하지 않은 규칙 제안 금지, 판정 표현 금지, "AI 참고 의견"으로 분리 표시, 기획안·선택에 반영하지 않음 | 9/16 구현(`llm/guard.py`). 데이터 담당 합의 전 |
| 【고도화】 작업 폴더 | 지역·사업 유형 확장은 **복제본 `Service_고도화_20260920`**에서만 진행. `Service_사본_20260918`과 원본 `Service`는 건드리지 않아 돌아갈 지점으로 둔다. 시작 시점 상태는 커밋 `0-0`으로 고정(시험 481개 통과, 공개 저장소 검사 위반 없음) | 결정(사용자, 9/20) |
| 【고도화】 근거 파일 2.1 | **2.0 레코드를 그대로 두고 `profiles`·`thresholds` 묶음만 더한다.** 허용 버전 문자열만 추가하는 방식은 쓰지 않고 2.1 전용 읽기 코드를 따로 둔다. 2.0 파일을 넣으면 기존 동작 그대로, 새 화면만 "자료 없음" | 결정(개발, 9/20) · `7지역확장계획.md` 3장 |
| 【고도화】 보완 기획안 장 | **8개 그대로 유지.** 지역 소비 프로필 요약과 유사 지역은 **별첨 2(근거와 해석 한계)**에 넣는다. 장을 늘리면 문서 뼈대·변경 전후·시험이 모두 따라 바뀌고, 프로필은 사업 내용이 아니라 근거이기 때문 | 결정(개발이 추천한 안 그대로 진행 지시, 사용자 9/20) |
| 【고도화】 질문 이름표 | 한 규칙이 질문을 둘 이상 내면 이름표를 `규칙ID-이름`으로 나눈다(예: 기간·시기). 재확인 기준(관련 입력 항목)도 질문 단위로 내린다. 지금은 둘 다 규칙 번호 하나여서 선택이 서로 덮어쓰거나 필요 없는 재확인이 뜬다 | 결정(개발, 9/20) |
| 【고도화】 기준값 | 질문을 띄우는 기준값(분위·지수 등)은 **코드에 적지 않고 근거 파일에서 버전과 함께 받는다.** 화면에는 "운영 기준"으로 표시 | 결정(개발, 9/20) |
| 【고도화】 유사 지역 | 고르지 않은 다른 지자체의 소비 구조를 보여 주는 기능이므로 **공개·시연은 합성 자료로만**, 실제 자료로는 내부 확인용으로만 켠다 | 결정(개발, 9/20) |
| 【고도화】 인구로 나눈 값 | 1인당 소비·등록외국인 1인당 결제액 같은 값은 **만들지 않는다.** 카드 이용자와 등록 인구는 세는 범위가 다르다 | 결정(개발, 9/20) |

## 2. 연결 확인 일정

| 차수 | 일시 | 확인 내용 | 결과 |
|---|---|---|---|
| 1차 | 9/17 | 첫 결과 1건을 서비스가 오류 없이 읽는가 (필드명·단위·상태값) | **사전 점검 — 확정본 도착 후 다시 확인.** 데이터 담당 임시본(9/16, 확정본 아님)으로 값은 보지 않고 구조·계산 일치 여부만 확인했다. **읽기:** `data_kind` 값과 `month` 표기 두 곳이 9-3 형식과 달라 서비스가 파일을 읽지 않음 → 확정본에 반영 요청. 두 곳만 고친 사본(메모리 안)으로는 규칙 실행과 근거 화면 데이터 생성까지 진행됨. **계산:** 파일에 담긴 인접 월 비교 결과가 서비스 계산과 모두 일치.**추가 칸:** `comparisons`(경고 후 무시). 유지 여부는 데이터 담당과 결정 |
| 2차 | 9/18 (시간) | 1~6월 실제 결과의 수치·질문·문서 반영이 분석 결과와 같은가 | |
| 3차 | 9/20 (시간) | PDF에 넣을 화면·문서 최종 대조 | |

## 3. 전달 형식

- `review_evidence.json` schema 2.0 (워크플로우 9-3장)
- 금액: 원 단위 정수 / 비중: 퍼센트 값 (8% → `8.0`)
- 구조·단위 변경 시 데이터 담당이 버전을 올리고 알림. 개발 담당은 결과 파일을 직접 수정하지 않음

상태: **데이터 담당 확정 (9/15 결정 문서, 저장소 밖)** — 9-3 예시 형식 그대로, 금액 원 단위 정수, 비중 퍼센트 값(미리 반올림하지 않음), `calculation_status`·`applicability`·`reason` 필수, 행이 없거나 분모가 0 이하면 0으로 채우지 않고 상태값으로 표시. 원본 CSV는 모든 열을 문자열로 읽고 금액·건수만 숫자로 변환. 9/17 임시본 사전 점검에서 `data_kind`·`month` 표기 두 곳이 달라 확정본에서 다시 확인한다(2장 1차).

## 4. 테스트 (워크플로우 12장)

| 시험 | 기대 결과 | 결과 |
|---|---|---|
| GENDER_CD=x와 AGE_CD=x가 섞임 | U는 성별 컬럼 미상만 집계 | 데이터 담당 소관 (원본 집계) · **정의 확인 9/15 (데이터 담당 결정 문서):** U는 `GENDER_CD` 정규화 값 `x`로만 정하고 `AGE_CD=x`라는 이유만으로 넣지 않음, 허용 값 `1`·`2`·`3`·`x`이고 그 밖은 `invalid_input`. 이 정의로 1~6월을 다시 계산한 결과가 최종기획서 4-2 표와 일치(수치는 기획서에만). 원본 집계 단계라 서비스 테스트 대상은 아님 |
| 숫자·코드 오류 | invalid_input과 사유 표시 | 통과 9/16: 계산(`test_evidence_loader`) · 화면은 오류 화면 503에 수치 없는 문구 (`test_step_two_error_page_when_evidence_fails`) |
| 분모 0·자료 없음 | null과 정확한 상태, 나누기 오류 없음 | 통과 9/16: 계산 + 화면 "계산 불가"·"분모 0 · 계산 불가"·보류 구간 (`test_step_two_expected_values`) |
| 미상 0 또는 관측 행 없음 | 값과 관측 상태를 구분, 실제 0 단정 없음 | 통과 9/16: no_data에 0 거부, 화면 "자료 없음", 0이 아닌 작은 값은 "0.01 미만" (`test_small_nonzero_values_are_not_shown_as_zero`) |
| 비교 A / 비교 B | 별도 결과로 계산·표시 | 통과 9/16: 계산 + 구간 표의 별도 열 |
| 이전 F=0·변화 없음·월 누락 | 증감률 null·반대 아님·비교 보류 | 통과 9/16 (`test_evidence_compare`, 화면 "계산 불가"·"보류 — 사유") |
| 아주 작은 방향 차이 | 중립적 표시, 강한 경고·자동 수정 없음 | 통과 9/16: 화면 "0.01%p 미만 (증가)", 경고 클래스 없음 (`test_step_two_shows_tiny_change_label`, `test_step_two_has_no_red_warning_classes`) |
| 방향이 같은 두 기간 | 반대 방향 예시 문구를 복사하지 않음 | 2단계 요약 문장 통과 9/16 (`test_same_direction_sentence_has_no_opposite_wording`) · 3단계 "왜 묻나요" 문구 통과 9/17 (`test_same_direction_does_not_reuse_opposite_wording`) |
| 전국 자료 + 특정 지역 기획 | 전국 참고 표시 | 2단계 통과 9/16 (범위 칩·지역 안내 문구, `test_region_note_for_sigungu_plan`) · 3단계 통과 9/17 (질문 카드 범위·지역 안내가 2단계와 같음, `test_region_note_and_scope_label_match_evidence_screen`) · 5단계 통과 9/17 (근거 추적에 "전국 참고 — 특정 지역의 진단이 아님", `test_changed_line_carries_evidence_chip_and_trace`) |
| 검증 시도 자료 + 시군구 기획 | 넓은 범위 참고 표시, R02 자동 활성화 없음 | R02는 항상 "검토하지 않음"으로 표시 9/16 (`test_r06_and_r02_are_not_reviewed`) · 시도 자료 연결은 C-2 결정 후 |
| 참고용 지표 | 불필요한 직접 성과 오류 없음 | 통과 9/16: 지표 용도가 참고 현황이면 안내만 표시하고 질문 0건, 판정 단어 없음 (`test_reference_indicator_use_is_notice_without_error_wording`) |
| 자료 수집 미정 | 추가 확정 필요 표시 | 통과 9/16: 빈 담당자·주기가 문서에 `[추가 확정 필요]`로 남고 8장에 모임 (`test_empty_owner_and_cycle_become_pending_marks`, `test_plan_pending_items_are_listed_once`) · 9/17 6-5b: 쓰지 않게 된 실행 조건은 8장에 남지 않음 — 질문이 사라짐·원안 유지로 변경·대안 A→B (`test_disappeared_question_leaves_no_execution_pending`, `test_keep_original_after_adopt_leaves_no_execution_pending`, `test_switching_a_to_b_leaves_no_execution_pending`), 문장에서 쓰는 주기는 유지 (`test_r04_b_keeps_entered_cycle_after_r07_is_kept_original`) |
| 목표 유지·대안 취소 | 미채택 내용이 최종 문서에 남지 않음 | 통과 9/16 (`test_cancelled_choice_leaves_no_trace`, `test_without_choices_document_keeps_original_and_has_no_changes`) |
| 목표·지역 변경 | 관련 선택과 문서 재검토 | 통과 9/16: 재확인 필요가 남으면 문서를 만들지 않고 4단계로 되돌림 (`test_unanswered_question_sends_back_to_step_four`, `test_changed_plan_marks_choice_for_recheck`) · 9/17 6-5a: 다시 저장하면 재확인이 풀리고 5단계로 감, 4단계를 건너뛰어도 5단계·내려받기가 재확인을 요구 (`test_saving_again_clears_recheck_and_opens_step_five`, `test_step_five_without_step_four_still_asks_for_recheck`, `test_download_without_step_four_is_blocked_after_plan_change`), 보관 안내는 이번 변경분만 (`test_archive_notice_shows_only_the_latest_plan_change`) · 브라우저 확인 9/17 (5장) |
| Markdown 저장 | 저장 위치 선택 후 외부에서 열었을 때 화면과 내용 일치 | 자동 확인 9/16: 내려받기 본문이 화면 내용과 같고 한글 파일명 인코딩 정상 (`test_download_sends_markdown_with_korean_filename`) · 9/17 6-5d: 5단계를 연 뒤 원안이 바뀌면 [결과 저장]이 재확인을 안내하고 내려받지 않음 (`test_save_script_does_not_download_recheck_response`, 브라우저 확인) · **외부 편집기로 열어 보기·실제 저장 위치 선택 창은 사용자 확인 대기** |
| PDF 저장 | 인쇄 결과에 화면 전용 요소가 없고 표·변경 표시·고지가 남음 | 사용자 검토 완료 9/16 · 개발 확인: 인쇄 규칙 14개 적용, A4 폭 186mm에서 표 잘림 없음(654px), 변경 줄은 배경색 없이 왼쪽 선으로 구분 (`test_pdf_button_is_on_every_result_screen`, `test_print_stylesheet_hides_screen_only_parts`) |
| 공개 배포 | 실제 제한 자료 없이 합성 표시 유지 | 저장소 검사 통과 9/16 (`check_public_bundle.py` 위반 0) · 배포본 확인은 7단계 |

## 5. 진행 기록

| 날짜 | 완료한 일 | 확인 방법 |
|---|---|---|
| 9/15 | 기획 입력 화면(S01) TypeScript 버전 구현 (커밋 `37071ee`) | `npm run build`, 브라우저 확인 |
| 9/15 | 개발 환경을 uv + Python 3.12로 전환하고 기획 입력 화면(S01)을 FastAPI + Jinja2로 다시 구현: 필수·선택 항목, 목표·지표 복수 선택, 전국/시도/시군구 선택(시도 17·시군구 269), 예산 미입력/미정/금액 구분, 서버 검증·오류 표시, 추가 확정 필요 요약, [검토 시작] 시 원안 보관, 원안 변경 후 재제출 시 재확인 표시, 예시 기획 채우기 | `uv run pytest` 15개 통과, 브라우저에서 예시 채우기→시도 변경 시 시군구 목록 갱신→검토 시작→2단계 원안 표시, 콘솔 오류 없음 |
| 9/15 | 폴더 구조 전환: plan/·review/·web/(routes·templates·static)·resources/ 분리, 규칙 표시를 `review_rules.json`에서 읽도록 변경, 테스트를 검증·화면 흐름으로 분리, `../Analysis/` 폴더와 안내 README 생성 | `uv run pytest` 18개 통과, 지역 목록 재생성 결과 변경 없음, 실행 중 서버에서 입력 화면·규칙 목록·정적 파일 응답 확인 |
| 9/16 | 서비스 완성 기준 폴더 전체 생성(evidence·choices·document·llm·web 하위·resources 하위·tests/fixtures·docs·private 등 26개), 폴더마다 README.md에 만들 파일·상세 사양·원칙·테스트·일정 작성. `private/`는 README만 추적되도록 `.gitignore` 조정 | 모든 폴더 README 존재 확인, `git add --dry-run`에서 private는 README만, `uv run pytest` 18개 통과 |
| 9/16 | 1. 근거 계산 계층 완료 (계획 검토 11건 반영): 설정(`config.py`, 실제 자료+클라우드 LLM 차단), 근거 파일 형태·검증(수치 비노출 오류 문구, 규칙 선행 조건)·로더, 경계 사례 22개(생성 스크립트), 비교 A/B(정수 교차곱)·기간 요약, 합성 근거 파일 demo-001, 공개 자료 검사(한글 경로·원자료 csv 포함)·의존 방향 검사 | `uv run pytest` 119개 통과, `check_public_bundle.py` 추적 파일 106개 위반 0, 금지 import 임시 삽입 시 경계 테스트 실패 후 되돌려 통과 확인. 소수점 판정이 틀리는 큰 금액 사례에서 교차곱 정답 확인 |
| 9/16 | 4. 보완 선택 완료: 기본 검토 규칙에 대안 추가(R03·R04는 R07과 실행 조건 공유), 안내는 선택 가능·필수 아님, 보류·추가 확정 필요는 선택 없이 문서 기록, 원안 변경 비교(`plan/changes.py`), 선택 로직(검증·취소·추가 확정 필요·재확인·보관함), `/step/4` 화면 | `uv run pytest` 280개 통과, 브라우저에서 대안 A 선택 시에만 실행 조건 표시·저장 후 유지·R03에 공유 입력 채워짐·모든 질문 답하면 5단계 버튼 표시·보류는 추가 확정 필요로 기록·400px 정상·콘솔 오류 없음 |
| 9/16 | 규칙 ID·코드 함수 일치 검사를 요청 처리 중에서 앱 로드 시점으로 옮김 | 규칙 JSON에 R08을 임시로 넣고 `import policy_signal_map.app` 실행 → 종료 코드 1, 문구 "실행 함수가 없는 규칙: R08" 확인 후 파일 복구(`git diff` 변경 없음), `uv run pytest` 238개 통과 |
| 9/16 | 3번 자체 검토 후 수정: 질문 식별자를 규칙 ID로 바꿔 4단계 선택이 서로 덮어쓰지 않게 함(merge_group은 추가 입력 공유용으로 분리), 원안 변경 안내가 계속 남지 않도록 제출마다 다시 판단, 3단계 카드의 대안 미리보기 제거(대안은 4단계에서만), 규칙 JSON 전체 문구의 판정 단어 검사 추가 | `uv run pytest` 236개 통과, 질문 키 중복 없음·같은 내용 재제출 시 안내 사라짐을 테스트로 고정 |
| 9/16 | 3. 검토 질문 완료: `docs/review_rules.md` 초안(데이터 담당 검수 대기), 규칙 원본 JSON v1.0(문구·대안·merge_group), 규칙 실행(R07 판단 7단계, R01·R03·R04·R05, R06·R02는 검토하지 않음), 같은 자료를 묻는 질문은 카드를 합치지 않고 서로 표시, `/step/3` 화면(질문 카드·확인했으나 해당 없음·검토하지 않음 구분) | `uv run pytest` 232개 통과(규칙 30 + 화면 9 추가), 브라우저에서 예시+관광객 입력 시 질문 2건·추가 확정 필요 1건·해당 없음 2건·검토하지 않음 2건 확인, 400px 폭 정상, 콘솔 오류 없음 |
| 9/16 | AI 참고 의견(로컬 LLM) 구현: 규칙별 수치 없는 상황 설명(`llm_context`), 제공자 계층·로컬 호출(표준 라이브러리), 요청 문장, 출력 검사 6종, `/step/3/opinions`와 세션 캐시 | `uv run pytest` 388개 통과. 가짜 OpenAI 호환 서버로 끝까지 확인: 3단계 화면 9ms·의견 요청 별도, 수량 표현이 섞인 줄은 검사에서 버려짐(3줄 중 2줄만 표시), 모델·생성 시각 표시, 서버를 끄면 "AI 의견을 불러오지 못했습니다"만 표시되고 질문 카드는 정상, 콘솔 오류 없음. **실제 모델 품질·속도 확인(C-0)은 Ollama 설치 후** |
| 9/16 | 5-6 PDF로 저장 추가: 브라우저 인쇄 방식(`print.js`, `@media print`), 세 결과 화면에 [PDF로 저장] 버튼. 기존 결정 "인쇄/PDF 제외"를 변경 (`.md`는 고쳐 쓰는 원고, PDF는 공유용 사본) | `uv run pytest` 340개 통과, 인쇄 규칙을 화면에 적용해 A4 폭(186mm)에서 확인: 상단바·버튼 없음, 표 654px로 잘림 없음, 변경 줄 왼쪽 선 표시, 하단 고지 유지. **사용자 검토 완료** |
| 9/16 | 5. 보완 기획안과 저장 완료: 원안 문장 변환(`document/describe.py`), 선택 반영·변경 기록·8장·근거·요청서 초안(`builder.py`), Markdown 양식 2종, 저장 파일 이름, `/step/5` 비교 화면·전체 문서·요청서 초안·내려받기, 저장 위치 선택(`save.js`)과 근거 추적(`trace.js`), 임시 화면 삭제 | `uv run pytest` 338개 통과, 브라우저에서 예시 입력→대안 A 채택→5단계에서 변경 5건·추가 확정 필요 3건·근거 추적 패널·문서 본문 확인, 콘솔 오류 없음. 화면에 Markdown 기호(`**`)가 보이던 문제와 문서에 내부 키(`participation_data`)가 실리던 문제를 발견해 수정 |
| 9/16 | 2. 근거 확인 화면 완료 (계획 검토 13건 반영): 표시 형식(`formatting.py`, 사사오입·0.01 미만), 근거 상태 보관·오류 화면(전체 경로 비노출)·실제 자료+클라우드 시작 차단, 공통 렌더링, 화면 데이터 조립(요약 문장 3종, blocked 수치 숨김, 보류 사례는 합성만), `/step/2` 화면(차트 SRI·대체 문구, 월별·구간 표), 상단 자료 칩·하단 고지, 테스트 격리(`conftest.py`) | `uv run pytest` 193개 통과(셸에 PSM_ 변수 설정해도 동일), 공개 자료 검사 위반 0, 실제 자료+클라우드로 `uv run policy-signal-map` 실행 시 종료 코드 1, 브라우저: 계획서 9장 값과 화면 일치·차트 2개·분모 전환·보류 사례·CDN 주소를 임시로 틀리게 해 대체 문구 확인 후 복구·콘솔 오류 없음. 400px 폭에서 본문이 766px로 넓어지는 문제를 발견해 수정 후 381px 확인 |
| 9/17 | AI 참고 의견 실제 모델 확인(C-0)과 담당자 모델 선택(C-8): Ollama 0.34.1 + `exaone3.5:7.8b`(4·8비트)·`gemma4:26b-a4b-it-qat` 측정, Gemma는 생각 과정 출력을 꺼야 본문이 나와 `reasoning_effort "none"` 전송, 시간 한도 20→60초, `PSM_LLM_MODELS`·모델 표시 문구 파일·받아 둔 모델 확인·모델별 의견 보관·3단계 선택 칸 | `uv run pytest` 413개 통과. 합성 자료 + 실제 Ollama로 브라우저 확인: 모델 전환(첫 응답 12~16초), 돌아오면 보관한 의견 즉시, 목록 밖 모델 422, 콘솔 오류 없음, 400px 폭 넘침 없음. 측정표는 `6LLM참고의견계획.md` "C-0 측정 결과" |
| 9/17 | 실제 자료 식별 보강(6-0)과 해석 한계 구분 표시(6-0b): 임시 근거 파일 점검에서 `data_kind: actual_internal` 파일이 검증에 실패하면 실제 자료로 알아보지 못하는 것을 발견 → synthetic이 아닌 값은 모두 실제 자료로 보고, 검증에 실패해도 원문에서 확인(공개 자료 검사도 같은 기준). 2단계 해석 한계를 "자료의 한계(분석 담당 작성)"와 "서비스 해석 원칙"으로 나눠 표시 | `uv run pytest` 418개 통과, `check_public_bundle.py` 위반 없음. 임시 근거 파일이 실제 자료로 인식됨을 참/거짓만 출력해 확인(값은 보지 않음). 브라우저: 2단계 두 묶음 표시, 콘솔 오류 없음, 400px 폭 넘침 없음 |
| 9/17 | 6-5 전체 검토 수정: 원안 변경 후 재확인이 풀리지 않던 문제, 4단계를 건너뛰면 재확인 없이 문서가 만들어지던 문제, 쓰지 않는 실행 조건이 8장에 남던 문제(질문 사라짐·원안 유지로 변경·A→B), AI 의견 대기 중 원안 변경 시 잘못 보관, 결과 저장이 실패 응답을 파일로 내려받을 수 있던 문제. AI 응답 시간 한도 120초(사용자 결정) | `uv run pytest` 434개 통과(재현 테스트 16개 추가, 경합 테스트는 수정 전 실패 확인). 계획은 별도 작업 사본에 시제품으로 적용해 검증 후 반영. 브라우저(합성 자료): 5단계를 연 채 원안 변경 → [결과 저장] 누르면 재확인 안내(저장 창 안 뜸) → 상단 5단계 누르면 4단계로 이동·재확인 표시 → 저장 → 재확인 풀림·[보완 기획안 만들기] 표시 → 5단계 저장(저장 창 흉내)으로 문서 기록, 선택 취소 클릭 후 5단계가 4단계로 되돌림, 콘솔 오류 없음 |
| 9/17 | 6-5e 4단계 보관 안내를 이번 원안 변경분만 표시(원안을 여러 번 바꾸면 같은 질문이 쌓이고 다시 답한 질문도 계속 보관됐다고 나오던 문제). 실제 자료 + 로컬 LLM 허용 결정 기록 | `uv run pytest` 통과, 재현 시나리오(관광객 넣고 빼기 반복·사업명만 변경)를 테스트로 고정 |
| 9/17 | 6-4 브라우저 전체 흐름 확인(합성 자료, 창 없는 Chrome 1440px·400px, 콘솔 기록 수집). 확인 중 favicon 404 콘솔 기록과 4단계 "질문(없음)" 안내를 발견해 6-4a로 수정 | **① 정상 → 대안 A → 저장:** 예시 채움 → 2단계 차트 2개 생성·상단 "시연용 합성 수치" → 3단계 R07 질문·R05 추가 확정 필요·해당 없음·검토하지 않음 → 4단계 화면 입력칸으로 R07 채택·대안 A(수집 자료·협의 중·담당·주기) 저장 → [보완 기획안 만들기] → 5단계 "변경 5건 · 추가 확정 필요 3건 · 원안 유지 7개 장" → 전체 문서 → 내려받기 200, 파일명 `보완기획안_하반기_외국인_소비지원_쿠폰_20260917.md`, 콘솔 오류 0. **② 자료 부족 보류:** 2단계 보류 사례에 자료 없음·분모 0 표시 / R07 적용 상태를 needs_review로 바꾼 임시 사본(저장소 밖)으로 2단계 보류 문구·요약 숨김 → 3단계 R07 "보류" 카드 → 4단계 R07 선택 없이 [보완 기획안 만들기] → 5단계 문서 8장에 R07 보류 기록, 콘솔 오류 0. **③ 선택 후 취소, ④ 5단계 → 원안 변경 → 재확인:** 같은 날 6-5 확인(위 행). **⑤ 400px:** 1~5단계·전체 문서·요청서 초안 7개 화면 모두 문서 폭 400 = 화면 폭 400, 콘솔 오류 0. 6-4a: 관광객 기획에서 R07·R03 카드에 "같은 자료를 묻는 질문(R03)/(R07)과 함께 쓰는 입력" 표시, 예시 기획에서는 생략 |
| 9/17 | 8-2 사용법과 화면 캡처: 캡처 스크립트(`scripts/capture_screenshots.py`)로 합성 화면 6장, `docs/usage_guide.md` 작성. 캡처 환경은 로컬 실행 + 합성 자료(사용자 결정) | 캡처 6장을 직접 열어 확인(차트 정상, 04 카드 머리 보임, 상단 "시연용 합성 수치", 주소창 없음), 03 AI 문장 3줄 사용자 확인, 캡처 중 콘솔 오류 0. 사용법의 버튼·안내 문구는 템플릿과 대조, 판정 표현 검사(화면 문구 인용 1곳 예외). `05_saved_markdown.png`(외부 편집기 화면)는 사용자 캡처 대기 |
| 9/17 | 폴더 README 30개 최신화·쉬운 설명(R-1~R-5): 모든 README를 "이 폴더는 무엇인가 / 파일 목록(쉬운 말) / 자세한 설명(개발자용)" 틀로 통일, `resources/prompts/`·`resources/llm/` README 새로 작성. 코드와 어긋난 내용 정정(대기 시간 120초·모델 목록 설정, 실제 자료 + 로컬 LLM 허용, document 폴더 구조·함수, 테스트 개수 437, 경계 사례 생성 구분, CSS 구역 순서 등) | 폴더마다 추적 파일이 모두 파일 목록에 있는지·README 안 상대 링크가 열리는지 스크립트로 확인, 설명한 함수·필드·설정 기본값·테스트 개수를 코드와 `pytest --collect-only`로 대조, `uv run pytest` 437 통과, `check_public_bundle.py` 위반 없음 |
| 9/18 | 데이터 담당 검수 반영(A-7·A-6)과 이번 범위 확정(A-1): R04 "31일 미만은 서비스 운영 기준이며 통계 기준이 아님"을 `docs/review_rules.md`·`review/README.md`·`basic_checks.py` 주석에 명시, R01·R03·R04 검수 칸에 2026-09-17 기록(R07·R05는 대기로 표시), R02 "검토하지 않음" 이유에 "현재 자료는 외국인 전체 기준이라 관광객만 따로 볼 수 없습니다" 추가, 시도(제주) 근거·업종 비율 화면 계산·쿠폰 실적 합성을 향후 확장으로 확정(1장) | `uv run pytest` 437개 통과, `check_public_bundle.py` 추적 파일 198개 위반 없음. 판단 기준값(`SHORT_PERIOD_DAYS`)과 화면 문구는 바꾸지 않아 규칙 동작 변화 없음. 캡처 6장 재생성(합성 자료, 상단 "시연용 합성 수치" 확인) 후 `03_questions.png`에서 R02 새 문구가 잘림 없이 표시되는 것을 눈으로 확인, 캡처 중 콘솔 오류 0. AI 참고 의견 3줄 재생성(Gemma 4 26B) |
| 9/18 | 문서·주석 최신성 점검과 정리: `작업진행.md` 6-2(시도 추가)를 "하지 않음"으로 체크, 6-3 남은 칸에서 시도 자료 제외, 0-2 전달 방법 정리, 옛 폴더 경로(`1차공모/…`) 5곳을 현재 위치로, 현재 상태 표의 테스트 수 388→437·AI 항목 갱신, `checks.md` 1장 전달 방법 행 확정, `docs/README.md` 규칙 설명서 검수 상태, `docs/review_rules.md` 문장 빈칸 이름을 JSON과 통일(`{목표}`→`{goal_label}` 등 4곳), README 최신화 날짜 4곳 | 점검 스크립트로 README 30개 구조·링크(깨짐 0), 환경변수 7개(코드=`.env.example`=README), 화면 주소 15개(모두 문서에 있음), 파일별 테스트 수 29개 표와 합계 437 일치, 규칙 JSON 문구 ↔ 설명서 불일치 0(수정 후), CSS 구역 12개·1,219줄 확인. `uv run pytest` 437개 통과, `check_public_bundle.py` 위반 없음 |
| 9/18 | 검토 규칙 관리 번호를 화면·문서에서 제거: 카드 제목·1단계 예정 목록·3단계 오른쪽 목록·함께 확인 안내·4단계 보관/재확인 안내·AI 의견 문장과 근거 배지·5단계 변경 표·근거 추적·기획안 8장·별첨 표를 모두 규칙 제목으로 바꿈. `ReviewOutcome.related_titles`, `Change.rule_title` 추가, AI 의견은 `display_text`와 `rule_labels`로 전달 | `uv run pytest` 437개 통과(문구 검사 14곳 갱신), `check_public_bundle.py` 위반 없음. 캡처 6장 재생성 후 1·3·4·5단계를 눈으로 확인: 화면에 R01~R07 표기 없음(근거 파일 ID `DEMO-R07-NATIONAL`은 자료 이름이라 유지). 규칙 판단·질문 문구는 그대로 |
| 9/18 | 1단계 입력 상태 점검에서 찾은 5가지 수정: ① **입력 상태 패널을 입력 즉시 갱신**(제출 전에는 옛 값이 남아 "입력해도 안 바뀐다"로 보였음) ② 자료 확보 상태를 고르면 **고른 값을 항목 이름에 표시**(`성과 자료 확보 (협의 중)`, `자료 확보 상태 (선택 안 함)`) ③ `checks.md`·사용법·`plan/README` 문구 정정(변경 불가 조건은 비어도 추가 확정 필요로 남지 않음) ④ 금액과 [미정]이 함께 오면 **오류로 안내**하고 금액칸을 잠그지 않음(잠그면 입력 금액이 서버로 가지 않고 사라짐) ⑤ 아무것도 입력하지 않은 화면은 "입력 전"으로 표시 | `uv run pytest` 439개 통과(검증 시험 2개 추가), `check_public_bundle.py` 위반 없음. 창 없는 Chrome으로 확인: 예산 입력→1개, 협의 중 선택→`성과 자료 확보 (협의 중)`, 확보됨+사용처→0개, 사업명 지움→1개 확인 필요, 금액+미정→안내 문구와 오류 표시, 모두 지우기→"입력 전", 400px 폭 넘침 없음, 콘솔 오류 0. 캡처 6장 재생성 |
| 9/18 | 입력 상태가 그대로라는 확인을 추적: ① 사용자 브라우저가 **옛 `input.js`를 캐시**해 실시간 계산이 실행되지 않았고 ② 실행 중이던 서버가 수정 전 파이썬 코드라 항목 이름이 옛 값이었다. 정적 파일에 `Cache-Control: no-cache`를 붙여 브라우저가 매번 확인하게 함 | 사용자 Chrome에서 확인: 캐시된 파일 2,641바이트(실시간 계산 없음) vs 서버 파일 6,391바이트 → 원인 확정. 새 서버(8010)로 다시 확인하니 사업명→6개 확인 필요, 예산 100만원→항목 3→2개, 협의 중→`성과 자료 확보 (협의 중)`, 확보됨→0개로 **즉시 갱신**. `uv run pytest` 440개 통과(정적 파일 헤더 시험 추가) |
| 9/18 | 실시간 갱신이 계속 안 되던 원인 해결: 사용자 Chrome이 **옛 `input.js`(2,641바이트)를 캐시에서** 쓰고 있었다(서버 파일은 6,391바이트). `Cache-Control: no-cache`만으로는 이미 저장된 캐시를 비우지 못해, **정적 주소에 버전(`?v=수정시각`)을 붙여** 파일이 바뀌면 주소가 바뀌게 함 | 사용자 Chrome에서 캐시를 지우지 않은 채 확인: 새 주소로 불러와 예산 입력→항목 3→2개, 협의 중 선택→`성과 자료 확보 (협의 중)`로 즉시 갱신. `uv run pytest` 441개 통과(정적 주소 버전 시험 추가) |
| 9/19 | **지역(시도) 근거 확장 — 복제본 `Service_사본_20260918/`에서만 작업, 원본 미반영.** 레코드 선택을 `review/context.select_main(file, plan)` 한 곳으로 모아 고른 시도의 자료를 쓰고, 없거나 사용 불가면 전국으로 되돌린 뒤 되돌린 이유를 화면에 적는다. 시군구는 그 시도 자료 + "넓은 범위의 참고자료" 안내. 행정표준코드는 화면에 쓰지 않고 지역 이름으로 바꿔 표시. 표본 부족 지역(계산 가능 4개월 미만)은 차트·요약을 접고 계산하지 못한 달만 표시. 사업 기간이 자료 기간과 겹치지 않으면 한 줄 안내. 시연용 합성 파일에 강원 시도 레코드 추가(C-5). 규칙 문장의 "전국 자료에서"를 `{source}`로 바꿔 출처를 고정하지 않음 | `uv run pytest` **458개 통과**(지역 시험 9개·경계 파일 3개 추가, 기존 시험 15곳 갱신), `check_public_bundle.py` 위반 없음. 지역 레코드가 없는 파일은 결과가 바뀌지 않는 것을 회귀 시험으로 고정. 업종 후보 검토(R02)는 지역 자료가 있어도 계속 "검토하지 않음". 사용자 지시로 **git 작업은 하지 않음**(커밋·브랜치·push 없음) |
| 9/19 | **시연용 합성 파일에 시도 16곳 추가 (복제본).** 입력 화면의 시도 17곳 중 16곳에 레코드를 두고, **세종특별자치시는 일부러 비워** "자료가 없어 전국으로 되돌림" 화면을 시연할 수 있게 했다. 지역마다 다른 상황을 담아(정상·반대 없음·입력 확인 필요·자료 없음·분모 0·이전 달 0원·변화 없음·0.01%p 미만·비교 B만 반대·미상 제외 분모 0·확인 필요·사용 불가·표본 부족) **지역을 바꿔 고르는 것만으로 근거 화면의 모든 경우**를 볼 수 있다. 사유 문장은 지역이 아니라 자료 상태만 말한다. 보류 사례 예시는 한 건만 보여 주도록 제한 | `uv run pytest` **459개 통과**, `check_public_bundle.py` 위반 없음. 창 없는 Chrome으로 7가지 지역 확인: 강원(예시 기획)·서울(정상)·대구(입력 확인 필요)·전북(확인 필요)·전남(되돌림)·경북(표본 부족)·세종(자료 없음) 모두 의도대로, **10자리 코드 화면 노출 0**, 콘솔 오류 0. 예시 기획(강원) 수치는 그대로라 캡처 6장은 재생성 불필요. 억원 단위 화면은 합성 규모 규칙(수만 원 이하) 때문에 시연 파일에 넣지 않고 시험 파일에서만 확인 |
| 9/19 | **시군구까지 확장 (복제본).** 근거 고르기를 **시군구 → 시도 → 전국 사다리**로 바꿔 **쓸 수 있는 가장 좁은 범위**를 쓰고, 못 쓰면(자료 없음·사용 불가·표본 부족) 한 칸 넓힌 뒤 **넓힌 이유를 화면에 적는다**. 표본 부족은 같은 지역의 더 넓은 범위가 남아 있을 때만 넓히고(시군구→시도), 마지막 지역 칸에서는 수치를 접고 이유를 보여 준다. 시연 파일에 시군구 3건 추가(춘천시 정상 / 종로구 표본 부족 / 제주시 사용 불가). 근거 파일 형식에 `sigungu` 범위 허용값 추가 | `uv run pytest` **464개 통과**(시군구 시험 5개 추가), `check_public_bundle.py` 위반 없음. 창 없는 Chrome으로 5가지 확인: 춘천시(그대로)·강릉시(자료 없음→시도)·종로구(표본 부족→시도)·제주시(사용 불가→시도)·세종(→전국) 모두 의도대로, **10자리 코드 화면 노출 0**, 콘솔 오류 0. 예시 기획 안내 문구가 바뀌어 **캡처 6장 재생성**(AI 포함). 제출 의미: "확장 가능"을 말이 아니라 **동작하는 화면**으로 보이면서, 시군구 자료가 아직 부족하다는 사실도 같은 화면이 설명한다 |
| 9/19 | **랜딩 페이지 추가 (복제본).** `/`가 1단계로 넘기던 것을 랜딩으로 바꾸고 구역 8칸 구성(머리말·검토 흐름·왜 다른가·근거 설계·**지역 사다리**·신뢰 원칙·규칙 현황·맺음). 시안(`프론트_예시`)에서 규칙 번호·실제 자료 버전·억원 단위·`support.js`를 걷어내고 실제 값으로 맞춤. 동적 효과는 4층으로 나눠 **설명하는 움직임만**: 미세 반응 / 스크롤 등장(한 번만) / 지역 사다리 칩·방향 비교(숫자 없음)·보류 표시·근거 추적 / 숫자 카운트업·패럴랙스·자동 슬라이드·애니메이션 라이브러리는 넣지 않음 | `uv run pytest` **475개 통과**(랜딩 시험 11개 추가), `check_public_bundle.py` 위반 없음. 창 없는 Chrome 확인: 사다리 칩 5개 모두 의도대로, 스크롤 시 등장 32/32, 1440px·400px 가로 넘침 없음, `?motion=off`에서 숨겨진 구역 0·사다리 5칸 모두 표시, 콘솔 오류 0. 랜딩 캡처 `00_landing.png` 추가(7장). 캡처 안전장치(상단 '시연용 합성 수치' 확인)는 약화시키지 않고 랜딩 꼬리말에 같은 표시를 둬서 통과시킴 |
| 9/19 | **서비스 이름을 "콕콕"으로 변경 (복제본).** 화면(탭 제목·로고·꼬리말), 랜딩 글자, `app.py` 앱 이름, `README.md`·`docs/usage_guide.md`·`pyproject.toml` 설명, 저장소 밖 `CLAUDE.md`·`알고리즘.md`·`인수인계_ai.md`까지 24곳. 꾸러미·실행 명령 이름은 그대로(170여 곳, 겉이름과 패키지명이 다른 것은 흔한 구성) | `uv run pytest` **475개 통과**. 꼬리말에 이름을 넣자 자료 표시 문구가 끊겨 시험 2개가 실패 → 이름을 꼬리말 **맨 앞**으로 옮겨 해결. 캡처 7장 재생성(AI 포함). 화면 확인: 랜딩·1단계 모두 탭 제목·로고·꼬리말에 새 이름, 옛 이름 0곳. `최종기획서.md`·`개발업무_워크플로우.md`는 지시 없이 고치지 않는 문서라 **옛 이름이 남아 있음**(각 1곳) |
| 9/19 | 이름 변경에 이어 문서 정리: `최종기획서.md`·`개발업무_워크플로우.md` 제목의 옛 이름 교체(사용자 지시), **없어진 폴더를 가리키던 시안 경로 5곳** 정정 (`기획서 보완 서비스 랜딩페이지/` → `프론트_예시/`) — `2근거확인화면계획.md`, `checks.md` 1장, `web/README.md`, `style.css` 첫 줄 주석, `templates/README.md` | `uv run pytest` 475개 통과, `check_public_bundle.py` 위반 없음. **원본 `Service/`에도 같은 옛 경로 5곳이 남아 있다** — 복제본을 합칠 때 함께 정리된다. `인수인계.md`·`인수인계_ai_20260916_원본.md`는 9/16 과거 기록이라 옛 이름을 그대로 둔다 |
| 9/19 | 사용자 확인 2건 반영: ① **작업 화면의 서비스 이름을 누르면 랜딩으로 간다**(전에는 1단계로 갔다 — `base.html` 로고 주소). ② 같은 시도 안에서 **시군구를 바꿔도 화면이 안 바뀐 것처럼 보이던 문제** — 자료가 없어 시도 자료로 넓히는 정상 동작이지만 범위 칩과 수치가 같아 구분이 안 됐다. 2단계 맨 위에 **"고른 지역 ○○○"** 줄을 추가해 무엇을 골랐는지 화면에 적는다 | `uv run pytest` **477개 통과**(시험 2개 추가, 회귀 시험 1개 갱신). 창 없는 Chrome 확인: 2단계에서 로고를 누르면 랜딩(`/`)으로 이동, 춘천시=60원(시군구 자료)·강릉시/동해시=110원(강원 자료)이며 "고른 지역" 줄이 각각 다르게 표시. 콘솔 오류 0. 캡처 7장 재생성 |
| 9/19 | **시연용 합성 자료를 시군구 전체로 확장 (복제본).** 입력 화면의 시군구 269곳 중 **268곳**에 레코드를 넣어 어느 지역을 골라도 그 지역 자료가 나오게 했다(강원 태백시 한 곳만 일부러 비워 사다리를 보인다). 수치는 행정표준코드에서 만들어 내는 가상 값이라 난수가 없고 같은 코드면 항상 같다. 예시 기획이 쓰는 **강릉시는 손으로 설계**(반대 2구간·같음 3구간). `evidence_id`는 근거 칩에 그대로 보이므로 코드 대신 이름을 쓴다(`DEMO-R07-SIGUNGU-강원-강릉시`). 랜딩 사다리 예시도 강릉시(자료 있음)·태백시(자료 없음)로 교체 | `uv run pytest` **479개 통과**(시험 2개 추가, 16개 갱신). 파일 71KB → **957KB**, 시험 시간 5.5초 → **18초**(자료가 커져 검사량이 늘어난 것). 화면 확인: 강릉시 130·춘천시 60·동해시 352·속초시 36으로 **시군구마다 다른 자료**, 태백시는 강원 자료(110)로 넓히고 이유 표시. 10자리 코드 화면 노출 0, 콘솔 오류 0. 랜딩 사다리 5칸·등장 32/32·`?motion=off` 정상. 캡처 7장 재생성 |
| 9/19 | 랜딩 등장 효과를 **화면에 들어올 때마다 재생**되도록 고침(사용자 확인). 처음에는 산만해 보이지 않게 **한 번만** 보이고 관찰을 끊었는데(`unobserve`), 위로 올렸다 다시 내리면 아무것도 안 나타났다. 화면 밖으로 나가면 표시를 떼고 다시 들어오면 붙이는 방식으로 바꾸고, 가장자리에 걸친 요소가 깜박이지 않게 위아래 여백을 뒀다 | 창 없는 Chrome 확인: 아래로 내림 6/32 → 위로 올림 12/32 → **다시 내림 6/32**로 재생 확인. 화면 중간에서 40px만 움직였을 때 개수 변화 없음(깜박임 없음). `?motion=off`는 그대로 전부 표시(캡처 안전). `uv run pytest` 479개 통과, 콘솔 오류 0 |
| 9/19 | **4단계 저장이 실패해도 아무 말이 없던 문제**(사용자 확인). 필수 칸(수집할 자료)을 비운 채 저장하면 422로 다시 그리는데, 화면이 **저장된 선택만 보고** 그려서 방금 고른 결정·대안이 사라졌다. 그러면 `choices.js`가 실행 조건 칸을 접고, **그 안에 있던 오류 문구까지 감춰져** "저장을 눌러도 다음으로 안 넘어간다"로 보였다. ① 실패 시 폼 원문을 그대로 돌려줘 고른 값을 되살리고 ② 오류 자리로 화면을 옮긴다(1단계와 같은 처리) | `uv run pytest` **480개 통과**(시험 1개 추가). 창 없는 Chrome 확인: 실패 전 `결정=없음 대안=없음 / 실행칸 숨김 / 오류 안 보임` → 고친 뒤 `결정=adopt 대안=A / 실행칸 펼침 / 오류 523px 보임`, 스크롤이 198px로 이동. 수집 항목까지 채우면 맨 아래에 **[보완 기획안 만들기 →]**가 나타난다 |
| 9/19 | **3단계 화면 정리 5건**(사용자 확인): ① AI 영역 맨 위의 "확인할 점을 제시하며 결정하지 않습니다…" 안내를 **맨 아래 작은 글씨**로 옮김 ② 모델 고르는 칸 아래의 **모델별 설명 제거**(고르는 데 도움이 안 되고 안내만 길어짐) ③ "모델에 따라 표현이 다를 수 있습니다…"도 맨 아래로 합침 ④ AI 의견의 **근거 표시를 문장 위로** 올리고 하늘색 적용 (무엇에 대한 의견인지 먼저 보이게) ⑤ 왼쪽 검토 질문을 **항목마다 상자로 나누지 않고 한 상자 안에 문서처럼** 이어 붙임 (구분은 가로선과 왼쪽 선으로) | `uv run pytest` **481개 통과**(시험 1개 추가, 모델 설명 시험 2개를 "안 나온다"로 뒤집음). 창 없는 Chrome + 로컬 모델로 확인: 왼쪽 상자 4개 → **2개**(검토 문서 1 + AI 1), 검토 항목 2건, 근거 표시가 문장 위(하늘색 `#e3f2fd`), 안내가 맨 아래, 모델 설명 없음, 콘솔 오류 0. 캡처 7장 재생성 |
| 9/20 | **고도화 묶음 0(계약 정리) 완료.** 지역·사업 유형 확장을 위한 복제본 `Service_고도화_20260920`을 만들고 시작 시점 상태를 커밋 `0-0`으로 고정. `7지역확장계획.md` 작성: 확정 결정 10건, 용어표, 근거 파일 2.1 계약(2.0 레코드 유지 + `profiles`·`thresholds` 추가, 준비 상태 4종, 파일 검사 규칙), 지역 대응표 서식과 따로 다룰 자리 6종, 질문 이름표·재확인 기준을 질문 단위로 내리는 방침, **새 질문 6종의 대안 16개가 보완 기획안 어느 장에 어떤 방식으로 들어가는지 배치표**. 코드는 바꾸지 않았다 | 복제본에서 `uv sync` 후 `uv run pytest -q` **481개 통과**(원 사본과 같음), `check_public_bundle.py` 추적 파일 213개 **위반 없음**, 복사 파일 수 214개로 원 사본과 일치. 작업 폴더·자료 쪽 지역표(255줄)와 서비스 지역 목록(269개) 대조는 개수만 확인(이름 254/255, 두 코드 칸 함께 254/255) |
| 9/20 | **고도화 묶음 1(지역 대응표) 완료.** 분석 쪽 지역표를 서비스 지역 코드(10자리)에 잇는 대응표 생성기(`scripts/build_region_mapping.py`)와 시험 11개를 더했다. 대응표는 어느 지역이 제공 자료에 들어 있는지가 드러나므로 **`private/`에 두고 공개 저장소에 올리지 않는다**(화면에는 개수·상태만 찍는다). 서비스가 도는 코드는 바꾸지 않았다 | `uv run pytest -q` **492개 통과**(481 + 11), `check_public_bundle.py` 위반 없음. 대응표 결과는 개수만 확인: 연결 252 · 구성(합산·분할) 3 · 미연결 15 · 자료에만 있는 줄 0, **합계가 두 번 더해질 위험 없음**(구를 둔 시가 모두 미연결). `git status`에 `private/` 파일이 보이지 않음 |
| 9/20 | **고도화 묶음 2-1: 질문 이름표 분리.** 한 규칙이 질문을 둘 이상 낼 수 있게 바탕을 바꿨다. ① 이름표를 만드는 함수(`question_key_of`) — 질문 하나면 규칙 번호 그대로, 둘 이상이면 `규칙ID-이름` ② 규칙 파일에 질문별 관련 입력 항목을 적는 칸(`questions`)과 `related_fields_for()` ③ 선택이 관련 항목을 기억하도록 `Choice.related_fields` 추가, 재확인이 그 값을 먼저 봄. 지금 규칙은 모두 질문 하나씩이라 화면 동작은 달라지지 않는다 | `uv run pytest -q` **503개 통과**(492 + 11). 기존 492개가 그대로 통과해 동작이 바뀌지 않았음을 확인. 새 시험 10개로 ① 두 질문의 선택이 서로 덮어쓰지 않음 ② 한쪽 항목만 바뀌면 그 선택만 재확인 ③ 함께 걸린 항목이면 둘 다 재확인 ④ 관련 항목을 모르는 옛 선택은 규칙 값으로 판단 ⑤ 규칙에 없는 질문 이름은 바로 알림을 고정. `check_public_bundle.py` 위반 없음 |
| 9/20 | **고도화 묶음 2-2: 1단계 입력 확장.** ① **사업 유형**(쿠폰·지역화폐 / 외국인·관광 / 축제·행사 / 연령 대상) 필수 입력 추가 — 고르지 않으면 서비스가 임의로 분류하지 않고 묻는다 ② **성과지표 선택지**를 묶음 세 개로 넓힘(카드로 산출 가능 5개 / 카드에 없는 방문객·참여자·고객 수 3개 / 그 밖 2개) ③ **목표 방문객 수** 선택 입력 — 빈칸(적지 않음)과 0명을 구분하고, 축제·행사에서 빈칸이면 '추가 확정 필요'로 남긴다. 아직 질문을 켜고 끄는 데 쓰지 않는다. 사용처 업종·대상 연령은 고를 수 있는 목록이 제공 자료에서 와야 하므로 2.1 근거 파일 연결 뒤에 만든다 | `uv run pytest -q` **523개 통과**(503 + 20), `check_public_bundle.py` 위반 없음. 브라우저(합성 자료, 폭 1440px): 1단계에 사업 유형 4개·지표 묶음 3개·목표 방문객 칸이 보이고, [예시 기획 채우기]를 누르면 사업 유형이 '외국인·관광'으로 채워지며 설명 줄이 바뀜. 콘솔 기록 없음, 가로 넘침 없음(문서 폭 1425 ≤ 창 1440). **폭 400px 창은 브라우저 최소 창 너비 때문에 만들지 못해, 입력 카드 폭을 400px로 줄여 안쪽 넘침이 없는지만 확인(398 = 398)** |
| 9/20 | **고도화 묶음 2-3: 근거 파일 2.1 읽기.** 2.0 레코드는 건드리지 않고 지역 프로필(`profiles`)과 운영 기준(`thresholds`) 묶음을 읽는 코드를 더했다(`evidence/profile_schema.py` 새 파일, `loader.py`가 `LoadResult.profiles`·`thresholds`·`profile(지역키)`로 넘김). 고장 났을 때의 태도를 나눴다 — 레코드가 틀리면 파일 전체 거부(지금과 같음), **프로필 한 곳이 틀리면 그 지역만 버리고 경고**(기존 검토는 그대로 동작), **형식 2.0 파일에 프로필이 들어 있으면 조용히 무시하지 않고 거부**(담당자가 넣은 프로필이 화면에서 '자료 없음'으로 사라지는 일을 막음). 경계 시험 파일 3개를 생성기에 추가. 아직 화면에는 쓰지 않는다 | `uv run pytest -q` **549개 통과**(523 + 26), `check_public_bundle.py` 위반 없음. 기존 합성 근거(2.0) 파일은 그대로 읽히고 프로필만 비어 있음을 확인. 경고 문구에 자료의 금액·비중 값이 들어가지 않는지 시험으로 고정 |
| 9/20 | **고도화 묶음 2-4: 시연용 2.1 합성 파일과 2단계 지역 소비 프로필 카드.** 시연 파일을 2.1로 다시 만들어 지역 285곳의 프로필(업종 11 · 연령 6 · 월별 흐름 6 · 외국인/미상 비중 · 순위 · 준비 상태 · 한계)과 운영 기준을 넣었다. 2단계 맨 위에 프로필 카드를 붙였다. **고른 지역 자료만 쓰고 범위를 넓히지 않으며**, 자료가 없거나 부족하면 이유 한 줄만 보여 준다. 순위에는 '자료가 있는 N곳 중'을 함께 적는다. 시연 업종 이름은 시연용(시연 업종 A~K)이며 실제 이름은 데이터 담당이 2.1 파일에 넣는다. 유사 지역·외부 자료는 미연결로 뒀다 | `uv run pytest -q` **563개 통과**(549 + 14), `check_public_bundle.py` 위반 없음. 브라우저(합성 자료, 1440px): 2단계에 프로필 카드가 뜨고 업종 11줄·연령 6줄·월별 6칸·순위 2줄·한계 2줄 표시, '자료 없음' 달이 회색으로 구분됨, 기존 차트 2개 그대로, **콘솔 기록 없음**, 가로 넘침 없음(문서 1425 ≤ 창 1440). 값 칸이 가로 스크롤 밖으로 밀리던 것을 발견해 좁은 표의 최소 폭을 풀고 제목을 눈에 보이게 고쳤다 |
| 9/20 | **고도화 묶음 2-5(묶음 2 마무리): 업종·연령 입력과 성과지표 점검.** ① 사용처 업종·대상 연령 입력을 1단계에 추가 — 고를 수 있는 목록을 **근거 파일의 전국 프로필에서 읽고**, 목록에 없는 코드는 버리며, 근거 파일에 프로필이 없으면 입력칸 자체를 두지 않는다(서비스가 목록을 지어내지 않음) ② **성과지표와 자료 범위 확인(R10)** 규칙 추가 — 사람 수 지표(방문객·참여자·고객)를 고르면 어떤 자료로 확인할지 묻고, 대안 A는 수집 계획을 받고 B는 결제금액·건수로 바꾼다. 쿠폰 정산 실적과 다른 자료라 공유 입력 묶음을 `performance_data`로 나눴다 | `uv run pytest -q` **589개 통과**(563 + 26), `check_public_bundle.py` 위반 없음. 브라우저(합성 자료, 1440px)로 **축제 시나리오 전체 흐름 확인**: 1단계에 업종 11칩·연령 6칩·지표 10칩·유형 4칩 표시 → 2단계 프로필 카드 → 3단계 '성과지표와 자료 범위 확인' 질문에 '방문객 수'가 적힘 → 4단계에서 대안 A와 수집 계획 입력 → 5단계 보완 기획안에 '축제 입장 집계'·'문화관광과'가 들어감(변경 5건·추가 확정 필요 4건). 콘솔 기록 없음, 가로 넘침 없음, 화면에 규칙 번호 없음 |
| 9/20 | **고도화 묶음 3-1: 사업 유형별로 질문 켜기.** 고른 사업 유형에 맞는 질문만 실행한다(공통: 기간·운영 조건·성과지표 / 쿠폰: 목표와 사용처 / 외국인·관광·축제: 금액·비중, 대상과 자료). **유형을 고르지 않은 기획은 아무 질문도 숨기지 않는다** — 서비스가 마음대로 분류하지 않는다. 켜지지 않은 질문은 3단계에 **'사업 유형이 달라 확인하지 않음'**으로 남겨 '물을 것이 없음'·'이번 범위에서 만들지 않음'과 구분한다. 유형 목록은 `business_packs.json`에 번호만 적고, 규칙 파일에 없는 번호가 있으면 서버가 시작할 때 멈춘다 | `uv run pytest -q` **605개 통과**(589 + 16), `check_public_bundle.py` 위반 없음. 브라우저(합성, 1440px): 쿠폰 기획으로 3단계를 열면 질문 1건, '확인했으나 해당 없음'에 성과지표·기간, '사업 유형이 달라 확인하지 않음'에 금액·비중, 대상과 자료가 들어간다. 콘솔 기록 없음, 가로 넘침 없음, 화면에 규칙 번호 없음 |
| 9/20 | **고도화 묶음 3-2: 사용처 업종 확인(R02) — 첫 값 기반 질문.** 고른 사용처 업종이 그 지역 카드 결제에서 ① 결제가 잡히지 않았거나 ② 전국에서의 자리가 운영 기준 이하면 묻는다. '향후 기능'이던 규칙을 실제로 켰다. **결제 0원과 자료 없음을 구분**하고, 결제가 잡히지 않은 것을 상점이 없다는 뜻으로 쓰지 않는다. **고른 지역 자료만 쓰고 없으면 보류**한다(넓히지 않음). **기준값은 코드가 아니라 근거 파일에서 받고**, 없으면 '낮은 편'을 서비스가 정하지 않는다. 순위는 몇 곳 중인지 아는 항목만 쓴다. 시연 파일의 업종·연령 항목에 전국에서의 자리를 넣고, 강릉시 첫 업종을 결제 0 사례로 뒀다 | `uv run pytest -q` **620개 통과**(605 + 15), `check_public_bundle.py` 위반 없음. 브라우저(합성, 1440px): 쿠폰 기획에서 결제가 없는 업종을 고르면 3단계에 '고른 사용처 업종 가운데 이 지역 카드 결제가 잡히지 않은 업종이 있습니다'가 뜬다. 콘솔 기록 없음, 가로 넘침 없음, 화면에 규칙 번호 없음 |
| 9/20 | **고도화 묶음 3-3: 시기 확인(R04 시기)과 외국인 대상 기반 확인(R12).** ① 기간 확인 규칙이 **질문 두 개**(기간·시기)를 내도록 나눴다 — 이름표와 관련 입력 항목뿐 아니라 **보완 방법(대안)도 질문마다 다르게** 둘 수 있게 했다 ② 시기: 사업 기간에 든 달 가운데 그 지역에서 결제가 평소보다 컸던 달이 있으면 묻는다. **자료에 없는 달(제공 기간 밖)은 판단하지 않고**, 결제 규모가 작은 지역은 흔들림이 커서 기준을 따로 쓴다(근거 파일이 알려 줄 때만) ③ 외국인 대상 기반: 대상에 외국인이 있고 그 지역 외국인 결제 비중의 전국 자리가 기준 이하면 묻는다. **'비중이 낮다'를 '방문 가능성이 낮다'로 쓰지 않는다.** '국내 방문객'에는 걸리지 않게 했다(축제·행사 유형에서만 켜짐) | `uv run pytest -q` **640개 통과**(620 + 20), `check_public_bundle.py` 위반 없음. 브라우저(합성, 1440px): 축제 기획(5월 사흘, 방한 관광객)에서 3단계 질문 6건, '결제가 평소보다 컸던 달이 있습니다: 5월', '외국인 비중은 낮은 편입니다 (자료가 있는 286곳 중 …)'가 뜬다. 콘솔 기록 없음, 가로 넘침 없음, 화면에 규칙 번호 없음 |
| 9/20 | **고도화 묶음 3-4: 대상과 고객 구성(R08).** ① 근거 파일 2.1의 `industry_age` 형식을 확정하고 업종별 연령 구성·분모·전국에서의 자리·유효 지역 수를 읽는다 ② 묶음이 틀리면 지역 프로필 전체가 아니라 그 지역 R08만 끈다 ③ 고른 업종×대상 연령 조합 가운데 순위와 분모가 있는 항목만 근거 파일 운영 기준과 비교한다. 일부 조합을 확인하지 못했고 낮은 조합도 없으면 '해당 없음'으로 넘기지 않고 보류한다 ④ 쿠폰·지역화폐와 연령 대상 사업에서 R08을 켜고, 대안 A~C를 4·7장 문장에 연결했다 | `uv run pytest -q` **653개 통과**(640 + 13), `check_public_bundle.py` 추적 파일 236개 위반 없음. 합성 자료 생성기가 모든 시연 프로필에 업종 11×연령 6 구성을 만들고 재로딩 경고 없음. 3단계 경로 시험에서 질문 노출·규칙 번호 비노출 확인. **브라우저 실물 확인은 현재 세션에 연결 가능한 브라우저가 없어 미확인** |
| 9/20 | **고도화 묶음 4-1: 외부 인구와 규모·수용 여건(R11).** ① 근거 파일 2.1의 `external` 형식을 확정해 주민등록인구·등록외국인 수를 관측일·출처와 함께 읽는다 ② 외부 묶음이 틀리면 그 지역의 외부 기능만 끄고 카드 자료 질문은 계속 사용한다 ③ 축제·행사에서 목표 방문객 수가 주민등록인구와 같거나 클 때 숙박·주차·교통 확인 자료를 묻는다. 질문에 주민등록인구와 방문객이 같은 집단이 아니며 규모 참고라는 한계를 함께 적는다 ④ 인구로 카드 결제값을 나눈 지표는 만들지 않는다. 유사 지역은 업종 목록 확정 전이라 계속 미연결로 둔다 | `uv run pytest -q` **665개 통과**(653 + 12), `check_public_bundle.py` 추적 파일 238개 위반 없음. 합성 자료 생성기가 모든 시연 프로필에 관측일·출처가 붙은 외부 인구를 만들고 재로딩 경고 없음. 3단계 경로 시험에서 R11 질문 노출·규칙 번호 비노출 확인. **브라우저 실물 확인은 현재 세션에 연결 가능한 브라우저가 없어 미확인** |
| 9/21 | **고도화 묶음 5: 선택·문서·AI 경계 통합(유사 지역 제외).** ① 선택 당시 자료 버전·운영 기준 버전을 저장하고, 자료 버전이 달라지면 값 기반 질문만, 운영 기준 버전이 달라지면 기준값을 쓰는 질문만 재확인한다 ② Markdown 변경 표기를 내부 규칙 번호에서 공개 순번(`변경 001`)으로 바꾸고 본문·화면·별첨 1을 같은 이름표로 연결했다 ③ 별첨 2와 5단계 근거 추적에 고른 지역 프로필 요약을 항상 넣되, 미연결 지역은 다른 지역 값으로 넓히지 않는다. 외부 인구는 관측일·출처와 함께 규모 참고로만 쓰고 1인당 값을 만들지 않는다 ④ AI 출력 검사가 프로필·순위·인구·거리 수치를 한글로 쓴 경우도 버리도록 넓혔다. 이웃 모델 버전과 유사 지역 표시는 업종 목록 확정 전이라 적용하지 않았다 | `uv run pytest -q` **673개 통과**(665 + 8), `check_public_bundle.py` 추적 파일 238개 위반 없음. 자료 버전 변경 경로, 프로필 미연결 비대체, Markdown 규칙 번호 비노출, 새 수치의 LLM 요청문 비포함을 자동 확인. **브라우저 실물 확인은 현재 세션에 연결 가능한 브라우저가 없어 미확인** |
| 9/21 | **고도화 묶음 6-1: 통합 흐름과 사용 안내.** ① 기존 축제 시나리오가 5단계 화면·전체 문서·저장용 Markdown까지 규칙 번호 없이 이어지는지 넓혔다 ② 쿠폰 시나리오를 새로 추가해 사용처 업종과 대상·고객 구성 질문을 함께 선택하고 프로필 별첨이 있는 Markdown까지 통과시켰다 ③ `docs/usage_guide.md`를 사업 유형·프로필·새 질문·자료 버전 재확인·공개 변경 순번 기준으로 갱신했다 ④ 기존 캡처가 9/19 촬영본임을 명시했다 | `uv run pytest -q` **674개 통과**, `check_public_bundle.py` 추적 파일 239개 위반 없음. **400px 실물 화면·새 캡처는 현재 세션에 연결 가능한 브라우저가 없어 미확인**, 실제 2.1 파일 수치 대조는 자료 수령 뒤 사람이 확인 |
| 9/21 | **분석 전달본 2.1 서비스 적용(묶음 7-1~7-7).** 같은 2.1 버전이지만 내부 구조가 다른 새 전달본을 `delivery_v21.py`에서 판별·정규화한다. 전국 R02 선택 처리, 업종·연령·업종×연령·시기·외국인 유형·외부 인구·월별 등록외국인·유사 지역 보존, 평면 임계값 변환과 지문 버전, R04 지역별 기준, R11 자료 기준 비율, R08 참고 배지를 연결했다. 실자료 지역은 `private/region_mapping.json`의 linked/composed만 10자리 코드로 잇고 대응표가 없으면 전국만 쓴다. 1인당 파생값은 가져오지 않는다. 공개 검사에 pickle 계열 차단도 추가했다 | 전체 `pytest` **679개 통과**. 공개 전달본 **13 레코드·13 프로필**, 비공개 전달본 **256 레코드·256 프로필** 로드. 비공개 시군구 255개가 대응표로 연결되고 미연결 서비스 지역 15개는 그대로 유지됨을 개수로 확인. 비공개본 경고는 미연결 서비스 지역 요약 1건뿐이며 실제 수치는 출력하지 않았다. 실제 JSON·대응표는 git 변경분에 없음 |
| 9/21 | **공개 배포 AI를 Google AI Studio API로 전환.** 로컬 제공자와 관련 코드를 제거하고 `google_ai` 제공자만 구현했다. 모델 순서는 Gemini 3.8 Flash(기본) → 3.6 Flash → 2.5 Pro → Gemma 4 31B IT이며 3.5 Flash-Lite는 제외했다. 2.5 Pro는 `thinkingBudget=128`, Gemini 3은 `thinkingLevel=low`, Gemma 4는 `minimal`, 출력 한도는 사고 토큰 포함 2,048로 정했다. 화면 자동 호출을 없애고 생성 버튼의 POST 요청으로 바꿨다. API 키는 `GEMINI_API_KEY` 서버 환경변수와 요청 헤더로만 쓴다 | Google 전환 관련 시험 73개와 전체 `pytest` **685개 통과**. 공개 저장소 추적 파일 249개 검사 위반 없음, 실제 Google 호출은 API 키를 작업 환경에 넣지 않아 실행하지 않음 |
| 9/21 | **Vercel 배포 구조 완료.** `policy_signal_map.app:app` 진입점, 서울 리전·60초 함수 설정, `/health`, Vercel 보안 쿠키를 추가했다. 작업 상태는 Upstash Redis에 버전 있는 JSON으로 24시간 저장하며 Lua CAS로 동시 덮어쓰기를 막는다. 로컬은 메모리를 유지하고 Vercel에서 Redis 설정이 없으면 조용히 메모리로 바꾸지 않고 503을 낸다. 랜딩 캡처를 설치 패키지 정적 파일에 포함했다 | 전체 `pytest` **700개 통과**, `uv lock --check`·`uv build`·설치 패키지 앱/정적 파일/health smoke 통과. 공개 추적 파일 **256개 위반 없음**, 실제 키 패턴 없음. 현재 PC에는 Node/Vercel CLI가 없어 `vercel build`와 Preview 실배포는 Vercel 프로젝트 연결·Secret 등록 뒤 확인 |

## 6. 남은 작업·막힌 결정

- [x] ~~보완 기획안 Markdown 원문의 변경 표시에 규칙 번호가 남아 있음~~ → 9/21 공개 순번 `변경 001`로 교체하고 본문·화면·별첨 1을 같은 이름표로 연결했다. 내부 `rule_id`는 기록 구조에만 남는다.

- [x] ~~공개 배포 여부·호스팅 방식 결정~~ → 9/21 Vercel FastAPI + Upstash Redis로 결정하고 배포 코드를 반영. 남은 것은 Vercel 프로젝트 연결·Secret 등록·Preview 실배포 확인
- [x] ~~제출 화면 캡처 방법 결정~~ → 9/17 로컬 실행 + 합성 자료로 결정(사용자), 캡처 6장 완료(`docs/screenshots/`). 외부 편집기 화면 1장은 사용자 캡처 대기
- [ ] 1장 미정 항목을 데이터 담당과 확정
- [ ] 2장 연결 확인 시간 확정
- [ ] 3장 전달 형식 공동 확인 — 9/17 데이터 담당 확정 기록. 확정본에서 `data_kind`·`month` 표기 반영 확인 남음
- [x] ~~A→C 확장과 LLM 전달 자료 원칙(①+②)을 데이터 담당과 합의하고, 합의 시 최종기획서 8장·11장 수정~~ → 9/16 로컬 LLM만 쓰기로 해 외부 전송이 없으므로 8장 수정 불필요. 남은 것은 아래 한 줄
- [ ] 최종기획서 11장 "조건부 확장"에 AI 참고 의견(로컬 LLM) 한 줄을 넣을지 결정
- [x] ~~실제 자료로 AI 참고 의견을 쓸지~~ → 9/17 로컬 LLM은 허용으로 결정(1장). 6-1 대조 중에는 끈다
- [ ] `docs/review_rules.md` 검수 (조건·문구·문서 문장·AI용 상황 설명) — **R01·R03·R04는 9/17 데이터 담당 검수 완료, 9/18 반영.** R07·R05 검수 대기(의견 오면 문구 수정 후 3·4단계 캡처 재생성)
- [ ] **추가로 확보할 자료** (데이터 담당 결정 문서 9/15 5장, 9/17 기록)
  - [ ] 참여 점포 식별정보와 카드 업종 연결표 — 9/21 전 확보 어려움(개발 판단). 대안 D 정밀 분석 요청서 초안의 확인 항목, 제출 PDF 향후 과제로 표시
  - [ ] 외국인 전체와 관광객을 구분하는 자료 — 제공 자료에 없음. R02 보류 유지 근거, R03 질문이 다루는 한계. **9/18: 이 한계를 R02 "검토하지 않음" 화면 문구에도 적음**
  - [ ] 운영 사례 원문 검수(출처·발표일·대상·사용처·모집·정산) — 제출 PDF "외부자료 활용" 근거 후보. 검수 전에는 확정 근거로 쓰지 않음. 담당·기한 미정
  - [ ] 원본 보고서 확보: `제공데이터_기반_기획안_재검토보고서.md`, 기존 유사성 검토보고서 — 작업 폴더에 없음. 8-1 대안 비교표와 제출 PDF 분석 예시 확인용
