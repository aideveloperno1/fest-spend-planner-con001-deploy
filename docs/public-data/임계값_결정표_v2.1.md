# 임계값 결정표 (근거 파일 real-002)

임계값은 규칙이 질문을 띄우는 지점이다. 통계 검정의 유의수준이 아니라 운영 기준이다. 아래 값은 BC카드 제공 자료(2026-01~06, 255개 시군구)로 세 가지를 점검해 정했다. 시군구별 값은 없고 전국 분포와 집계만 실었다.

- 재현성: 1~3월과 4~6월로 나눠 각각 뽑았을 때 같은 지역이 뽑히는가(겹침 정도, 0~1). 0.8 안팎 이상이면 안정적으로 본다.
- 발동 빈도: 규칙 하나가 전체 시군구의 20~25%를 넘게 걸리지 않는가.
- 절대 수준: 상대 순위만이 아니라 평소 변동보다 실제로 큰 값인가.

## 최종 값과 근거

| 규칙 | 값 | 데이터 점검 결과 | 판단 |
| --- | --- | --- | --- |
| R02·R08 비중 낮음 | 업종(업종×연령) 비중 하위 20% | 업종별 반기 겹침이 하위 10%에서 0.75, 20%에서 0.81(최저 업종 0.70), 30%에서 0.82다. 업종×연령은 20%에서 0.75다 | 20% 유지. 20%부터 재현성이 0.8 수준에 올라서고 발동은 지역의 20%로 고정된다. R08은 0.75라 참고 무게로 표시한다 |
| 소액 지역 표시 | 6개월 총결제 하위 20% (약 126억 원 미만, 51곳) | 결제 규모 하위 구간에서 월별 변동이 크다 | 20% 유지. 화면 문구용이며 R04의 규모 구간과 목적이 달라 따로 둔다 |
| R03 유형 | 외국인 결제 규모 상위 50%만 대상, 편의점+슈퍼·일반한식 비중 각 상위 30% | 컷오프 20% → 복합형 76곳, 30% → 50곳, 40% → 30곳. 반기 유형 일치율은 0.94, 0.91, 0.95다 | 30% 유지. 세 유형이 26~50곳으로 균형(생활 39·한식 39·복합 50)이고 일치율이 0.91이다 |
| R04 시기 | 규모 하위 10%는 1.30, 10~30%는 1.15, 나머지 1.10 | 지수의 평소 흔들림(견고 표준편차)이 하위 10% 0.127, 10~20% 0.069, 20~30% 0.058, 30~50% 0.036, 50~100% 0.035다 | 기준 = 1 + 흔들림×2.5를 반올림한 값이다(1.32, 1.17·1.15, 1.09). 1개월 이상 발동 30곳(12%), 구간별 23%, 24%, 7% |
| R12 외국인 비중 낮음 | 하위 20% (약 3.9% 이하, 51곳) | 반기 겹침이 10%에서 0.73, 15%에서 0.81, 20%에서 0.79, 25%에서 0.83, 30%에서 0.90이다 | 20% 유지. 0.79는 0.8과 같은 수준이고 다른 규칙과 발동 비율을 맞춘다 |
| R11 규모 | 목표 방문객 ÷ 주민등록 인구 1.0 이상 | 인구 중앙값 17만 명. 방문객 3만 명이면 22곳(9%), 5만 명이면 59곳(23%), 10만 명이면 94곳(37%)이 1.0 이상이다. 0.5로 낮추면 5만 명에서 94곳(37%)이다 | 1.0 유지. 지역 행사에 흔한 3만~5만 명 규모에서 발동 비율이 다른 규칙과 비슷하다. 방문객은 사용자가 입력하는 값이라 실제 입력이 쌓이면 다시 점검한다 |

## 이번에 바뀐 것

R04만 바뀌었다. 이전(real-001)은 하위 20%가 1.25, 나머지가 1.10이었다. 규모 구간별 흔들림을 재 보니 하위 10%(0.127)와 10~30%(0.06~0.07)의 차이가 커서 두 구간을 따로 세웠다. 발동 지역은 32곳에서 30곳으로 줄었다. 5월에 22곳이 몰리는데, 전국 5월 흐름을 뺀 지수에서도 5월이 큰 지역이 많다는 뜻이며 6개월 자료로는 계절성으로 단정하지 않는다.

## 아직 남은 것

- R08은 다른 규칙보다 재현성이 낮다(반기 겹침 0.75, 업종×연령 쌍에 따라 0.57~0.89).
- R11은 실제 입력 방문객 분포가 없어 시군구 인구로만 점검했다.
- 사용자 검증(담당자 반응)은 아직 하지 않았다. "질문이 너무 많다·적다"가 나오면 값을 조정하고 근거 파일을 다시 만든다.
- 인구감소지역 목록은 2021년 지정 기준이라 2026년 하반기 재지정 후 갱신이 필요하다.

## 값을 바꾸는 방법

`09_make_evidence.py` 맨 위 `THRESHOLDS`에서 값만 바꾸고 `python 09_make_evidence.py real`(비공개), `python 09_make_evidence.py synthetic`을 다시 실행한다. 파일의 `dataset_version`도 올린다. 실행 후 `10_verify_evidence.py`로 검증한다.
