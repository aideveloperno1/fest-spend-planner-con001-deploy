# 2. 근거 확인 화면 — 상세 구현 계획

문서 기준일: 2026-09-16
상위 문서: [작업진행.md](./작업진행.md) 2장
선행 작업: [1근거계산계층계획.md](./1근거계산계층계획.md) (완료)
기준 문서: [개발업무 워크플로우](../개발업무_워크플로우.md) 8-3·8-4·9-2·12장 · [최종기획서](../최종기획서.md) 3장·4-2장
화면 기준: `../프론트_예시/정책맵 워크스페이스.dc.html` 시안 S2
폴더 사양: [web/README.md](src/policy_signal_map/web/README.md) · [routes/README.md](src/policy_signal_map/web/routes/README.md) · [templates/steps/README.md](src/policy_signal_map/web/templates/steps/README.md) · [partials/README.md](src/policy_signal_map/web/templates/partials/README.md) · [static/js/README.md](src/policy_signal_map/web/static/js/README.md)

이 문서는 2번 작업을 **그대로 따라 구현할 수 있을 정도로** 데이터 형태, 표시 규칙, 문장 규칙, 화면 구성, 합성 파일 기준 기대 화면 값, 테스트 목록을 정리한다.

---

## 목차

1. [목표와 범위](#1-목표와-범위)
2. [확정한 결정](#2-확정한-결정)
3. [계획 전 확인 사항](#3-계획-전-확인-사항)
4. [진행 순서](#4-진행-순서)
5. [2-1 표시 형식과 근거 로드 공통](#5-2-1-표시-형식과-근거-로드-공통)
6. [2-2 화면용 데이터 조립](#6-2-2-화면용-데이터-조립)
7. [2-3 근거 확인 화면](#7-2-3-근거-확인-화면)
8. [2-4 상단 자료 표시](#8-2-4-상단-자료-표시)
9. [합성 파일 기준 기대 화면 값](#9-합성-파일-기준-기대-화면-값)
10. [테스트 전체 목록](#10-테스트-전체-목록)
11. [커밋과 기록](#11-커밋과-기록)
12. [완료 기준](#12-완료-기준)
13. [위험과 대응](#13-위험과-대응)
14. [뒤 단계와 연결되는 점](#14-뒤-단계와-연결되는-점)

---

## 1. 목표와 범위

### 목표

원안을 보관한 사용자가 2단계에서 **1~6월 전국 근거(차트·월별 표·인접 구간 전체)와 해석 한계**를 확인하고, 3단계 질문이 왜 나오는지 이해할 수 있게 한다.
계산은 1번 `evidence/` 결과만 사용하고, 화면 계층은 **표시만** 한다.

### 범위에 포함

- 표시 형식 필터 (금액·비중·변화·증감률·방향·상태·기간)
- 근거 파일을 앱에서 한 번 읽어 보관하고, 오류 시 오류 화면
- 실제 자료 + 클라우드 LLM 조합일 때 서버 시작 차단
- 화면용 데이터 조립 (레코드 선택, 금액 단위, 적용 상태, 요약 문장, 표·차트 데이터, 보류 사례)
- `/step/2` 화면, 화면 조각 3개, 차트 스크립트, CSS
- 모든 화면 상단의 자료 종류·버전 표시

### 범위에 포함하지 않음

| 항목 | 담당 단계 |
|---|---|
| 검토 질문·대안 문장 | 3번 `review/` |
| 입력 지역 행정코드와 근거 `region_key` 연결 | 2-3 전 결정 대기 (C-2). 결정 전에는 전국 참고로만 표시 |
| 근거 추적 패널 (기획안 변경 문장 → 근거) | 5-2 |
| 공개 배포 설정 | 7번 |

### 산출물

| 구분 | 파일 |
|---|---|
| 신규 코드 | `web/formatters.py`, `web/evidence_state.py`, `web/evidence_view.py`, `web/dependencies.py`, `web/routes/evidence.py` |
| 수정 코드 | `__init__.py`(시작 전 검사), `app.py`(라우터·필터 등록), `web/templating.py`, `web/routes/input.py`, `web/routes/steps.py` |
| 템플릿 | 신규 `error.html`, `steps/evidence.html`, `partials/evidence_badges.html`, `partials/month_table.html`, `partials/pair_table.html` / 수정 `base.html` |
| 정적 파일 | 신규 `static/js/charts.js` / 수정 `static/css/style.css` |
| 테스트 | 신규 `test_formatters.py`, `test_evidence_state.py`, `test_evidence_view.py` / 추가 `test_routes.py` |

---

## 2. 확정한 결정

| # | 결정 | 채택안 | 이유 |
|---|---|---|---|
| ① | 근거 파일 로드 실패 시 동작 | **앱은 켜두고 2단계부터 오류 화면(HTTP 503).** 1단계 입력은 계속 가능. 단 **실제 자료 + 클라우드 LLM 조합은 서버 시작 자체를 막는다** | 파일 형식 문제로 입력 화면까지 막을 필요는 없다. 보안 조합은 실수로라도 켜지면 안 된다 |
| ② | 금액 단위 | **레코드별 자동:** 레코드의 최대 전체 금액이 1억 원 이상이면 억원, 미만이면 원. 표·차트·기간 합산이 같은 단위를 쓴다 | 합성 파일(수백~수만 원)을 억원으로 바꾸면 모두 "0.00억원"이 된다. 실제 파일(수천억 원)은 억원으로 보인다 |
| ③ | 인접 구간 표시 | **표** (구간별로 증감률·비중 변화·비교 A·비교 B·보류 사유를 한 줄에). 시안의 칩 대신 | 최종기획서 4-2의 구간 표와 같은 형태이며, 비교 A와 B를 섞지 않고 나란히 보여줄 수 있다 |
| ④ | 보류 사례 예시 | **파일에 계산할 수 없는 월이 있는 다른 레코드가 있으면 "보류 사례 예시"로 접어서 표시.** 사용자 지역과 연결하지 않는다. 없으면 섹션을 숨긴다 | 시안의 "자료가 부족한 경우 보기"를 유지하되, 가상 시도 자료를 사용자 지역 진단처럼 보이지 않게 한다 |
| ⑤ | 차트 라이브러리 | **jsDelivr CDN Chart.js 4.5.1 고정** (`dist/chart.umd.min.js`). 불러오지 못하면 안내 문구 | 빌드 없이 쓰는 기존 구조 유지. 모든 수치는 표에도 있어 차트가 없어도 정보가 빠지지 않는다 |

### 검토 반영 (9/16)

계획 검토에서 발견한 문제를 본문에 반영했다. "실행 확인"은 코드로 직접 재현한 항목이다.

| 구분 | 문제 | 반영 |
|---|---|---|
| A-1 | `/step/{step}` 임시 라우터가 먼저 등록돼 있으면 `/step/2` 요청을 가로채 1단계로 보냄 (실행 확인) | 근거 라우터를 임시 라우터보다 먼저 등록, 테스트로 고정 (5-6, 10장) |
| A-2 | 셸의 `PSM_` 환경변수를 테스트가 그대로 읽음 → 실제 파일로 테스트, 실패 출력에 수치 노출 가능 (실행 확인) | `tests/conftest.py` 자동 준비: 환경변수 비움, 합성 상태 고정, 교체한 의존성 복구 (10장) |
| A-3 | 문맥에 `evidence`가 없으면 템플릿 오류로 페이지 전체 500 (실행 확인) | `templating.render()` 하나로 항상 `evidence`·`step` 주입, 모든 화면 칩 테스트 (5-6, 8장) |
| A-4 | 차트 데이터에 `Fraction`이 섞이면 JSON 변환 오류 500 (실행 확인) | 차트 데이터는 정수·소수·null로만 변환, 형식 테스트 (6장) |
| A-5 | 0이 아닌 작은 비중·증감률·억원 금액이 `0.00`으로 표시됨 (실행 확인) | 비중·증감률·억원 금액에도 "0.01 미만" 규칙 적용 (5-1) |
| A-6 | blocked인데 월별 표 표시, 보류 사례 예시가 다른 레코드의 적용 상태·실제 여부를 무시 | blocked면 수치 전부 숨김, 보류 사례는 합성 파일 + blocked 아닌 레코드만 (6장, 7-2) |
| B-1 | CDN 스크립트 무결성 확인 없음 | `integrity` sha384 + `crossorigin` (7-4) |
| B-2 | 오류 화면에 서버 전체 경로 노출 | 화면은 파일 이름만, 전체 경로는 서버 로그 (5-2, 5-4) |
| B-3 | 표시 형식이 `web/`에 있으면 5번 문서 생성에서 쓸 수 없음 | `policy_signal_map/formatting.py`(웹 의존 없음)에 작성 (5-1) |
| B-4 | 시작 차단을 문자열로 찾는 코드 예시와 필드 사용 설명이 모순 | `EvidenceState.blocked` 기준으로 정리 (5-5) |
| B-5 | `uvicorn` 직접 실행 시 시작 차단이 빠짐 | `llm/README.md`에 "근거 상태가 차단이면 LLM 호출 금지" 요구사항 추가 (11장) |
| C-1 | 테스트 증가 | 약 41 → 약 50개 (10장) |
| C-2 | 브라우저 도구로 CDN 차단 확인이 어려움 | 로컬에서 스크립트 주소를 임시로 틀리게 바꿔 확인 후 되돌림, 커밋하지 않음 (10장) |

---

## 3. 계획 전 확인 사항

| 확인 | 결과 | 계획 반영 |
|---|---|---|
| 테스트의 앱 실행 방식 | `tests/test_routes.py`가 `TestClient(app)`를 `with` 없이 사용 | FastAPI 시작 이벤트(lifespan)가 테스트에서 실행되지 않는다 → 근거 파일은 **처음 필요할 때 읽어 프로세스에 보관**하고, 테스트는 FastAPI 의존성 교체(`app.dependency_overrides`)로 사례 파일을 넣는다 |
| Chart.js 4.5.1 CDN 경로 | `https://cdn.jsdelivr.net/npm/chart.js@4.5.1/dist/chart.umd.min.js` 응답 200 (9/16 확인) | 이 경로로 고정 |
| 합성 파일 금액 규모 | 전국 레코드 최대 전체 금액 10,890원 | 결정 ② 자동 단위 → 원 단위로 표시 |
| 기존 상단 자료 표시 | `base.html`에 "시연용 합성 수치" 고정 문구 | 2-4에서 불러온 파일 기준으로 변경 |

---

## 4. 진행 순서

```
2-1 표시 형식·근거 로드 공통
  ↓
2-2 화면용 데이터 조립 (evidence_view.py)
  ↓
2-3 근거 확인 화면 (라우트·템플릿·조각·차트·CSS)
  ↓
2-4 상단 자료 표시 (base.html)
```

- 2-1이 먼저인 이유: 2-2의 요약 문장이 표시 형식 함수를 쓰고, 2-3·2-4가 근거 상태 의존성을 쓴다
- 2-4를 마지막에 두는 이유: 1단계 화면까지 모든 라우트가 근거 상태를 받도록 바꾸므로, 2단계 화면이 안정된 뒤 한 번에 바꾼다

---

## 5. 2-1 표시 형식과 근거 로드 공통

### 5-1. `policy_signal_map/formatting.py` (B-3)

웹 객체에 의존하지 않는 순수 함수 모듈로 패키지 루트에 둔다. `web/templating.py`는 이 함수들을 Jinja2 필터로 등록만 한다. 5번 `document/`가 Markdown 문서를 만들 때도 같은 함수를 import한다 (경계 규칙상 `document/`는 `web/`을 import할 수 없음).

#### 공통 규칙

- 입력은 `int`, `Fraction`, `None`. **반올림은 이 모듈에서만** 한다
- 반올림: `Fraction` → `Decimal(분자) / Decimal(분모)`(정밀도 50자리) → `quantize(Decimal("0.01"), ROUND_HALF_UP)`
  - float를 거치지 않으므로 경계에 걸린 값도 사사오입이 정확하다. 예: `2.675`는 float 반올림(`round(2.675, 2)`)이면 `2.67`, 이 방식이면 `2.68` (9/16 실행 확인)
- 부호: 양수 `+`, 음수 ASCII `-`. 0은 부호 없음
- 천 단위 쉼표: 정수부에만
- **None은 어떤 필터에서도 `0`으로 나오지 않는다**
- **0이 아닌 값이 반올림 후 0이 되면 `0.00`으로 쓰지 않고 "0.01{단위} 미만"으로 쓴다** (A-5). 적용: 비중 `0.01% 미만`, 증감률 `0.01% 미만 (증가)`, 비중 변화 `0.01%p 미만 (증가)`, 억원 금액 `0.01억원 미만`. 정확히 0이면 `0.00%`·`0.00%p`·`0원`
- 색·아이콘을 정하지 않는다 (문자열만 반환)

#### 필터 목록

| 함수 (Jinja2 필터 이름) | 입력 | 출력 예 | 규칙 근거 |
|---|---|---|---|
| `amount_unit(max_total: int) -> "억원" \| "원"` | 레코드 최대 전체 금액 | `100_000_000` 이상 → `"억원"` | 결정 ② |
| `amount(value, unit)` | 원 정수 | 억원: `210618000000` → `2,106.18억원`, `400000` → `0.01억원 미만` / 원: `820` → `820원` / None → `자료 없음` | 8-1 (억원 변환은 화면) |
| `pct(value)` | Fraction % | `Fraction(8404255319,10**9)` → `8.40%` / `Fraction(3,1000)` → `0.01% 미만` / None → `계산 불가` | 8-3 소수 둘째 자리 |
| `pp_change(value)` | Fraction %p | `+0.20%p` / `-0.05%p` / 0 → `0.00%p` / 0이 아니고 절대값 < 0.01 → `0.01%p 미만 (증가)` 또는 `(감소)` / None → `계산 불가` | 8-3 표시 정밀도 |
| `growth(value)` | Fraction % | `+8.86%` / `-6.00%` / 0이 아니고 절대값 < 0.01 → `0.01% 미만 (증가)` / None → `계산 불가` | 8-2 (이전 F=0) |
| `direction(value)` | up/down/flat/None | `증가` / `감소` / `변화 없음` / `계산 불가` | 8-2 |
| `comparison(value, skipped)` | opposite True/False/None | `방향 다름` / `방향 같음` / `판단 불가` / skipped면 `보류` | 8-2 |
| `calc_status(value)` | 상태값 | `계산됨` / `자료 없음` / `입력 확인 필요` / `분모 0 · 계산 불가` | 9-1 |
| `month_label(month, same_year)` | `2026-04` | 같은 연도면 `4월`, 아니면 `2026년 4월` | — |
| `pair_label(m0, m1, same_year)` | `2026-01`,`2026-02` | `1→2월` / 연도가 다르면 `2026-12→2027-01` | — |
| `period_label(start, end)` | `2026-01`,`2026-06` | `2026년 1~6월` / 연도가 다르면 `2025년 12월~2026년 2월` | — |

- `pp_change`의 "0.01%p 미만"은 **중요성 기준이 아니라 표시 정밀도**다 (8-3). 판정 문구를 붙이지 않는다
- 절대값이 정확히 0.005 이상 0.01 미만이면 반올림하면 0.01이 되지만 규칙상 "0.01%p 미만"으로 표시한다 (실제 크기가 0.01보다 작음을 숨기지 않기 위해)

### 5-2. `web/evidence_state.py`

```python
@dataclass(frozen=True)
class EvidenceState:
    settings: Settings | None
    result: LoadResult | None
    errors: tuple[str, ...]          # 화면용 문구: 설정 오류·로드 오류·조합 차단 (전체 경로 없음, B-2)
    is_real: bool
    blocked: bool                    # 실제 자료 + 클라우드 LLM 조합 (B-4)

    @property
    def ok(self) -> bool             # errors가 비어 있고 result가 있음

    @property
    def badge(self) -> str           # 2-4 상단 표시 문구

def load_evidence_state(environ: Mapping[str, str] = os.environ) -> EvidenceState
    # 1) load_settings → SettingsError면 errors에 담고 종료
    # 2) load_evidence(settings.evidence_path) → EvidenceError면 errors에 담음 (file=None)
    # 3) is_real_evidence(path, file) 계산
    # 4) check_llm_data_combination → SettingsError면 errors에 담음
    # 예외를 밖으로 던지지 않는다 (서버가 죽지 않게)

@cache
def get_evidence_state() -> EvidenceState   # 프로세스에서 한 번만 읽음
```

- 근거 파일을 바꾸면 서버를 다시 시작한다 (개발 중 `--reload`는 코드 변경에만 반응하므로 README에 안내)
- 오류 문구는 1번에서 이미 수치를 출력하지 않도록 만들어져 있다
- **화면용 문구에는 서버의 전체 경로를 넣지 않는다** (B-2). 로더 문구의 경로 부분(`: C:\…\파일.json`)은 파일 이름으로 바꿔 `errors`에 담고, 원래 문구는 `logging`으로 서버 로그에만 남긴다

`badge` 규칙

| 상태 | 문구 |
|---|---|
| 합성 파일 정상 | `시연용 합성 수치 · demo-001` |
| 실제 파일 정상 | `실제 분석 자료 · {dataset_version} · 내부 검증용` |
| 오류 | `근거 파일 오류` |

### 5-3. `web/dependencies.py`

```python
SessionCookie = Annotated[str | None, Cookie(alias=COOKIE_NAME)]

def session_dep(session: SessionCookie = None) -> tuple[str, WorkState]
def evidence_state_dep() -> EvidenceState          # get_evidence_state() 호출. 테스트에서 교체
```

- 기존 `routes/input.py`·`routes/steps.py`에 흩어진 `SessionCookie` 정의를 이 파일로 모은다
- 단계 잠금은 라우트에서 명시적으로 확인한다 (2단계: `state.original is None` → `/step/1` 303)

### 5-4. `web/templates/error.html`

- 제목: "분석 근거 파일을 불러오지 못했습니다"
- 오류 문구 목록 (`<ul>`, 각 줄 그대로)
- 안내: "이 문구를 데이터 담당에게 전달해 주세요. 문구에는 금액·비중 수치가 포함되지 않습니다."
- 조치: [기획 입력으로] 링크
- HTTP 상태 503

### 5-5. 서버 시작 전 검사 (`__init__.py` `main()`)

```python
def main() -> None:
    state = get_evidence_state()
    if state.blocked:                              # 실제 자료 + 클라우드: 시작하지 않음 (B-4)
        raise SystemExit("\n".join(state.errors))
    for message in state.errors:                   # 그 밖의 오류: 경고만 출력하고 시작
        print(f"[근거 파일] {message}")
    uvicorn.run(...)
```

- `uvicorn` 명령으로 직접 켠 경우에는 이 검사가 빠지지만, 2단계 화면이 오류 화면이 되므로 실제 자료가 표시되지 않는다
- C 단계에서 LLM을 부르는 코드는 **호출 전 `get_evidence_state().blocked`와 `ok`를 확인해야 한다** → `llm/README.md` 요구사항으로 추가 (B-5)

### 5-6. 앱 등록과 공통 렌더링

- `app.py`: 라우터 등록 순서 **input → evidence → steps** (A-1). FastAPI는 먼저 등록된 주소 규칙이 이기므로, `/step/{step}`이 앞에 있으면 `/step/2`를 가로챈다. 순서 이유를 주석으로 남긴다
- `web/templating.py`
  - `formatting.py` 함수를 `templates.env.filters`에 등록
  - **`render(request, template, context, *, step, session_id, evidence, status_code=200)`** 하나로 모든 화면을 그린다 (A-3). 이 함수가 `evidence`, `step`, `state`를 문맥에 반드시 넣고 쿠키를 붙인다. 라우트는 `templates.TemplateResponse`를 직접 호출하지 않는다

---

## 6. 2-2 화면용 데이터 조립

### 위치

`web/evidence_view.py` — 라우트를 얇게 유지하고 문장·선택 규칙을 단위 테스트하기 위한 모듈. 템플릿에는 **원래 값(int·Fraction·None)**을 넘기고 필터로 표시한다. 문장만 이 모듈에서 완성한다.

### 데이터 형태

```python
@dataclass(frozen=True)
class MonthRow:
    month: str
    label: str                        # "4월"
    status: str                       # 상태값
    foreign_amount: int | None
    total_amount: int | None
    foreign_share_pct: Fraction | None     # 정수 금액으로 다시 계산 (파일 비중 사용 안 함)
    unknown_share_pct: Fraction | None
    known_only_share_pct: Fraction | None
    warnings: tuple[str, ...]
    held: bool                         # status != "ok"

@dataclass(frozen=True)
class PairRow:
    label: str                        # "1→2월"
    pair: MonthPair                   # 1번 계산 결과 그대로
    held: bool                        # pair.status == "skipped"

@dataclass(frozen=True)
class RecordView:
    evidence_id: str
    scope_label: str                  # "전국" / "시도 DEMO-SIDO-A"
    period_label: str                 # "2026년 1~6월"
    unit: str                         # "억원" | "원"
    months: tuple[MonthRow, ...]
    pairs: tuple[PairRow, ...]
    summary: PairSummary
    totals: PeriodTotals
    applicability: Applicability       # R07
    limitations: tuple[str, ...]

@dataclass(frozen=True)
class EvidenceView:
    main: RecordView | None            # 전국 레코드
    missing_main_message: str | None   # 전국 레코드가 없을 때
    region_note: str | None            # 입력 지역이 시도·시군구일 때
    status_kind: str                   # "allowed" | "needs_review" | "blocked"
    summary_sentence: str | None       # allowed일 때
    held_message: str | None           # needs_review·blocked일 때
    method_lines: tuple[str, ...]
    interpretation_notes: tuple[str, ...]
    hold_examples: tuple[RecordView, ...]
    chart_data: dict                   # JSON으로 넘길 값
    dataset_version: str
    data_kind: str

def build_evidence_view(plan: PlanInput, result: LoadResult) -> EvidenceView
```

- 비중은 **정수 금액으로 다시 계산한 값**을 표시한다. 파일의 `*_share_pct`는 1번에서 대조용으로만 쓰며, 불일치 경고가 있어도 화면은 금액 기준 값을 보여준다 (계산과 표시가 같은 원천을 쓰게)

### 레코드 선택

| 순서 | 규칙 |
|---|---|
| 1 | `find_record(file, "national", "ALL")`를 주 레코드로 사용 |
| 2 | 전국 레코드가 없으면 `main=None`, 문구 "전국 근거 레코드가 없어 비교를 표시하지 않습니다. 데이터 담당에게 확인해 주세요." |
| 3 | 입력 지역이 시도·시군구면 `region_note` = "전국 참고 자료입니다. 선택한 {지역 이름}의 소비를 진단한 결과가 아닙니다." (워크플로우 8-4, 12장 "전국 자료 + 특정 지역 기획") |
| 4 | 시도 레코드는 **사용자 지역과 연결하지 않는다.** 행정코드와 `region_key` 연결 규칙(C-2)이 정해지면 이 함수만 수정 |

### 금액 단위

- `unit = amount_unit(max(ok 월의 total_amount))`, ok 월이 없으면 `"원"`
- 주 레코드와 보류 사례 예시는 **각자** 단위를 정한다

### 적용 상태(R07)에 따른 표시

| `applicability.R07.status` | 요약 문장 | 표·차트 | 보류 문구 |
|---|---|---|---|
| `allowed` | 표시 | 표시 | 없음 |
| `needs_review` | **표시 안 함** | 표시 (자료 확인용) | "근거 기반 결론을 보류합니다. 확인할 내용: {reason}" |
| `blocked` | 표시 안 함 | **월별 표·구간 표·기간 합산·차트 모두 숨김** (A-6) | "현재 자료로 금액·비중 비교를 사용할 수 없습니다. 사유: {reason}" |

(워크플로우 9-2: needs_review는 결론 보류·확인할 내용 표시, blocked는 기능 실행 안 함 → 수치를 보여주지 않는다)

`EvidenceView`에 표시 여부 플래그를 둔다: `show_numbers`(blocked면 False), `show_summary`(allowed만 True).

### 요약 문장 규칙

변수: 기간 `P`(예: 2026년 1~6월), 비교 가능 구간 `N`, 비교 A 반대 `k`, 보류 구간 `m`

| 상황 | 문장 |
|---|---|
| `N ≥ 1`, `k ≥ 1` | "{P} 전국 자료에서 비교 가능한 {N}개 인접 월 구간 중 {k}개 구간에서 외국인 결제금액과 전체 분모 비중의 변화 방향이 달랐습니다." |
| `N ≥ 1`, `k = 0` | "{P} 전국 자료에서 비교 가능한 {N}개 인접 월 구간에서는 외국인 결제금액과 전체 분모 비중이 서로 반대 방향으로 움직이지 않았습니다." |
| `N = 0` | "비교 가능한 인접 월 구간이 없어 금액·비중 방향 비교를 보류합니다." |
| `m ≥ 1` (위 두 문장 뒤) | " 자료가 부족한 {m}개 구간은 비교하지 않았습니다." |

금지 사항 (테스트로 확인)
- "문제", "오류", "위험", "실패", "성공", "잘못" 단어
- `k = 0`일 때 "달랐습니다" 문구 사용 (12장 "방향이 같은 두 기간")
- "검토가 필요 없다"는 취지의 문장 (8-3: 반대 방향이 없다고 검토가 불필요한 것은 아님)

### 계산 방법 문구 (`method_lines`)

1. "전체 분모 외국인 비중 = 외국인 결제금액 ÷ 전체 결제금액 × 100"
2. "미상 제외 외국인 비중 = 외국인 결제금액 ÷ (전체 결제금액 − 미상 금액) × 100"
3. "변화 방향은 반올림한 비중이 아니라 원래 금액의 교차곱으로 판정합니다" (시안의 "정수 비교" 문구를 정정)
4. "기간 합산 비중은 월별 비중의 평균이 아니라 금액을 모두 더한 뒤 계산합니다"
5. "비교 A(금액 vs 전체 분모 비중)와 비교 B(전체 분모 vs 미상 제외 비중)는 따로 계산하며 하나의 점수로 합치지 않습니다"

### 해석 한계 (`interpretation_notes`)

- 파일의 `limitations` 전부
- 고정 문구 (최종기획서 4-2)
  - "금액과 비중은 서로 다른 질문에 답합니다. 방향이 다르다고 비중이 잘못된 지표라는 뜻은 아닙니다."
  - "이 자료는 계획 중인 사업이 시행되기 전의 관측이며, 사업의 성과가 아닙니다."
  - "‘미상’은 성별 코드가 확인되지 않은 결제이며, 미상 제외 비중이 더 정확한 값이라는 뜻은 아닙니다."

### 보류 사례 예시 (`hold_examples`)

- 조건 (A-6): **파일이 합성 자료(`data_kind: synthetic`)이고**, 주 레코드가 아니며, 그 레코드의 R07 적용 상태가 `blocked`가 아니고, `calculation_status != "ok"`인 월이 1개 이상 있는 레코드
  - 실제 파일에서는 표시하지 않는다: 사용자 지역과 무관한 실제 지역 수치가 "예시"라는 이름으로 보이는 것을 막기 위해서다
- 제목: "보류 사례 예시 — {scope_label}" + 안내 "사용자가 입력한 지역의 자료가 아닙니다. 자료가 부족할 때 화면이 어떻게 보이는지 보여주는 예시입니다."
- 월별 표와 구간 표만 표시 (차트·요약 문장 없음)
- 실제 파일에 해당 레코드가 없으면 빈 목록 → 섹션 숨김

### 차트 데이터 (`chart_data`)

```json
{
  "unit": "원",
  "labels": ["1월", "2월", "3월", "4월", "5월", "6월"],
  "status": ["ok", "ok", "ok", "ok", "ok", "ok"],
  "foreign_amount": [820, 790, 860, 905, 930, 880],
  "share_all_pct": [8.2, 8.4043, 8.3495, 8.5377, 8.5399, 8.2243],
  "share_known_pct": [8.5864, 8.8964, 8.8296, 8.996, 9.3093, 8.8531]
}
```

- **값은 `int`, `float`, `None`만 쓴다. `Fraction`·`Decimal`을 넣지 않는다** (A-4: JSON 변환 오류로 500). 테스트가 모든 값의 형식을 확인한다
- 금액: 단위가 원이면 정수 그대로, 억원이면 소수 둘째 자리 숫자
- 비중: 차트 좌표용으로 소수 넷째 자리까지 (화면 글자 표시는 필터로 둘째 자리)
- 계산할 수 없는 월은 모두 `null`
- 템플릿에서 `<script type="application/json" id="chart-data">{{ view.chart_data | tojson }}</script>`로 넘긴다

---

## 7. 2-3 근거 확인 화면

### 7-1. 라우트 `web/routes/evidence.py`

```python
@router.get("/step/2", response_class=HTMLResponse)
def show(request, session=Depends(session_dep), evidence=Depends(evidence_state_dep)):
    session_id, state = session
    if state.original is None:              → redirect("/step/1")
    if not evidence.ok:                     → error.html, 503
    view = build_evidence_view(state.original, evidence.result)
    → steps/evidence.html (step=2, state, view, evidence)
```

- `routes/steps.py`는 `/step/{step}`에서 `3 <= step <= 5`만 처리

### 7-2. 템플릿 `steps/evidence.html` 구성 (위에서 아래 순서)

| # | 구역 | 내용 | 조건 |
|---|---|---|---|
| 1 | 머리 | 제목 "근거 확인", 부제 "기획안의 성과지표와 관련된 소비 자료를 먼저 확인합니다. 판정이 아니라 확인할 점을 찾기 위한 자료입니다." | 항상 |
| 2 | 범위 칩 | `partials/evidence_badges.html`: `전국 참고 — 특정 지역의 진단이 아님` · `{기간}` · `자료 버전 {버전}` · `합성 자료`/`실제 자료` | 주 레코드 있음 |
| 3 | 지역 안내 | `region_note` (회색 안내 상자) | 입력 지역이 시도·시군구 |
| 4 | 요약 또는 보류 | `summary_sentence`(큰 글씨) 또는 `held_message`(회색 점선 상자) | 적용 상태별 |
| 5 | 차트 2개 | 왼쪽 "외국인 결제금액 ({단위})" 막대, 오른쪽 "외국인 결제 비중 (%)" 선 + [전체 분모 \| 미상 제외] 전환 버튼. 캔버스 아래 대체 문구 "같은 수치는 아래 표에 있습니다" | `show_numbers` |
| 6 | 월별 표 | `partials/month_table.html` | `show_numbers` |
| 7 | 인접 구간 표 | `partials/pair_table.html` | `show_numbers` |
| 8 | 기간 합산 | "기간 합산 (계산 가능 {n}개월)": 외국인 결제금액, 전체 분모 비중, 미상 제외 비중, 미상 비중. 제외한 월이 있으면 "{월} 제외" | `show_numbers` |
| 9 | 계산 방법·해석 한계 | 두 카드 나란히 (모바일 세로) | 항상 |
| 10 | 보류 사례 예시 | `<details>` 접힘: 제목 "자료가 부족한 경우 보기 (보류 사례 예시)" → 레코드마다 안내·범위 칩·월별 표·구간 표 | `hold_examples` 있음 |
| 11 | 이동 | [← 기획 입력으로] [검토 질문 보기 →] (`/step/3`) | 항상 |

### 7-3. 화면 조각

#### `partials/evidence_badges.html`

매크로 `evidence_badges(record_view, dataset_version, data_kind, is_main)`

| 칩 | 문구 |
|---|---|
| 범위 | 주 레코드 전국: `전국 참고 — 특정 지역의 진단이 아님` / 보류 예시: `{scope_label} (예시)` |
| 기간 | `2026년 1~6월` |
| 버전 | `자료 버전 demo-001` |
| 종류 | `합성 자료` / `실제 자료` |

#### `partials/month_table.html`

매크로 `month_table(record_view)`

| 월 | 외국인 결제금액 | 전체 분모 비중 | 미상 비중 | 미상 제외 비중 | 상태 |
|---|---|---|---|---|---|
| `row.label` | `amount(row.foreign_amount, unit)` | `pct` | `pct` | `pct` | `calc_status` (+ 경고가 있으면 줄바꿈 후 작은 글씨) |

- `held` 행: 클래스 `row-held` (회색 글자)
- 수치 칸: `font-variant-numeric: tabular-nums`, 오른쪽 정렬
- 표 감싸는 `<div class="table-scroll">` (모바일 가로 스크롤)
- `<caption>`: "월별 외국인 결제금액과 비중"

#### `partials/pair_table.html`

매크로 `pair_table(record_view)`

| 구간 | 외국인 금액 증감률 | 전체 금액 증감률 | 전체 분모 비중 변화 | 비교 A | 미상 제외 비중 변화 | 비교 B |
|---|---|---|---|---|---|---|
| `1→2월` | `growth` | `growth` | `pp_change` | `comparison` | `pp_change` | `comparison` |

- 보류 구간: 수치 칸을 합쳐(`colspan`) `보류 — {skip_reason}` 한 칸으로 표시, 클래스 `row-held`
- 비교 A "방향 다름" 칸: 클래스 `cmp-opposite` (굵은 글씨, **색은 본문색**) — 강조는 하되 경고색을 쓰지 않는다
- 표 머리 설명 한 줄: "비교 A: 외국인 금액과 전체 분모 비중의 방향 · 비교 B: 전체 분모와 미상 제외 비중의 방향"
- `<caption>`: "인접 월 구간별 변화"

### 7-4. `static/js/charts.js`

| 동작 | 내용 |
|---|---|
| 데이터 | `#chart-data` JSON만 사용. 숫자를 다시 계산하지 않음 |
| 라이브러리 확인 | `window.Chart`가 없으면 `.chart-fallback` 요소를 보이고 종료 |
| 금액 차트 | `#amount-chart`: 막대, y축 0부터, 툴팁 `{값}{단위}` (원: 천 단위 쉼표, 억원: 소수 둘째 자리) |
| 비중 차트 | `#share-chart`: 선, `spanGaps: false`, 점 표시, 툴팁 `{값}%`(둘째 자리)·상태 |
| 분모 전환 | `button[data-denominator="all" \| "known"]`, `aria-pressed` 갱신, 데이터셋 교체 후 `chart.update()` |
| 색 | 파랑 계열만 (`#2563EB`, 연한 막대 `#93B8F8`). 반대 구간 강조색 없음 |
| 전역 | 즉시 실행 함수로 감싸 전역 변수 없음 |

템플릿에서 스크립트 순서:

```html
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.5.1/dist/chart.umd.min.js"
        integrity="sha384-jb8JQMbMoBUzgWatfe6COACi2ljcDdZQ2OxczGA3bGNeWe+6DChMTBJemed7ZnvJ"
        crossorigin="anonymous"></script>
<script src="{{ url_for('static', path='js/charts.js') }}"></script>
```

- `integrity` 해시는 9/16에 CDN 파일(208,522바이트)을 받아 계산한 sha384 값이다 (B-1). CDN 파일이 바뀌면 브라우저가 실행을 거부하고 대체 문구가 보인다
- 버전을 올리면 해시를 다시 계산해 바꾼다

### 7-5. CSS 추가 구역 (`style.css` 7번 "근거 화면")

| 클래스 | 용도 |
|---|---|
| `.badge-row`, `.scope-badge` | 범위 칩 |
| `.notice`, `.notice-held` | 지역 안내(회색), 보류 문구(회색 점선) |
| `.summary-sentence` | 요약 문장 (20px, 굵게) |
| `.chart-grid`, `.chart-card`, `.chart-fallback` | 차트 2열(900px 이하 1열), 대체 문구 |
| `.segmented-buttons` | 분모 전환 버튼 (입력 화면 세그먼트와 같은 모양) |
| `.table-scroll`, `.data-table`, `.num` | 표, 가로 스크롤, 숫자 오른쪽 정렬 |
| `.row-held` | 보류 행 회색 |
| `.cmp-opposite` | 방향 다름 굵게 (본문색) |
| `.info-grid`, `.info-card` | 계산 방법·해석 한계 카드 |
| `details.hold-examples` | 보류 사례 접힘 영역 |

- 빨간색 계열 클래스를 추가하지 않는다
- 400px 폭에서 본문 가로 스크롤이 생기지 않고, 표만 안에서 스크롤

---

## 8. 2-4 상단 자료 표시

- `base.html`의 `<span class="tag">시연용 합성 수치</span>` → `<span class="tag">{{ evidence.badge }}</span>`
- 모든 라우트가 `evidence_state_dep`를 받아 템플릿 문맥에 `evidence`를 넣는다 (`routes/input.py`, `routes/steps.py`, `routes/evidence.py`, 오류 화면)
- 오류 상태면 칩에 `tag-error` 클래스 (회색 테두리, 경고색 아님)
- 하단 고지 추가: "화면의 수치는 {합성이면 '시연용 합성 수치'}입니다 · BC카드 공식 서비스가 아닙니다 · 공모전 제안용"

---

## 9. 합성 파일 기준 기대 화면 값

`review_evidence_demo_v1.json`, 예시 기획(강원 강릉시)으로 2단계를 열었을 때. 테스트와 브라우저 확인의 기준이다.
반올림 규칙(사사오입, 소수 둘째 자리)을 적용한 값이며, 9/16에 합성 파일과 1번 계산 코드로 모든 칸을 계산해 확인했다.

### 상단·안내

| 항목 | 기대 값 |
|---|---|
| 상단 칩 | `시연용 합성 수치 · demo-001` |
| 범위 칩 | `전국 참고 — 특정 지역의 진단이 아님` · `2026년 1~6월` · `자료 버전 demo-001` · `합성 자료` |
| 지역 안내 | "전국 참고 자료입니다. 선택한 강원특별자치도 강릉시의 소비를 진단한 결과가 아닙니다." |
| 요약 문장 | "2026년 1~6월 전국 자료에서 비교 가능한 5개 인접 월 구간 중 2개 구간에서 외국인 결제금액과 전체 분모 비중의 변화 방향이 달랐습니다." |
| 금액 단위 | 원 |

### 월별 표 (`DEMO-R07-NATIONAL`)

| 월 | 외국인 결제금액 | 전체 분모 비중 | 미상 비중 | 미상 제외 비중 | 상태 |
|---|---:|---:|---:|---:|---|
| 1월 | 820원 | 8.20% | 4.50% | 8.59% | 계산됨 |
| 2월 | 790원 | 8.40% | 5.53% | 8.90% | 계산됨 |
| 3월 | 860원 | 8.35% | 5.44% | 8.83% | 계산됨 |
| 4월 | 905원 | 8.54% | 5.09% | 9.00% | 계산됨 |
| 5월 | 930원 | 8.54% | 8.26% | 9.31% | 계산됨 |
| 6월 | 880원 | 8.22% | 7.10% | 8.85% | 계산됨 |

### 인접 구간 표

| 구간 | 외국인 금액 증감률 | 전체 금액 증감률 | 전체 분모 비중 변화 | 비교 A | 미상 제외 비중 변화 | 비교 B |
|---|---:|---:|---:|---|---:|---|
| 1→2월 | -3.66% | -6.00% | +0.20%p | **방향 다름** | +0.31%p | 방향 같음 |
| 2→3월 | +8.86% | +9.57% | -0.05%p | **방향 다름** | -0.07%p | 방향 같음 |
| 3→4월 | +5.23% | +2.91% | +0.19%p | 방향 같음 | +0.17%p | 방향 같음 |
| 4→5월 | +2.76% | +2.74% | 0.01%p 미만 (증가) | 방향 같음 | +0.31%p | 방향 같음 |
| 5→6월 | -5.38% | -1.74% | -0.32%p | 방향 같음 | -0.46%p | 방향 같음 |

### 기간 합산

외국인 결제금액 5,185원 · 전체 분모 비중 8.38% · 미상 제외 비중 8.92% · 미상 비중 6.03% (계산 가능 6개월)

### 보류 사례 예시 (`DEMO-R07-SIDO-HOLD`, 접힘)

| 월 | 외국인 결제금액 | 전체 분모 비중 | 미상 비중 | 미상 제외 비중 | 상태 |
|---|---:|---:|---:|---:|---|
| 1월 | 120원 | 8.00% | 6.00% | 8.51% | 계산됨 |
| 2월 | 135원 | 8.54% | 6.33% | 9.12% | 계산됨 |
| 3월 | 자료 없음 | 계산 불가 | 계산 불가 | 계산 불가 | 자료 없음 (해당 월 관측 행 없음) |
| 4월 | 128원 | 8.31% | 6.17% | 8.86% | 계산됨 |
| 5월 | 0원 | 계산 불가 | 계산 불가 | 계산 불가 | 분모 0 · 계산 불가 (전체 금액 0으로 비중 계산 불가) |
| 6월 | 131원 | 8.40% | 6.28% | 8.96% | 계산됨 |

| 구간 | 표시 |
|---|---|
| 1→2월 | +12.50% · +5.33% · +0.54%p · 방향 같음 · +0.61%p · 방향 같음 |
| 2→3월, 3→4월 | 보류 — 2026-03 자료 없음(no_data) |
| 4→5월, 5→6월 | 보류 — 2026-05 분모 0(invalid_denominator) |

- 5월 금액 "0원": `invalid_denominator` 월은 금액이 0으로 들어 있어 그대로 표시하고, 비중은 "계산 불가"로 표시한다. `no_data` 월의 금액은 null이라 "자료 없음"
- 보류 사례 예시에는 기간 합산을 표시하지 않는다

---

## 10. 테스트 전체 목록

### `tests/conftest.py` (신규, A-2)

모든 테스트에 자동 적용되는 준비 단계(`autouse` fixture):

1. `PSM_`로 시작하는 환경변수를 모두 지운다 (셸·`.env` 설정이 테스트에 들어오지 않게)
2. `get_evidence_state.cache_clear()`
3. `app.dependency_overrides[evidence_state_dep]`를 합성 파일 상태로 고정
4. 테스트가 끝나면 `app.dependency_overrides`를 원래대로 되돌리고 캐시를 비운다

사례 파일이 필요한 테스트는 `use_evidence(path)` fixture로 교체한다.

### `tests/test_formatters.py` (신규)

| 테스트 | 확인 |
|---|---|
| `test_amount_unit_switches_at_one_hundred_million` | 99,999,999 → 원, 100,000,000 → 억원 |
| `test_amount_in_eok_and_won` | 210,618,000,000 → `2,106.18억원`, 820 → `820원`, 1,234,567 → `1,234,567원` |
| `test_none_is_never_zero` | 모든 필터에 None → `0` 문자열 포함 안 됨 |
| `test_pct_rounds_half_up_exactly` | `Fraction(2675, 1000)` → `2.68%` (float 반올림이면 2.67이 되는 경계) |
| `test_pp_change_sign_and_zero` | +0.2043 → `+0.20%p`, -0.0547 → `-0.05%p`, 0 → `0.00%p` |
| `test_pp_change_below_display_precision` | `Fraction(22, 10000)` → `0.01%p 미만 (증가)`, `Fraction(-4, 10013)` → `0.01%p 미만 (감소)` |
| `test_pp_change_just_below_point_zero_one` | 0.009 → `0.01%p 미만 (증가)` (반올림하지 않음) |
| `test_growth_none_is_not_computable` | None → `계산 불가` |
| `test_direction_and_comparison_labels` | 모든 값 대응, skipped → `보류` |
| `test_calc_status_labels` | 상태 4개 |
| `test_month_pair_period_labels` | 같은 연도·연도 넘김 |
| `test_filters_registered_in_templates` | `templates.env.filters`에 모든 필터 이름 존재 |
| `test_small_nonzero_values_are_not_shown_as_zero` | 비중 `Fraction(3,1000)` → `0.01% 미만`, 증감률 `Fraction(1,2000)` → `0.01% 미만 (증가)`, 억원 400,000 → `0.01억원 미만` (A-5) |
| `test_formatting_module_has_no_web_imports` | `formatting.py`가 fastapi·jinja2·web을 import하지 않음 (B-3) |

### `tests/test_evidence_state.py` (신규)

| 테스트 | 확인 |
|---|---|
| `test_default_state_loads_demo` | ok, badge `시연용 합성 수치 · demo-001`, is_real False |
| `test_invalid_file_is_error_state_not_exception` | `PSM_EVIDENCE_PATH`=오류 사례 → ok False, errors 1개, 예외 없음 |
| `test_missing_file_is_error_state` | 없는 경로 → errors "찾을 수 없습니다" |
| `test_invalid_settings_is_error_state` | `PSM_LLM_PROVIDER=gpt` → errors |
| `test_real_with_cloud_is_blocked` | `private/` 경로 사본 + cloud → blocked True |
| `test_real_badge` | real 레코드 파일 → `실제 분석 자료 · … · 내부 검증용` |
| `test_error_messages_do_not_expose_full_path` | 없는 경로 → 화면 문구에 파일 이름만, 상위 폴더 경로 없음 (B-2) |

### `tests/test_evidence_view.py` (신규)

| 테스트 | 사례 | 확인 |
|---|---|---|
| `test_demo_summary_sentence` | 합성 | 9장 요약 문장 그대로 |
| `test_same_direction_sentence_has_no_opposite_wording` | `same_direction` | "달랐습니다" 없음 |
| `test_all_skipped_sentence` | `missing_month` | "보류합니다" |
| `test_skipped_pairs_are_mentioned` | 보류 1개 이상 사례 | "비교하지 않았습니다" |
| `test_sentences_avoid_judgment_words` | 전체 정상 사례 | 금지 단어 없음 |
| `test_region_note_for_sigungu_plan` | 합성 + 강릉시 | 9장 지역 안내 문구 |
| `test_no_region_note_for_national_plan` | 합성 + 전국 | None |
| `test_unit_is_won_for_small_amounts_and_eok_for_large` | 합성 / `large_amounts` | 원 / 억원 |
| `test_needs_review_hides_summary` | R07 needs_review로 바꾼 사례 | summary None, held_message에 reason |
| `test_blocked_hides_all_numbers` | R07 blocked | `show_numbers` False, 보류 문구에 사유 (A-6) |
| `test_hold_examples_from_demo` | 합성 | 보류 예시 1개(`DEMO-R07-SIDO-HOLD`), 보류 구간 4개 |
| `test_no_hold_examples_when_only_national` | `amount_up_share_down` | 빈 목록 |
| `test_no_hold_examples_for_real_files` | 합성 파일을 real로 바꾼 사본 | 빈 목록 (A-6) |
| `test_blocked_record_is_not_a_hold_example` | 시도 레코드 R07을 blocked로 바꾼 사본 | 빈 목록 (A-6) |
| `test_chart_data_contains_only_json_types` | 합성 | 모든 값이 int·float·None·str, `json.dumps` 성공 (A-4) |
| `test_missing_national_record` | 시도 레코드만 있는 파일 | main None, 안내 문구 |
| `test_chart_data_keeps_nulls` | `missing_month` | 2월 값 모두 null |
| `test_shares_are_recomputed_from_amounts` | 비중을 틀리게 적은 파일 | 화면 값은 금액 기준 |

### `tests/test_routes.py` (추가)

| 테스트 | 확인 |
|---|---|
| `test_step_two_requires_original` | 기존 테스트 유지 (원안 없이 → `/step/1`) |
| `test_step_two_shows_demo_evidence` | 예시 제출 후 `/step/2`: 요약 문장, "전국 참고 — 특정 지역의 진단이 아님", 월별 표 6행, 구간 표 5행 |
| `test_step_two_shows_tiny_change_label` | "0.01%p 미만 (증가)" |
| `test_step_two_has_no_red_warning_classes` | 응답 HTML에 `danger`·`error-red` 등 경고 클래스 없음 |
| `test_step_two_chart_data_is_escaped_json` | `<script type="application/json" id="chart-data">` 안에 `</script>` 문자열 없음, JSON 파싱 가능 |
| `test_step_two_error_page_when_evidence_fails` | 의존성 교체로 오류 상태 → 503, 오류 문구 표시, 수치 없음 |
| `test_step_one_still_works_when_evidence_fails` | 오류 상태에서도 `/step/1` 200 |
| `test_top_badge_shows_dataset_version` | `/step/1` 상단 칩 `시연용 합성 수치 · demo-001` |
| `test_step_three_placeholder_still_reachable` | `/step/3` 임시 화면 |
| `test_step_two_route_is_not_shadowed_by_placeholder` | `/step/2` 응답이 임시 화면이 아닌 근거 화면 (A-1) |
| `test_every_page_renders_top_badge` | `/step/1`, `/step/2`, `/step/3`, 오류 화면, 1단계 422 응답 모두 상단 칩 포함 (A-3) |
| `test_blocked_evidence_page_has_no_numbers` | R07 blocked 사례 → 표·차트 데이터 없음 (A-6) |
| `test_chart_script_has_integrity` | Chart.js `<script>`에 `integrity="sha384-…"`와 `crossorigin` (B-1) |

### 브라우저 확인 (수동, 결과를 checks.md에 기록)

1. 예시 기획 채우기 → 검토 시작 → 2단계
2. 9장 값과 화면 표 대조 (요약 문장, 월별 표, 구간 표, 기간 합산)
3. 차트 2개 표시, [미상 제외] 전환 시 선 데이터 변경, 콘솔 오류 없음
4. "자료가 부족한 경우 보기" 펼침, 보류 구간 표시
5. CDN 실패 시 대체 문구: 로컬에서 스크립트 주소를 임시로 잘못된 값으로 바꿔 새로고침 → 대체 문구 표시 확인 → 되돌림 (커밋하지 않음, C-2)
6. 창 폭 400px: 본문 가로 스크롤 없음, 표만 스크롤

### 예상 테스트 수

| 파일 | 개수 |
|---|---:|
| test_formatters.py | 14 |
| test_evidence_state.py | 7 |
| test_evidence_view.py | 18 |
| test_routes.py 추가 | 12 |
| **합계** | **51** (현재 119 → 약 170) |

---

## 11. 커밋과 기록

| 순서 | 커밋 메시지 | 포함 |
|---|---|---|
| 1 | `2-1 표시 형식 필터와 근거 로드 공통` | formatting.py, evidence_state.py, dependencies.py, error.html, `__init__.py` 시작 전 검사, templating 필터 등록·`render()`, 기존 라우트를 `render()`로 전환, conftest.py, test_formatters.py, test_evidence_state.py, `llm/README.md` 호출 전 확인 요구사항 |
| 2 | `2-2 근거 화면 데이터 조립` | evidence_view.py, test_evidence_view.py |
| 3 | `2-3 근거 확인 화면` | routes/evidence.py, steps.py 수정, app.py, evidence.html, partials 3개, charts.js, style.css, test_routes.py 추가, 브라우저 확인 기록 |
| 4 | `2-4 상단 자료 표시` | base.html, 모든 라우트 문맥, 하단 고지, test_routes.py 추가 |

커밋마다 할 일

1. `uv run pytest` 전체 통과
2. `uv run python scripts/check_public_bundle.py` 위반 0
3. README 동기화
   - `web/README.md`: `evidence_state.py`, `evidence_view.py` 추가, `formatters.py` 항목을 루트 `formatting.py`로 이동 표기
   - `src/policy_signal_map/README.md`: 루트 파일 표에 `formatting.py`
   - `routes/README.md`: `evidence.py` 있음, `steps.py` 3~5단계
   - `partials/README.md`: `pair_chips.html` → `pair_table.html` (결정 ③)
   - `static/js/README.md`: `charts.js` 있음, 데이터 형식 갱신
   - `static/css/README.md`: 근거 화면 구역 있음
   - 루트 `README.md`: 근거 파일 교체 시 서버 재시작 안내
4. [작업진행.md](./작업진행.md) 2장 체크박스, 2-2 설명의 "화면 조각 3개" 이름 갱신
5. [checks.md](./checks.md) 진행 기록, 4장 시험 결과 칸 갱신 ("전국 자료 + 특정 지역 기획", "아주 작은 방향 차이", "방향이 같은 두 기간"의 화면 부분)

푸시: 커밋 4 이후 개발 담당에게 확인받은 뒤

---

## 12. 완료 기준

- [ ] 예시 기획으로 2단계를 열면 9장 기대 값과 화면이 일치
- [ ] 요약 문장 3종이 규칙대로 나오고, 반대 구간이 없을 때 "달랐습니다" 문구가 없음
- [ ] 입력 지역이 시군구일 때 "전국 참고" 안내가 보이고, 지역 진단처럼 표시하지 않음
- [ ] 1~6월 전체 표와 모든 인접 구간이 보이고, 반대 구간만 골라 보여주지 않음 (8-3)
- [ ] 0.01%p 미만 변화가 "0.01%p 미만 (증가)"로 표시되고 경고색이 없음
- [ ] 비교 A와 B가 별도 열로 표시됨
- [ ] 자료 없음·분모 0 월이 0%로 표시되지 않고, 해당 구간이 보류로 표시됨
- [ ] 적용 상태 needs_review·blocked일 때 결론 문장을 내지 않음 (9-2)
- [ ] 근거 파일 오류 시 2단계는 오류 화면, 1단계는 정상
- [ ] 실제 자료 + 클라우드 LLM 설정이면 `uv run policy-signal-map`이 시작하지 않음
- [ ] 모든 화면 상단에 불러온 파일의 종류·버전 표시
- [ ] 차트 2개와 분모 전환 동작, CDN 실패 시 대체 문구, 콘솔 오류 없음
- [ ] 400px 폭에서 본문 가로 스크롤 없음
- [ ] 적용 상태 blocked일 때 수치가 하나도 표시되지 않고, 실제 파일에서는 보류 사례 예시가 나오지 않음
- [ ] 셸에 `PSM_` 환경변수가 있어도 테스트 결과가 같음
- [ ] 오류 화면에 서버 전체 경로가 없음
- [ ] `uv run pytest` 약 170개 통과, 공개 자료 검사 위반 0
- [ ] README·작업진행.md·checks.md 갱신

---

## 13. 위험과 대응

| 위험 | 가능성 | 영향 | 대응 |
|---|---|---|---|
| CDN 차단 환경(내부 시연)에서 차트가 안 뜸 | 중 | 차트 없음 | 모든 수치가 표에 있음. 대체 문구 표시. 필요 시 `static/vendor/chart.umd.min.js`로 내려받아 교체 (라이선스 MIT, 파일 머리 주석 유지) |
| C-2(행정코드 ↔ region_key) 미결정 | 높음 | 시도 자료가 있어도 전국 참고로만 표시 | 안전한 쪽 동작. 결정되면 `evidence_view.py`의 레코드 선택 함수 1곳 수정, 3번 전 결정 필요 |
| 실제 파일 월 수가 6개가 아님 | 중 | 표·문장 어긋남 | 월 수·기간을 파일에서 읽고 "1~6월", "5개"를 코드에 고정하지 않음 (테스트: 3개월 사례로 문장 확인) |
| 요약 문장이 근거 이상으로 단정한다는 검수 의견 | 중 | 문구 수정 | 문장 규칙을 이 문서 6장 한곳에 두고 테스트로 고정, 3-1 규칙 문서 검수 때 데이터 담당에게 함께 확인 |
| 데이터 담당 Q1(미상=전체 상태값) 답이 다름 | 낮음 | 없음 | 화면은 상태값·계산 결과를 그대로 표시 |
| 근거 파일 교체 후 서버를 다시 켜지 않음 | 중 | 옛 파일 표시 | 상단 칩에 버전이 보여 확인 가능, README에 재시작 안내 |
| 실제 파일로 화면을 캡처해 공개 저장소에 올림 | 낮음 | 보안서약 위반 | 상단 칩에 "실제 분석 자료 · 내부 검증용"이 찍혀 구분됨, `docs/screenshots/README.md` 원칙 재확인 |

---

## 14. 뒤 단계와 연결되는 점

| 뒤 단계 | 이 작업에서 넘겨주는 것 |
|---|---|
| 3 검토 질문 | `evidence_state_dep`, `EvidenceView`의 요약 수치(`PairSummary`)와 적용 상태 — 3번 규칙이 같은 레코드 선택 결과를 쓰도록 `build_evidence_view`의 레코드 선택 함수를 별도 함수(`select_main_record`)로 분리해 재사용 |
| 3 검토 질문 | `partials/evidence_badges.html` (질문 카드 옆 범위 표시) |
| 5 보완 기획안 | 루트 `formatting.py`의 함수(`pct`, `pp_change` 등)를 `document/`가 그대로 import해 화면과 문서의 숫자 표기를 같게 한다 (B-3) |
| C 단계 LLM | `EvidenceState.blocked`·`ok` — LLM 호출 전 확인 (B-5) |
| 7 배포 | 오류 화면 503, 상단 칩으로 배포본이 합성 파일인지 확인 |
